# -*- coding: utf-8 -*-
"""更正：「|S|=2 找的两对不是关键那对」是错的。两个 inner 都逮到同一对，
分歧在【选了哪个配置】。把机制换成审计记录实证的版本。"""
import pathlib, sys

P = pathlib.Path("/home/lmy/iot-device-classification/results/"
                 "config_derivation_exploratory_20260905/README.md")
t = P.read_text(encoding="utf-8")

OLD = """注意差距不是"修得多"：`|S|=2` 过 **2 对**只得 +0.0050，`|S|=1` 过 **1 对**得 +0.0188 ——
`|S|=1` 找对了 `Light_T1|Sensor`，`|S|=2` 找的两对不是关键那对。
**闸门的灵敏度取决于 inner 怎么切，不取决于闸门本身。**

### 为什么 `|S|=1` 更灵敏（机制猜想，扫描中）

轮次几何（目录名实证）：`R2/R3/R4 = normal`（同位置同操作）、`R5 = positionB`、
`R6/R7 = jitter`。**没有任何 inner 任务里含位置漂移**，所以"inner 复现 outer 的
位移 regime"这个说法不成立（曾这样解释过，随即被目录名否掉）。

剩下的猜想是**放大**：R2/R3/R4 之间只差时间与噪声。训两轮 → 模型见到两份 rssi 实现、
自动把它降权 → 在位者在 R4 上正常（0.8384）→ 闸门看不见病；训一轮 → 模型死抓这一轮的
rssi → 在位者在 R3 上崩（0.0029）→ 闸门看见病。outer 训三轮降权更多，但 R5 的位置位移
远大于 normal 轮次间漂移，**照样崩**。即 `|S|=1` 是**用小位移把同一个病放大到可见的
压力测试**，不是同分布代理。"""

NEW = """~~注意差距不是"修得多"：`|S|=1` 找对了 `Light_T1|Sensor`，`|S|=2` 找的两对不是关键那对。~~
**这句是错的**（`inner_sweep` 与 `_ckpt_abl` 审计记录推翻）：**两个 inner 都逮到了
`Light_T1|Sensor`**，分歧不在选哪些对，在**选了哪个配置**。

### 机制（审计记录实证，非猜想）

同一对 `Light_T1|Sensor`，两个 inner 的逐对审计：

```
|S|=2  R2+R3→R4   可判决区 n=619   在位者 0.8384 → 配置 0.8627   掩 interarrival / xgboost
                             seed43  在位者 0.8437 → 配置 0.8644   掩 interarrival / xgboost
|S|=1  R2→R3                        在位者 0.0029 → 配置 0.9046   掩 rssi        / lr
```

**两个都不是搜索失误 —— 各自在各自的可判决区上都是真 argmax。**
（`cfg_hybrid` 实测：`lr/掩rssi` 在 `|S|=2` 的可判决区上只有 0.8498，确实低于
`xgboost/掩interarrival` 的 0.8627。）区别在于两个区被**不同的失效模式**主导：

| inner | 可判决区里主导的失效 | 胜出配置 | 在 R5 上 |
|---|---|---|---|
| `\\|S\\|=2` R2+R3→R4 | 普通混淆（在位者 0.84） | **高容量** xgboost + 掩 interarrival | 无用 |
| `\\|S\\|=1` R2→R3 | rssi 漂移导致的系统性判反（在位者 0.003） | **低容量** lr + 掩 rssi | 兑现 +0.057 |

而 **R5 的失效就是 rssi 漂移**。所以准确的说法不是"`|S|=1` 更敏感"，而是：
**inner 决定了可判决区里哪种病占主导，搜索就去修那种病。**
选对 inner ＝ 让 inner 的主导病与部署的主导病一致。

轮次几何（目录名实证）：`R2/R3/R4 = normal`、`R5 = positionB`、`R6/R7 = jitter` ——
**没有任何 inner 含位置漂移**，所以曾提出的"inner 复现 outer 的位移 regime"不成立，
已作废。`|S|=1` 起作用是因为**零 rssi 多样性使 rssi 依赖在任一其它轮次上立刻暴露**，
从而让 rssi 病主导可判决区；这是"制造主导病"，不是"复现 regime"。

两条附带：

1. `|S|=2` 是**擦边过闸**：0.8627 vs 门槛 0.8584（在位者+MARGIN），只多 0.0043。
   它开了火，但开的是**错的火**。
2. 又一次 [[always-rf-is-a-finding-not-a-baseline]]：**在轻松的 inner 上选，选出高容量；
   在受压的 inner 上选，选出 LR。** 这次是在"配置搜索会选什么"这一层上出现。

### `cfg_hybrid`：验收是白送的

`lr/掩rssi` 在 `|S|=2` 可判决区上 0.8384 → **0.8498（+0.0113）** ——
修复**不是**拿源域范围内准确率换范围外准确率，**两边都更好**。
`|S|=2` 看不见它，是因为 +0.0113 **低于 MARGIN=0.02**，不是因为它在那儿变坏。
故"在 `|S|≥2` 上不添乱"这条验收对本例零成本，可以常开。"""

if OLD not in t:
    sys.exit("未找到待替换段落")
P.write_text(t.replace(OLD, NEW), encoding="utf-8")
print("机制段已更正")
