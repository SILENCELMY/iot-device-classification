# 跨线讨论 20260830：UDS 与元层失配的同源性，及路线 C 的可能形状

**性质**：**讨论文档。非协议、非判定、不冻结。**
本文件**不修改** `docs/experiment_protocol_final.md`（FROZEN 2026-08-25）、**不修改**独立线任何冻结协议、
**不重开**任何已落定判定（E2、G1、M1/M1-R/M2）。文中"建议"一律不具约束力，落地前须各自走冻结程序。
**作者**：Fable 5（主线设计与审阅方）
**读者**：Sol（独立线 M 系列设计与裁定方）、项目负责人
**日期**：2026-08-30
**起源**：项目负责人提出"UDS 与独立线发现的问题本质相同"，本文件是该讨论的固化记录与待决项分派。

---

## 0. 目的与边界

1. **目的**：把一次口头讨论固化成可被双线共同引用的文本，避免事实只存在于聊天记录里。
2. **边界**：本文件区分三类陈述并逐条标注——
   - **【事实】**：已落盘、可由文件核对的数字；
   - **【判定】**：已按预注册规则作出、有判定书的结论；
   - **【假设】**：本次讨论提出、**尚未检验**的解释性论断。
3. **不做的事**：不做 scope 裁决（EC-MDM 以何身份进正文由项目负责人与导师在 9/22 写作检查点决定）；
   不替对方线做设计裁定；不因事后解释追改任何判定。

---

## 1. 事实基线（供双线共同引用，出处逐条可核）

### 1.1 主线已落定

| 项 | 内容 | 出处 |
|---|---|---|
| 【判定】E1 | 协议 §9.1 分组 OOF（B 臂）下 Stacking 崩溃**未消失反而加深**：旗舰 `loro_R2_R4_to_R3` gain −0.0662→−0.1057；`loro_R2_R3_to_R4` −0.0042→−0.2225；jitter 三任务 ≈−0.005→≈−0.08；IID 三任务两臂无差；唯一例外 `position→R5` 反转 +0.0244 | `results/e1_oof_arms/E1_CONCLUSION.md` |
| 【事实】E1 网格 | 150 任务上 A→B：\|S\|=2 −0.0388→−0.0931、\|S\|=3 −0.0335→−0.0541、**\|S\|=1（时间块 B）无加深** −0.0764→−0.0700 → 加深特异于**跨轮分组 OOF** | `results/e1_oof_arms_g0/` |
| 【判定】E2 | 控制逐类 recall 变化后：`CPD_y` ΔR²=+0.0066、f²=0.008、β −0.245 [−0.757, **+0.320**] 跨零、LOEO-CV **变差 4.6%** → **增量解释力消失**；`CPD_dir`(lenient, n=119) ΔR²=+0.0748、β −0.296 [−0.556, −0.175] 稳健但 LOEO-CV 变差 3.9% → **仅条件关联，不达机制性量门槛**。走 §13 降级分支：贡献 2 降为"失效模式的结构化描述" | `results/e2_conditional/E2_CONCLUSION.md` |
| 【事实】E2 敏感性 | 用历史随机 OOF（A 臂）作 DV 时增量**虚高**：M1 +0.267、M2-lenient +0.482（诚实口径为 +0.007 / +0.075）→ 旧口径同时抬高 CPD-gain 的表观强度 | 同上 §4 |
| 【判定】G1 | 路线 A 五条中**三条失败**：①环境等权平均 regret 0.0247 > 0.01；②最差环境 R7 0.0361 > 0.03；③vs always-Stacking 改善但 **vs always-RF 反而差 +0.0044**；④非 RF 且 regret ≤ 2e-3 共 7 任务（τ=0 为 6）通过；⑤无标签审计通过。转 §17.3 路线 B | `results/g1_route_a/G1_VERDICT.md` |
| 【事实】G1 内层 | 各 fold **内层最优** regret = 0.0081 / 0.0156 / 0.0185 / 0.0216 / 0.0219 / 0.0238，**5/6 fold 样本内即达不到 0.01**；阈值网格每 fold 4830 点、无并列歧义 | `g1_fold_params.csv` |
| 【事实】G1 oracle | 三候选事后最优分布 RF 63 / soft 49 / stacking 37 / 并列 1，oracle 均值高于 always-RF → **可分空间真实存在** | `g1_overall.csv` |
| 【事实】D8 | OOD 150 任务 5-class 均值：RF 0.6870 > 校准 soft 0.6670 > hard 0.6595 > 等权 soft 0.6559 > Stacking 0.6280（oracle 最佳单模 0.7009）。固定策略环境等权 regret：**always-RF 0.0203**（最差环境 0.0318）、always-soft 0.0459、always-Stacking 0.0793。lgb/xgb 源域 OOF 系统性过自信（温度中位 5.24 / 2.63，162/162 任务 T>1），RF 近校准（0.96） | `results/g0_environment_grid/voting_baselines.csv`、`G1_VERDICT.md` §4 |

### 1.2 独立线已落定（转自冻结事实卡，未读中间产物）

| 项 | 内容 | 出处 |
|---|---|---|
| 【事实】M1 | 固定目标域三基模型概率、仅换元决策关系：OOD 环境等权 MMG = **0.2029**，IID time-block MMG = 0.0085，差 0.1944；6/6 环境为正 | `results/meta_mismatch_exploratory/m1/m1_gate.json` |
| 【事实】M1-R | 完全匹配目标样本/时间/类别/五折后 matched MMG = **0.1919**，OOD−IID = 0.1834；工程门 PASS（双跑哈希一致、source 复现误差 0.0） | `m1r_matched_control/m1r_gate.json` |
| 【判定】M2 | 嵌套受限适配器的 OOD−IID 超额恢复与 `ER`（以完整 target-CF 为 ceiling）：**I 类别阈值 0.0810 / ER 0.442 / 2-6 环境达 0.5——不充分；G 全局块缩放 0.1238 / 0.675 / 5-6——不充分；C 类别条件块缩放 0.1634 / ER 0.891 / 6-6——首个充分；F ceiling 0.1834 / 1.000**。冻结分类 `CLASS_CONDITIONAL_BLOCK_REWEIGHTING_SUFFICIENT` | `independent/meta_mismatch/M2_FACT_CARD.md`（sha256 已冻结） |
| 【事实】M2 口径 | `ER` 已**减去**同日 IID 标签辅助重拟合收益，只保留环境条件的超额部分；raw recovery 仅作敏感性。F 明确标注为**有标签离线 ceiling** | 同上 §1 |
| 【事实】M3 | UNSW 四日 pcap 派生特征上的 EC-MDM 外部存在性（H-U1）与亚型复现（H-U2），160 步双跑，2026-08-30 17:32 起以 user systemd transient service 挂起（前一次尝试在 49/160 因 PTY 消失中断、未写输出、不计为有效运行） | `PROTOCOL_M3_UNSW.md`、`M3_PERSISTENT_RUN_RECORD.json` |

### 1.3 一个值得双线都注意的巧合（已核）

M2 的 `excess_F = 0.1833663976` 与 M1-R 的 `OOD−IID = 0.18336639759` **逐位相同**——说明 M2 的 ceiling
定义与 M1-R 的匹配支撑口径严格对齐，两个实验之间没有口径漂移。这是独立线内部自洽性的一个便宜的交叉验证点。

---

## 2. 核心论点：UDS 与 source Stacking 元层同源（负责人提出，Fable 5 认同并锐化）

**【假设】** —— 本节全部内容为**尚未检验的解释性论断**，可检验路径见 §4。

### 2.1 结构同源

两者都是"**在源域上确定的、关于基模型输出的关系，被原封不动搬到目标域使用**"：

- Stacking：`g_s : Z_t → ŷ`，其中 `Z_t` 是目标域三基模型概率拼接；
- UDS：以**源侧**分歧结构（OOF 预测构成）为参照，把目标侧分歧结构（测试预测构成）的偏离量映射为风险分。

两者共享同一个前提假设——**源域学到的那个关系在目标域仍然成立**。而 EC-MDM 的实测结论正是：
该假设在**类别条件**维度上失效。因此 UDS 有它自己的 EC-MDM。

### 2.2 用 M2 的阶梯说得更精确

`UDS = mean over 6 ordered model pairs of ||Off(G_src) − Off(G_tgt)||_F`（协议 §4.2）。两次压缩：
**对模型对求平均**销毁"哪个基模型变得不可信"，**Frobenius 范数**销毁类别维。

即：**UDS 在 M2 的阶梯上最多是 G 级（全局）构念，而现象需要 C 级（类别条件）**。
M2 量出的差正是 0.675 vs 0.891——UDS 结构上就丢掉了这一截，阈值怎么调都补不回来。

### 2.3 度量对象错位

UDS 度量的是**"变了多少"**（drift magnitude）；EC-MDM 指出要紧的是**"源关系还配不配"**（mismatch）。
二者不等价：大幅漂移若未打在元决策依赖的维度上可以无害；小幅漂移若正好打中则致命。
**UDS 在错误的坐标系里量漂移。**

### 2.4 与 G1 实测证据一致（三条）

1. 各 fold **内层最优** regret 0.0081–0.0238，5/6 fold **样本内**就达不到 0.01；网格 4830 点无并列歧义
   → 不是阈值没找好，**是那个标量里没有做决策所需的信息**；
2. oracle 分布（RF 63 / soft 49 / stacking 37）证明**可分空间存在**，失败不是"本来就无从选择"；
3. 条件 4 通过（存在正确的非 RF 选择）而条件 1–3 失败 → 信号**偶尔**够用、系统性不够用，
   符合"分辨率不足"而非"完全无信号"。

**因此：路线 A 的失败由 EC-MDM 可预测，而非运气不好。** 这构成独立线对主线负结果的**解释力**。

---

## 3. 由此得到的路线 C 形状（候选设计，**未冻结**）

### 3.1 一次方向修正（记录在案）

Fable 5 在讨论中先提出过"给 UDS 加分辨率、拿去做路线 C"，**方向错了**：那仍然是在漂移幅度的坐标系里
加精度。负责人的原意是反向的——**UDS 自己得了同一种病**。以下按修正后的方向写。

### 3.2 正确形状 = M2 的 C 级修正的**无标签版本**

三处关键差别（相对路线 A）：

| 维度 | 路线 A（已判失败） | 路线 C（候选） |
|---|---|---|
| 估计对象 | 漂移幅度（标量） | **目标域的类别条件信任结构本身** |
| 动作空间 | 三候选择一（离散） | **类别 × 模型块的连续权重** |
| 源域角色 | "偏离即报警"的基准 | 各类各模型可靠性的**先验** |

### 3.3 必须保留的一个区分（否则路线 C 没有输入）

目标域上唯一可得的原材料**就是**基模型的预测与概率结构；任何无标签方法都只能从这里取信息。
**分歧结构本身不是错的对象**——错的是把它压成标量、并当作漂移幅度来用。
这一条要写进路线 C 协议，避免把"不用 UDS"误读成"不用目标侧预测结构"。

### 3.4 目标线与工程关系

- **对手是 always-RF 的 regret 0.0203**，不是路线 B 的宽松线（≈ RF − 0.005）。要主张"更高效"必须压过它；
  参考：always-RF 最差环境 0.0318。
- 路线 C ≈ **路线 B + 类别条件分辨率 + transductive 目标信号** → **共用大部分实现**，
  是同一套机器的两个配置，不是两条独立工程。
- 路线 C 使用目标域无标签数据 → **transductive**，按协议 §7 必须单列，不得与 source-only 基线混报。

---

## 4. 两个关键风险与建议的预注册诊断（**最希望 Sol 回应的部分**）

### 4.1 风险一：伪标签分层偏置

M2 的 C 级用**目标标签**做时间块交叉拟合，能看到**真实**的逐类可靠性。无标签版本只能按**预测类别**分层，
而分层在模型出错处污染最重——漂移下正是错得最多的时候（OOD macro-F1 ≈ 0.63–0.69）。
估出的类别条件结构因此带有**依赖漂移强度的偏置**。

### 4.2 风险二：相关错误盲区（更致命）

分歧只是可靠性的代理，且有一个已知失效模式：**当全体基模型以同一种方式出错时，分歧低、准确率也低**
——分歧驱动的权重会**自信地错**。而"互补性坍缩"恰恰是本项目在 OOD 下反复观察到的现象。

### 4.3 建议的开发期诊断（自采数据，有标签，允许）

检验"由分歧导出的逐类可靠性"与"真实逐类可靠性"的一致程度，**并预先写明不通过的判据**；
不通过就停，不硬做。

### 4.4 建议的前置反事实检验（M4 候选，用独立线自己的手法）

**固定输入、只换关系、量恢复**——把"风险/加权关系"本身做一次反事实：允许在目标域内用标签重拟合
该关系，量能拿回多少 regret。三种读法预先写死：

```
大部分 regret 可拿回 → UDS 不是"弱代理"而是结构性错配（与元层同病）→ 路线 C 有戏
几乎拿不回          → 信息不在这些观测量里 → 路线 C 不值得做，止损
中间                → 按恢复比例决定投入规模
```

**这一步在写任何路线 C 实现之前就能定生死**，成本远低于做完整方法。它不碰主线，
适合作为独立线的下一个冻结协议（照 M1–M3 规格：先冻结、写判否条件、允许否定结果）。

---

## 5. 治理边界（不可协商，主线侧由 Fable 5 把关）

1. **路线 C 是事后方法**——它在 G1 失败与 M2 结果**之后**产生。合法，但必须：先冻协议后跑；
   在协议与论文中如实声明产生时点；**报告在这 6 个自采环境上一共试过几个方法**（多重性诚实）。
2. **路线 B 必须做完并报告。** 它是协议 §17.3 的预注册承诺，不能因为出现更漂亮的东西而跳过——
   那正是纪律崩塌的典型方式。路线 B 作锚，路线 C 作显式标注的扩展。
3. **确证设计（关键，且协议已替我们留好）**：6 个自采环境已被路线 A/B 用过，在同一批环境上评第三个方法
   有明显的适应性过拟合问题。正确做法是**自采开发 + UNSW 一次性确证**——而 §16.4 检验 2 本就预注册了
   "自采数据上确定的方法是否无需重新设计即可工作（报 selection regret）"，
   **这是现成的、尚未被方法评估污染的留出集**（P2 只做了 gate 与最小 LORO）。
4. **路线 C 是 transductive** → §7 单列。
5. **本讨论解释 G1，不重开 G1。** 判定书不因事后有更好的解释而改判；解释进 Discussion
   （"我们预注册的代理失败了，而独立机制工作解释了它为何必然失败"）。
   若用新解释追认路线 A"其实差一点就行"，即为越界。

---

## 6. 情景与价值（简表，供 9/22 框架决策参考）

| 路线 B | 路线 C / EC-MDM(含 M3) | 论文形态 | 备注 |
|---|---|---|---|
| 失败 | 失败 | 评估方法学 + 失效表征 | **下限，已在手**：E1（分组 OOF 下崩溃加深、旧口径虚高 CPD 关系）+ 时间块 IID 泄漏（R6 +0.155~0.206）+ G0 表征 + S1 种子噪声尺度。可投，档位偏低 |
| 失败 | 成功 | 诊断型论文 + 诚实负结果方法节 | 价值不降、类型变化；期刊口味偏测量/表征侧 |
| 成功 | 失败 | 现象 + 描述 + 够用方法 | 方法主张仅"≈RF 但更安全"，靠完整性支撑 |
| 成功 | 成功 | 机制 → 方法闭环 + 外部复现 | 上限；风险是**塞太满**，需考虑把 E1 的方法学发现拆分成文 |

**两条提醒**：① 论文价值的下限由**已落盘结果**决定，上限才由这几个未完成项决定；
② 全成功时 CPD 会从主角变配角（标题/摘要不应再以 CPD 为中心），这里有明显的沉没成本陷阱。

---

## 7. 待决项与分派

### 7.1 给 Sol（独立线设计与裁定方）

1. **§2 的同源性论断在你看是否成立？** 特别是"UDS 在 M2 阶梯上最多 G 级"这个对应关系是否严谨。
2. **是否愿意用 M 系列手法做 §4.4 的前置反事实检验（M4）？** 若愿意，判否条件建议由你按 M 系列标准写死。
3. **M2 现有产物里是否已有线索回答"类别条件权重能否只从源域估出"？**
   （源系数分块中心化、逐环境漂移表、可靠性/互补性表——这三处最可能有信号。）
   这个问题是路线 C 的生死线，比再做一次实验更重要。
4. **M3 gate 后的事实卡是否值得加一条**：C 级权重的**可估性**证据（而非仅充分性）。

### 7.2 给 Fable 5（主线设计与审阅方）

1. 路线 B 规格与判定线（把"类别条件权重能否只从源域估出"写成生死线）；
2. 路线 C 的主线侧接口、transductive 归属、多重性声明措辞；
3. G1 解释进 Discussion 的措辞把关（防止追认路线 A）；
4. EC-MDM 引用纪律的执行（见 §8）。

### 7.3 给项目负责人与导师（9/22 写作检查点，**不由任何 agent 决定**）

1. EC-MDM 以何身份进正文：scope 修订（作为独立编号的贡献）／Discussion 引用／独立成文；
2. 四项贡献的篇幅分配，以及是否把 E1 的评估方法学发现拆分成文；
3. 期刊档位与口味（方法型 vs 测量型）。

---

## 8. 引用纪律（写作时逐条适用）

- **MMG / ER 是有标签的离线上界诊断，不是可部署收益**；`ER` 已减去同日 IID 标签辅助重拟合收益。
- CPD_y 只作描述性证据（§2.3、§4.3 代数耦合）；CPD_dir 覆盖 119/150 且**缺失非随机**（集中于低误差参照）。
- 网格 150 任务**不是独立样本**（§8.6/§15）；不报 p 值，只报效应量与聚类区间。
- 路线 C 的比较对手是 **always-RF（regret 0.0203）**，不是路线 B 的通过线。
- E2、G1 两个预注册负结果**必须留在正文**；不得因后续成功而淡化或移除。



