# 实验登记表

协议 §19.1 要求：每次实验一行，字段为实验 ID、commit/hash、配置文件、种子、输入路径、输出路径、结论、是否进入正文。
新建实验前先在此登记，跑完填「结论」列。历史实验（协议冻结前）以补录形式登记，只列进入正文候选的关键运行。

## 登记表

| ID | 日期 | commit / code hash | 配置 | 种子 | 输入 | 输出 | 结论 | 进正文 |
|---|---|---|---|---|---|---|---|---|
| HIST-robust_v2 | 2026-06-22 | `1f81219`（前后） | `configs/research_experiments.json`, test_size=0.3, w=10s | 42 | `features_raw_all_w10.csv` (11303×102) | `results/robust_v2/`（110 条） | 主线基线；含 r=-0.630 等已降格结论 | 是（作历史基线） |
| HIST-gpu_capacity | 2026-07-03 | — | 同上 | 42 | 同上 | `results/gpu_capacity_full_20260703/`（48 条） | 深度模型历史对比 | 是（作历史基线） |
| HIST-cnn_contrast | 2026-07-07 | — | — | 42 | 同上 | `results/cnn_architecture_contrast_20260707/`（40 条） | 架构对比 | 部分 |
| P0-R630 | 2026-08-26 | `code/` @ 见 `r630_sensitivity.py` mtime | 脚本内置：n_boot=10000, n_perm=10000 | 42 | `results/robust_v2/report/controlled_cpd_data.csv` (n=11) | `results/p0_audit/` | r=-0.630 降为探索性（LOTO 3/11 显著；Spearman 不显著） | 是（降格结论） |
| P0-CPD-CORE | 2026-08-26→27 | `code/` @ 见文件 | `test_cpd_core.py`，TOL=1e-10 | 42 | 各历史 confusion_matrix.csv | 15/15 PASS，exit=0 | 三历史值 0.8397/0.1521/0.801 逐位复现并归因 | 是（回归门） |
| E1-ARMS-VERIFY | 2026-08-28 | `code/` @ `6a3f678` | `max_rows=∞`, rf+stacking, all_features | 42 | `features_raw_all_w10.csv`，任务 `loro_R2_R4_to_R3` | `/tmp/regression_out`, `/tmp/arm_a_out`（临时，结论已录此处） | RF 与 A 臂（random OOF）逐位复现历史（Δ=0）；B 臂（grouped OOF）macro_f1 0.5455→**0.4907**，gain −0.0693→**−0.1241**，崩溃**加深约 1.8 倍** | 是（E1 预验证） |
| E1-FULL | 2026-08-29 | `code/` @ `1fa5bcc`（结果入库 `3952388`） | `e1_oof_arms.py --tasks all --seeds 42,43,44,45,46`，三臂 A/A′/B，A′ 折数动态对齐 B，all_features | 42–46 | `features_raw_all_w10.csv`，11 个主线任务 | `results/e1_oof_arms/`（e1_arms_raw.csv / e1_decomposition.csv / e1_arms.json） | B 臂崩溃在全部 OOD 任务持续并**加深**（gain 5 种子均值，A→B）：旗舰 `loro_R2_R4_to_R3` −0.066→−0.106；`loro_R2_R3_to_R4` −0.004→−0.223；`loro_R3_R4_to_R2` −0.024→−0.181；`joint` −0.001→−0.104；jitter 三任务 ≈−0.005→≈−0.08；IID 三任务两臂无差（≈−0.002）；唯一例外 `position→R5` 反转为 +0.024。折数效应（A→A′）小而偏正 → 加深源于分组效应。**按 §12 预注册规则走第一分支：现象基础成立，按 X2 继续机制检验**。结论文档：`results/e1_oof_arms/E1_CONCLUSION.md`（含论文引用规则改为 B 口径、R5 反转与 joint 构造性代价两条专门记录） | 是 |
| G0-GRID | 2026-08-29 | `f740362` | `environment_grid_experiment.py` 全量：150 OOD（\|S\|=1/2/3 → 30/60/60 任务）+ 12 IID（6 环境 × random/time_block，§8.4 时间块对照）；模型 rf/xgboost/lightgbm/stacking；stacking 按 §9.1 grouped | 42（脚本常量 SEED，§14 单种子） | `features_raw_all_w10.csv`（缓存复用，无 tshark 重抽） | `results/g0_environment_grid/`：summary_metrics.csv（648 行 / 162 任务）、env_topology_matrix_rf.csv（RF macro-F1 6×6）、逐任务 CM / pred_proba / oof_meta（stacking） | 网格与时间块 IID 对照完成；同质拓扑矩阵已由 D4-TOPOLOGY 行交付。§8.6：网格不用于显著性结论。**2026-08-29 追记（时间块 IID 对照结论与处置，审阅方核数后审定）**：random − time_block 的 macro-F1 差（4 模型跨度）：R6 **+0.155~+0.206**（均值 +0.192；时间块口径下 R6 "IID" 仅 ≈0.67，说明 R6 轮内时间漂移强）、R4 +0.052~+0.068、R7 +0.031~+0.043、R2/R3/R5 ∈ [−0.015, +0.018]。**处置 = §18 预案原文执行**：随机划分 IID 一律标注为 session 内上界（§8.4），正文主结论基于环境外推；历史 110 条**只加注不重跑**——G0 已提供全 6 环境时间块数值，重跑徒增版本漂移风险（当日两起浮点事件为证）。README 历史基线表已同步加注 | 支撑（§8.6） |
| D4-TOPOLOGY | 2026-08-29 | `1038f3b` | `six_env_confusion_similarity.py`（§20.2 重写）读 G0 \|S\|=1 + 双 IID 参照；口径 = `cpd_y(ref=CM_iid(i), tgt=CM_{i→j})`，见 EXECUTION_PLAN D4 | —（确定性分析） | `results/g0_environment_grid/raw_all/` 下 30 个有序对 + `g0_iid_R*_{time_block,random}` 的 rf/all_features 混淆矩阵 | `env_topology_cpd_y_ref_{time_block,random}_rf.csv`、`env_topology_macro_f1_from_cm_rf.csv`、`env_topology_cpd_dir_*`、`TOPOLOGY_MATRIX_NOTE.md` | 同质 6×6 替代废弃六环境矩阵。macro-F1 核对 30/30（最大偏差 1.1e-16）；审阅方独立重算抽查逐位一致；CPD_dir 在 n_err≥20 下仅 14/30 有定义（缺失主要来自 IID 参照侧误差过少，与 P0 §6 同向）；test_cpd_core 15/15、test_oof_modes 17/17 全绿。旧 `six_env_off_diag_frobenius_rf.csv` 原地保留作 0.1521 回归锁定源（legacy 迁移废止，见 CPD_DEFINITIONS §5.2）。**2026-08-29 追记**：5 个拓扑 CSV 在 canonical 环境 iotcls 下重生成（初版产自 anaconda base；ULP 差异 9/30 格、max 2.22e-16，根因 numpy 1.23→2.4 fro-norm 归约路径变化；1e-6 硬门两版均过，同解释器逐位可复现）——裁定与环境政策见 EXECUTION_PLAN v1.3 D9 追记 | 支撑（§8.6） |
| S1-DEEP-5SEED | 2026-08-29 | 起跑于工作区 @ `042fa03`（实验脚本零改动，仅 CLI 参数） | §14 入口 + 三项必要 CLI 偏离：`--output-root results/s1_deep_5seed_20260829/seed{N}`（默认路径会因 metrics.json 已存在而静默短路复用 seed 42 → std=0）、`--cnn-v5-source legacy/results/extreme_capacity_1p2m_20260706/raw_all`（默认源已迁 legacy）、解释器 `~/anaconda3/envs/iotcls/bin/python`（torch 2.5.1+cu121；base 无 CUDA 会静默跑 CPU）；`PYTHONPATH=code/scripts/analysis` | 42–46。预检通过：splits 逐字节复制（diff 无差异）、`--random-state` 经 set_seed 影响初始化 + DataLoader 批次顺序，**不影响划分**——语义记为"划分固定、训练随机性（初始化+批序）变"，符合 §14 意图（其补救条款仅针对影响划分的情形） | `results/robustness_scaling_20260706_v2/splits`（固定划分） | `results/s1_deep_5seed_20260829/seed{42..46}/`（各 40 行 = 8 任务 × 5 模型）+ 聚合 `s1_multiseed_summary.csv` / `s1_all_seed_rows.csv` / `s1_aggregation_metadata.json`；聚合脚本 `code/scripts/analysis/s1_aggregate_multiseed.py`；日志 `results/s1_deep_5seed_logs/` | **已完成（5 种子全部 EXIT=0）**。锚点：seed 42 的 single_round_R2 三候选复现 7 月历史值至 4 位小数。**聚合警告已实测证实**：`cnn1d_v3` 与 `cnn1d_v5` 逐任务跨种子 std **恒为 0**（脚本 `copy_existing_result` 路径复制的单种子参照），聚合脚本以此为判据自动剔除其 ±std 并标注 `single_seed_reference_copied`——直接对全表求 std 会得出"v3/v5 极稳定"的错误结论。真 5 种子候选仅 `cnn1d_convnext` / `cnn1d_inception` / `cnn1d_tcn`，**种子噪声量级（跨任务平均 std）分别为 0.0163 / 0.0102 / 0.0110 macro-F1**——该量级是解读架构差异的必要尺度（多数任务上三者差异在 1 std 内）。逐任务 mean±std 见聚合表 | 是（§2.2 支撑实验；架构比较须带种子噪声尺度） |
| E1-G0-GRID | 2026-08-29 | 运行时工作区 @ `b9a4ddb`（`e1_oof_arms.py` 扩展与产物随本行入库；原版备份 /tmp 供 diff） | `e1_oof_arms.py --grid all --seeds 42`，3 分片 × n_jobs=8（实测 22.4 核，54.6 分钟）；150 个 G0 OOD 任务 × 三臂 × all_features；B 臂折叠按训练轮次数判定（\|S\|≥2 轮次分组 / \|S\|=1 时间块，`SINGLE_ROUND` 名单已删除）；任务定义 import 自 G0 生成器；§19.2 provenance（git、argv、包版本、逐任务折叠记录、耗时）落盘并在合并时跨分片强校验 | 42（追加 43–46 **裁定不做**：单种子已满足 §14 G0 行覆盖语义——见协议 §23 2026-08-29 澄清行；且输出 stem 不编码种子，追加会覆盖现有产物） | `features_raw_all_w10.csv` + G0 任务生成器 | `results/e1_oof_arms_g0/`：e1_arms_raw_all.csv（450 行）、e1_decomposition_all.csv、e1_arms_all.json、provenance_all.json、g0_consistency_all.json、shards/ | smoke 门禁 8/8 逐位通过（含与主线 loro_R2_R4_to_R3 三臂逐位一致 → 主线路径零回归）。全量一致性 597/600 ≤1e-6（基模型 450/450，max 5.6e-17）；**3 个 \|S\|=3 任务 stacking 差 3.3e-4–2.0e-3，审定接受**：诊断为 LightGBM 折内 ~1e-15 线程序浮点差（G0 与 E1 线程拓扑不同）经 lbfgs 元学习器放大；E1 同配置重跑逐位一致，**§18"同配置复跑不一致"未触发**；G0 落盘值仍为 G0 权威、E1 三臂同环境内部自洽；跨线程拓扑 stacking 复现界 ~2e-3 记为可复现性事实（非 §22.1 容差放宽），任何精确复现声明须钉死 num_threads。结果（描述性，§8.6）：A→B 加深在 \|S\|≥2 全网格成立（gain 均值 \|S\|=2：−0.039→−0.093；\|S\|=3：−0.034→−0.054）；\|S\|=1（时间块 B）无加深（−0.076→−0.070）→ 加深特异于跨轮分组 OOF；源环境越少加深越深。详见 `results/e1_oof_arms/E1_CONCLUSION.md` §8 | 支撑（§8.6）；E2/G1 输入 |
| P2-UNSW-PILOT | 2026-08-29 | 工作区 @ `0e5e791`（`extract_features_generic.py` 与 pilot 脚本随本行入库） | §16.3 五问 gate。4 天 pcap（09-23 / 09-30 / 10-11 / 10-12，约 11GB，直连 ~10MB/s 计约 12 分钟）；提取器 61 维通用特征（94 − 33 项 802.11 专属 = 61，对账闭合）；RF LORO import 主线 `build_model` / `sample_balanced`（§7/§16.4，不重实现）；脚本在 `code/scripts/analysis/unsw_pilot/` | 42 | `dataset/unsw/pcap/*.pcap` + `dataset/unsw/device_mac_map.csv`（入库副本见 `results/unsw_pilot/PROVENANCE_NOTE.md`） | `results/unsw_pilot/`：PILOT_FIVE_QUESTIONS.md、FEATURE_ALIGNMENT.md、INVENTORY.md、loro/、four_day/、BELKIN_PROBE.md、CSV_CROSSCHECK.md。特征缓存 `features_*.csv` 为可再生派生物且合并档 124/168MB 超 GitHub 单文件硬限，按 .gitignore 注释排除（各 run_meta.json 入库） | **五问全部通过（gate PASS，无需换候选）**：① MIT-0，论文使用与派生特征发布均可；② MAC 映射完整、四天零未知设备（唯一未列 MAC 为网关兄弟接口 `…:e9`，仅作 dst）——**限制进正文：23 个 IoT MAC 仅 11 个天天在线、10 个天天 ≥100 窗（测试床后期下线所致，10-11 全天对照排除短抓包与标签污染两种解释），§16.4 扩展必须按日交集取类并逐任务报实际类别数，不得笼统写 18 类**；③ 09-30 与 §16.1 预核数字逐项一致（20 活跃 / 18 ≥100 窗 / 18 ≥300 窗 / Dropcam 8640；CSV 清点交叉核对 673,414 / 86,398 / 84,291 逐位复现）；④ pcap 时间戳微秒级（唯一率 1.000000 vs CSV 0.125），61 特征 0 NaN / 0 inf；`side_/other_packet_ratio` 两列 Ethernet 上恒 0 仅作列对齐，**禁止进入跨数据集特征重要性比较**；⑤ 6 有序日对 LORO macro-F1 0.8164–0.8674（均值 0.8466，任务实际 10–14 类），Belkin Switch↔Motion 混淆经判别检验排除标签互换（域内即最难两类、双向混合形态，证据 BELKIN_PROBE.md）。执行决定：10-12 实为 14.48h 抓包，保留（真实末日、最大跨度）并加录 10-11 全天对照。相邻日对分数最高（0.8901/0.8778）、远隔 19 天对最低的梯度已记录不解读（属 §16.4 检验 1）。提取成本 ~11 min/天（窗口级 pandas 开销），20 天全量前需按天并行化 | 是（P2 gate 交付物） |
| D8-VOTING | 2026-08-29 | `c70fab1`（脚本+产物入库；运行时工作区 @ `b9a4ddb`，run_metadata 记 dirty）→ `1db263c`（NOTE 自核订正） | `voting_baselines.py` 纯后处理（无重训、无随机数）：§7-4 hard voting（多数票，平局 `argmax(votes + 0.5·mean_proba)`，平局实测占窗口 0.696%）、§7-5 等权 soft voting、§7-6 温度校准 soft voting（每任务×每基模型在**源域 OOF** 上以 NLL 拟合标量 T，486 个全收敛、0 触界，测试侧只施加；函数签名结构性无泄漏）；指标 5-class / 4-class / 最差类 F1 / accuracy（§10）；`nice -19` 单进程 15.4s，双跑 md5 相同 | 无（全流程确定性，`random_seed=null`） | G0 全部 162 任务的 predictions / pred_proba / oof_meta | `results/g0_environment_grid/voting_baselines.csv`（1296 行 = 162 × 8 method，`method_kind` 区分投票/对照行）、`voting_calibration_temperatures.csv`（486 行审计）、`VOTING_BASELINES_NOTE.md`、`voting_baselines_run_metadata.json` | 自检 648/648 逐位一致（**加严**至含 stacking 与逐类 F1，偏差恒为 0.0；超差即退出不写产物）；独立重算 soft-equal 抽查逐位一致（收尾会话与 Fable 各一次，后者 `g0_R2_R4_to_R3`=0.548005 逐位吻合）。§7 基线 4–6 补齐，X2/X4/G1 必备对照就位。OOD 150 任务 5-class 均值：RF 0.6870 > 校准 soft 0.6670 > hard 0.6595 > 等权 soft 0.6559 > Stacking 0.6280（oracle 最佳单模 0.7009）。gain_vs_best_base 均值（n_sources=0/1/2/3）：stacking −0.003/−0.070/−0.093/−0.054；**校准 soft −0.003/−0.067/−0.029/−0.023**；hard −0.002/−0.073/−0.034/−0.033；等权 soft −0.002/−0.081/−0.037/−0.035——三投票法均不超 oracle，但 \|S\|≥2 上一致优于 stacking（校准 soft 逐任务 101/162 胜，均值 +0.036）；校准>等权稳定（119/162，+0.010）。**G1 内层景观（任务级，非 G1 判据）**：三候选事后最优分布 RF 67 / soft_equal 48 / stacking 35（非"永远 RF"退化）；固定策略平均 regret：always-RF 0.0203 / always-soft 0.0514 / always-stacking 0.0793。校准 T 观察（留给 X2/G1）：lgb/xgb 源域 OOF 系统性过自信（T 中位 5.24 / 2.63，162/162 任务 T>1），RF 近校准（0.96）。审阅裁定（Fable 追认收尾会话三项处置）：①对照行保留（`method_kind` 防重复计数，NOTE §7.1 警示）；②最差类 F1 双口径均保留（§10 未定死，出表再选）；③NOTE 初稿一处精度表述订正（"T=1 还原 3.6e-12"→落盘精度限制下 ~1e-7）合规，脚本未改以保持产物溯源。**待决（G1 规格 D10 定死，接触任何外层评估前）**：G1 候选"soft voting"取校准版 / 等权版 / 或按 §8.3 折内内层选择 | 是（§7 基线 + G1 候选动作数据源） |
| E2-CONDITIONAL | 2026-08-29（**运行前登记**） | 规格 = EXECUTION_PLAN v1.2 D9（先写后看：本行与 D9 的 commit 先于任何 E2 数值产生） | §13 分层回归 M0/M1/M2。DV = E1-G0-GRID 的 B 臂 gain（150 任务，seed 42；3 个 ~1e-3 差异任务用 E1 值）；M0 = RF 逐类 recall 变化（参照 = 源集 G0 time_block IID 均值）+ L2 + max；M1/M2 加 CPD_y / CPD_dir（ref 同参照，多源构造沿用 0.8397 历史口径，只经 cpd_core；CPD_dir 按 §4.3 n_err≥20，M2 与其 M0 对照同子集拟合）；双通道 = 标准化 OLS 增量 R²（环境聚类 bootstrap B=10000）+ 留一目标环境 CV 的 MSE 改善；敏感性 = A 臂 DV / random IID 参照 / 三基模型均值 recall | 42（bootstrap） | `e1_arms_raw_all.csv` + G0 各任务与 IID 的 rf confusion_matrix.csv + `cpd_core` | `results/e2_conditional/`（回归表、逐环境结果、M2 覆盖率、bootstrap 明细、provenance、E2_RESULTS_NOTE.md） | **已完成并判定（2026-08-29，判定书 `results/e2_conditional/E2_CONCLUSION.md`）**。验收四门全过（M0 手算 0 偏差；CPD_y 与 D4 拓扑 30/30 逐位；bootstrap 双跑 md5 相同；行数 9/9）。终值（primary）：M1(+CPD_y) ΔR²=+0.0066、f²=0.008、β −0.245 [−0.757,+0.320] 跨零、LOEO-CV 变差 4.6% → **CPD_y 增量解释力消失**；M2(+CPD_dir, lenient n=119) ΔR²=+0.0748、f²=0.091、β −0.296 [−0.556,−0.175] 稳健但 LOEO-CV 变差 3.9% → 条件关联成立、**不满足机制性量门槛**（strict n=26 全数秩亏仅并列）。**判定走 §13 降级分支**：贡献 2 降为"失效模式的结构化描述"，主线转贡献 3（部署方法），H2 强形式不成立、CPD_y 上判否条件触发。敏感性要点：A 臂（历史口径）DV 下增量虚高（M1 +0.267 / M2 +0.482）——旧口径抬高 CPD-gain 表观强度，进方法学讨论 | 是（判定 + 结构化描述素材；引用规则见判定书 §3/§4） |

> **修订记录（2026-08-28）**：本行首次登记时误记 B 臂 macro_f1 为 0.5498（"崩溃略软"）。
> 该数字来自 `_splitter` 的一个缺陷：多轮分组分支缺 `return`，执行掉进单轮时间块分支并覆盖
> GroupKFold 结果，实际跑的是时间块 OOF 而非协议 §9.1 的轮次分组。修复后真实值为 0.4907，
> **方向与原记录相反**。修复见 commit `6a3f678`，严格化测试 `code/scripts/analysis/test_oof_modes.py`（11 项断言）。
>
> **注意（待你决定）**：`loro_R2_R4_to_R3` 只有 2 个源轮次（R2、R4），`GroupKFold` 的
> `n_splits` 因此退化为 **2**——每折用单轮训练、另一轮验证。这是协议 §9.1「group=round」的
> 直接后果，但元特征噪声显著大于历史的 5 折。E1 正式跑之前需确认这是否是期望口径。
>
> **决定（2026-08-29，Fable 5 审定，详见 `EXECUTION_PLAN_20260829.md` D1）——本待决项关闭**：
> 确认 2 折退化为协议 §9.1 的期望口径。理由：① 2 轮条件下按轮分组是唯一无泄漏实现；
> ② 折数混淆由 A′ 臂（折数动态对齐 B）显式控制，E1-FULL 实测折数效应 ≈ +0.008（小而偏正），
> 崩溃加深全部来自分组效应；③ 轮内时间块凑折数的替代方案混合两种分组语义，偏离 §9.1 文本。
> E1-FULL 结果按此口径有效。



| G1-ROUTE-A | 2026-08-29 登记 / 2026-08-30 判定 | 规格 = EXECUTION_PLAN v1.4 D10（先写后看：本行与 D10 的 commit 先于任何 G1 数值；含 §17.1 红线重读结论——E2 降级不改变任何判据，一字不调） | 嵌套 LOEO（§9.2）：外层 6 fold × 25 任务（目标环境 = e_out）；内层池 = **严格全含** 70 任务/fold（源与目标均不含 e_out）；候选 = RF / Stacking(B 口径) / soft voting（等权 vs 校准由内层选，§8.3）；选择器 = **UDS 单调双阈值**（唯一预注册家族，[Stacking, soft, RF] 风险序），内层穷举 (变体, t1, t2) 最小化内层平均 regret（并列打破：内层最差环境 regret → 更保守）；判据 = §17.1 五条，regret 聚合主口径 = 环境等权；条件 4 = ≥1 外层任务选非 RF 且 regret ≤ **τ_repro = 2e-3**（v1.5 运行前修订，τ 取自本表 E1-G0-GRID 行实测复现界，动因 = 精确零判定与复现界相撞：三候选 top-2 差 <2e-3 占 8.0%/5.3%；τ=0 严值并报）；UDS 只经 cpd_core.uds、不读标签（判据 5 审计） | 无（全流程确定性，零重训） | G0 + D8 落盘产物（predictions / pred_proba / oof_meta / voting_baselines.csv）+ `cpd_core.uds` | `results/g1_route_a/`（逐任务明细、逐环境 + 汇总 regret、win/loss、各 fold 阈值与变体、无泄漏审计、provenance、G1_RESULTS_NOTE.md） | **已完成并判定（2026-08-30，判定书 `results/g1_route_a/G1_VERDICT.md`）：路线 A 未通过 → 转 §17.3 路线 B**。五条判据：①外层平均 regret 0.02469 > 0.01 **失败**；②最差环境 R7 0.03611 > 0.03 **失败**；③vs always-Stacking(0.07925) 改善但 **vs always-RF(0.02025) 反而差 +0.00444 失败**；④非 RF 且 regret ≤ τ_repro 共 7 任务（τ=0 为 6，落 R4/R7）**通过**；⑤UDS 无标签审计（12 函数 0 标签 token）**通过**。失败性质：各 fold **内层最优** regret 0.0081–0.0238，5/6 fold 样本内即达不到 0.01 → 是家族/信号问题而非外层泛化；选择器相对 always-RF 逐任务 8 胜 16 负、125/150 选 RF；但 oracle 分布 RF 63 / soft 49 / stacking 37 / 并列 1 且 oracle 均值高于 always-RF → **头顶空间真实存在、UDS 抓不住**（H3 判否触发，H4 半failed 半passed，如实并列）。验收硬门全过（候选 F1 300 项逐位、元数据三源 450 项、fold 结构自检、确定性双跑）。移交路线 B 的关键事实：真实竞争对手是 always-RF（0.02025，其最差环境 0.03182），比协议通过线 RF−0.005 暗示的更严苛 | 是（负结果进正文/附录；贡献 3 改为路线 B） |

| ROUTE-B | 2026-08-30 登记 / 2026-08-31 判定 | 规格 = EXECUTION_PLAN v1.6 D11（先写后看：本行与 D11 的 commit 先于任何路线 B 数值） | §17.3 环境稳健加权聚合，**全局逐基模型权重**（纯洁性条款：禁止用 M2 事实卡的类别条件结论改造设计，类别条件版本归路线 C）。骨架复用 D10 嵌套 LOEO（外层 6×25 / 内层严格全含 70）；权重 = `exp(lb/τ)·max(1−γρ,0)` 后向均匀收缩 λ，`lb` = 源域 OOF 各留出折 macro-F1 最小值、`ρ` = 源域 OOF 错误指示相关均值；内层网格 180 组、目标 = 内层环境等权平均 5-class macro-F1（非 regret，因 §17.3 通过线为 F1 口径）；source-only（权重不含任何目标侧量，审计门） | 无（全流程确定性、零重训） | G0 落盘 `oof_meta.csv` / `pred_proba.csv` / `metrics.json` + D8 `voting_baselines.csv` | `results/route_b/`（task_detail / env_summary / fold_params / weights / passline / acceptance / provenance / NOTE） | **已完成并判定（2026-08-31，判定书 `results/route_b/ROUTE_B_VERDICT.md`）：未通过 §17.3**。五道硬门全过（λ=1 逐位复现 D8 等权 soft 162/162、偏差 0.0；候选 F1 300/300；双跑 5/5 md5 一致；fold 自检；source-only 审计）。三条通过线：①环境等权平均 macro-F1 **0.661379 vs RF 0.687025，缺口 −0.020646**（容差 0.005 的四倍）→ **失败**；②最差环境 R5 0.617007 > Stacking 0.594361 → 通过；③5/6 环境 vs Stacking 正增益（仅 R3 为 −0.0072）→ 通过。排名：RF 0.6870 > 校准 soft 0.6670 > **路线 B 0.6614** > hard 0.6595 > 等权 soft 0.6559 > Stacking 0.6280；路线 B regret 0.0459 = always-RF（0.0203）的 2.3 倍，且**不及 D8 温度校准 soft voting**。失败性质：内层各 fold 最优 γ **全部取边界 1.0**、λ 多为 0 → 该族在给定信号下已用尽仍不足（容量问题，非实现问题）；仅在 R6/R7 胜过 RF（恰为时间块泄漏最重的两环境）。跨线一致性（事后解释，非判定依据）：独立线 M2 测得全局块缩放仅恢复 67.5%、类别条件 89.1%，与「全局分辨率不足」同向；§17.3 设计未被 M2 污染（D11 纯洁性条款）。**处置**：§17.1 路线 A 与 §17.3 路线 B 均未通过 → 贡献 3 目前无成立方法，两者如实作负结果；路线 C（类别条件、无标签）为唯一剩余候选，须自带冻结协议 + 开发用自采、确证在 UNSW 一次性 + 报告方法族多重性，且通过线必须对标 RF（0.687025 / regret 0.020275）。**透明度**：因 API 中断，实现脚本由判定人本人编写（实现与判定同一方），五道门均为机器可复算的客观检查 | 是（负结果进正文/附录） |

## 待登记（已排期，执行口径见 `docs/EXECUTION_PLAN_20260829.md`）

（当前无待登记项。）

2026-08-29 全部完结并在主表登记：D4-TOPOLOGY、P2-UNSW-PILOT、D8-VOTING（两次登记于
2026-08-29 晚合并为一行，内容取并集，无信息丢失）、S1-DEEP-5SEED、E1-G0-GRID、
E2-CONDITIONAL（判定见其行与 `results/e2_conditional/E2_CONCLUSION.md`）。

已完成并转入正式登记：G0 网格全量、时间块 IID 对照（并入 G0-GRID 行）、E1 全部 11 任务（E1-FULL 行，
含原"剩余 4 个任务"——其 B 臂为时间块口径，`b_split_basis` 字段已单独标注）。

## 维护规则

1. 每次实验**开工前**登记一行（ID 可先用日期前缀），跑完补「结论」。
2. commit 列填 `code/` 仓库的 `git rev-parse HEAD`；纯分析脚本改动也要先提交再跑。
3. 「进正文」= 该实验的数字可能出现在论文正文；改为「否」需注明理由。
4. 临时验证（如 `/tmp` 里的 smoke）若结论有价值，登记时在输出列注明"临时，结论已转录"。

**P2-UNSW-TEST1 运行前补充（2026-08-31，结果不可见）**：下方登记行保留 v1.8 D12 的原始
预注册文字作审计轨迹；正式执行规格已由 EXECUTION_PLAN **v1.9 D13** 闭合，冲突处以 D13 为准。
关键更正为：固定 `appliance/camera/hub/sensor/speaker/switch` 六类公共支持；74 个任务定义分别运行
dynamic-device primary 与 stable-device parallel 两臂；训练/测试各以主线 `sample_balanced` 固定
`max_rows=20000`；`CPD_y(T)` 统一为训练侧无泄漏 RF OOF CM 到测试侧 RF CM 的漂移。判据 1 运行
20 个 IID，但首日因无可配对 OOD 只作描述，正式聚类对比为 54 OOD vs 19 paired-day IID；判据 2
仍为 54 OOD 的预声明中位数二分。任何正式运行须等候主线审阅实现并追加 `RUN_AUTHORIZED`。

| P2-UNSW-TEST1 | 2026-08-31（**运行前登记**） | 规格 = EXECUTION_PLAN v1.8 D12（先写后看：本行与 D12 的 commit 先于任何检验 1 数值）；正式执行规格 = v1.9 D13；implementation commit = `6e3d763`、launcher commit = `35defd1`（sha256 `93ebdff…f532b4`）、`D12_R4_EXECUTION_HEAD` = `d9b0c7d` | §16.4 检验 1 机制存在性。输入特征 = `results/unsw_features_full/`（20 天 / 1,317,887 窗 / 61 维；与 pilot 重叠 4 天 md5 逐位相同）。任务 = 连续 k 天训练 + 次日测试，k∈{1,2,3} → 54 OOD + 20 个日内时间块 IID 参照 = 74；标签层级 = **设备类型**（`category`，非设备身份）；设备面板 = **逐任务按 k+1 天取交集**（主口径）+ 全 20 天稳定面板（10 设备 / 6 类型）作并列敏感性；入选门槛沿用 P2 gate 每 (类型, 日) ≥100 窗；逐任务报实际类别数。模型 = rf/xgboost/lightgbm/stacking（`gain_vs_best_base` 的最小必要集，超参同 G0、不调参）；Stacking OOF 按 §9.1 分组：k≥2 按训练日分组、k=1 退化为日内时间块（与 G0 单源处置一致）。`CPD_y` 只经 `cpd_core`，参照 = 同测试日 IID time_block RF 混淆矩阵（口径同 D4/E2）。判据运行前冻结：①OOD−IID `CPD_y` 均值差 >0 且按测试日聚类 bootstrap（B=10000）95% CI 下界 >0；②按 `CPD_y` **中位数**二分后高 CPD 组平均 gain <0 且 95% CI 上界 <0（低组与差值无论正负并报）。三分支裁定：两条过=通过（进正文一图一表）／仅一条过=默认按不通过处置、禁止挑有利条／两条不过=降格为边界条件。§15：不报未校正 p 值 | 42（含 bootstrap） | `results/unsw_features_full/features_day_*.csv` + `dataset/unsw/device_mac_map.csv` + `cpd_core` + 主线 `build_model` / `sample_balanced` | `results/unsw_test1/`（task_detail / cpd_table / gain_table / passline / acceptance / provenance / NOTE） | **已完成（R4 正式双跑 exit `0`，2026-09-01 15:47:44→19:17:01 CST，墙钟 3h29m17s）；裁定 = 部分通过 → 按预注册默认作不通过处置**。七道硬门**全部 PASS**：双跑 md5、`test_cpd_core` 15/15、概率行和 1（1e-9）与 61 特征 0 NaN/inf、逐任务类别数与设备清单落盘、逐任务 OOF 折叠记录与声明一致、`side_/other_packet_ratio` 恒 0 审计且不进特征重要性比较、测试日标签无泄漏（函数级）。明文不做：跨数据集完整基线对比、深度模型、特征工程调优、CORAL、投票基线、leave-device-out、协议/载荷特征（自采侧 WPA2 加密物理不可得，见讨论文档 §17.4）、任何窗口长度变更。**检验 2 当前无输入**（路线 A/B 未通过、路线 C 未实现）→ 按 §16.4 脚本进附录、正文方法适用范围限定为同部署内标定。**双跑门**：10 个规范文件逐字节一致，packet manifest sha256 `b8b71ca…2247d323`（R1 概率行和门失败、R2 被监督中断、R3 因 Stacking 非确定性在 26/148 task-panel 行的 `stacking_macro_f1` 上出现字节差异而 fail-stop；R4 以 Stacking `n_jobs=1` + `threadpool_limits(1)` + LightGBM `deterministic/force_col_wise` 修复，科学对象未变）。**判据实测（primary，唯一裁定臂）**：①OOD 均值 0.065653 vs 19 配对日 IID 0.047944，点估计 **+0.017709**，测试日聚类 bootstrap CI **[−0.006637, +0.037742] 跨零 → FAIL**；②预声明中位数 0.059158 二分（27/27），高组 gain **−0.002049**，CI **[−0.003293, −0.001047]** 全负 **→ PASS**；`overall_three_branch = partial_default_not_pass`。**并报（预注册要求无论方向一律报）**：低组 gain 亦为负 −0.001237（CI [−0.001600, −0.000748]）；`high_minus_low` −0.000813，CI **[−0.002092, +0.000205] 跨零** → 负增益与 CPD 高低**无可检出关联**；量级比 E1 的 −0.066~−0.223 小约两个数量级；stable 敏感性臂判据 1 塌至 +0.003332（CI [−0.028684, +0.028418]，且全 20 IID 的 CPD 均值 0.078281 反高于 OOD 0.074712），其 `high_minus_low` **翻正** +0.000482 → 不具面板稳健性。`best_base_model` 分布 xgboost 115 / lightgbm 27 / rf 6。判定书 = 讨论文档 §D12_TEST1_FROZEN_ADJUDICATION（主线未行使"另出书面裁定作通过"的例外权，理由见该节）；机械执行记录 = §D12_EXECUTION_COMPLETE | **否**（降格为 Discussion 边界条件 / 单测试床案例；CPD 的机制、预测、部署身份一律不恢复；禁止与 E1 的 −0.07~−0.22 并列为"同一机制跨数据集复现"） |

| UNSW-IID-REF | 2026-09-02（**事后登记**） | 规格 = `docs/SPEC_UNSW_IID_REFERENCE_20260902.md` sha256 `a1e84916b8ffdb9bff52df8e…`（规格冻结于运行之前，先写后看性质成立；**但本行为跑完后补登，违反维护规则 1「开工前登记」，如实记录不掩饰**） | 设备身份粒度日内时间块 IID 参照。3 天 09-23/09-30/10-12；每设备每天 ≥100 窗；主线 `sample_balanced(max_rows=20000)`；划分 = 设备内按 `(window_start_epoch, window_id)` 升序前 70% 训练/后 30% 测试（口径逐字对齐 D12 IID）；模型 rf；`feature_columns`/`build_model`/`clean_x`/`sample_balanced` 全部 import 不重实现 | 42 | `results/unsw_pilot/four_day/features_unsw_w10_4day.csv` + `results/unsw_pilot/loro/loro_summary.csv` | `results/unsw_iid_reference_20260902/`（iid_summary.csv / paired_comparison.csv / 逐任务 cm 与 per_class / provenance.json） | **已完成**，判定 `CROSS_DAY_DROP_AT_DEVICE_GRANULARITY`：6 个配对 IID−LORO 差均值 **+0.032778**，**6/6 为正**（+0.0233 ~ +0.0520）。同测试日、同类集、同门槛、同平衡、同模型、同种子，唯一差异是训练数据来自本日前段还是另一天。**关键对照**：D12 在**类别**粒度（6 类）上 OOD−IID = **−0.0131**（跨天反而更好）→ 符号翻转纯由标签粒度驱动；D12 零结果的成因 = `light` 类被排除（LiFXBulb 逐设备 drop **+0.1506**）+ 多设备类别稀释（`BelkinWemoSwitch` +0.1143、`WithingsAura` +0.1507 被池入 3/4 设备的 switch/sensor）。逐设备结构与自采同形：`Dropcam` +0.0005 免疫（对应自采 Socket），Belkin 对单向（Switch +0.1143 / Motion −0.0045，对应自采 Light_XM→Light_T1）。幅度为自采 `strict59` gap（+0.1201）约 1/4、`full94`（+0.2643）约 1/8 → 残差归因于 UNSW 无部署事件变更。**分离天数不预测 drop**（7 天 +0.0520 最大、19 天 +0.0242 最小）→ 逐日特异性而非累积漂移。按规格属参照测量，**不构成外部复现主张，不改 D12 判定** | 支撑（现象成分） |

| UNSW-META-MISMATCH | 2026-09-02（**运行前登记**） | 规格 = `docs/PROTOCOL_UNSW_META_MISMATCH_20260902.md` sha256 `9344de0ecb36561ce34db1685b32fbdf`（先写后看：本行与该协议的 commit 先于任何 UNSW stacking 数值产生）；implementation = `results/unsw_meta_mismatch_20260902/run_unsw_meta_mismatch.py`（341 行）。**透明度**：实现与审阅同一方（链路中断期间由裁定人本人编写），四条签名与七道硬门均为机器可复算的客观检查，先例见本表 ROUTE-B 行 | 与自采逐项同构：3 任务（2 源采集日 → 1 未见采集日，`{23,30}→12` / `{23,12}→30` / `{30,12}→23`）× 模型 rf/xgboost/lightgbm/stacking；标签 = **设备身份**（非 `category`，后者已实测掩盖现象）；stacking 经 `SimpleStackingClassifier.fit(..., train_round=day)` 走 `GroupKFold(groups=day)`，`n_splits = max(2, min(cv, 2)) = 2`——与自采 2 源轮次退化为 2 折逐项对应；元学习器 `LogisticRegression(max_iter=2000, class_weight=balanced)` 不改；入选门槛 = 三天各自 ≥100 窗的设备交集；`max_rows=20000`；线程钉死 OMP/MKL/OPENBLAS=1、串行独占。**两阶段**：S0 只跑 RF 并按 §3 规则（`f_ij=CM_rf[i][j]/rowsum(i)` 按任务均值，`max(f_ij,f_ji)>=TAU=0.10` 连边，簇 = 节点数≥2 连通分量）导出簇成员写入 `cluster_definition.json`；main 阶段校验该文件已存在方可拟合非 RF 模型 → 簇成员在机制上不可能由 stacking 结果决定。`TAU=0.10` 已在自采逐位验证（成员 f 0.149–0.897 / 非成员 0.069–0.095，恰好切出 `{Light_T1, Light_XM, Sensor}`），**禁止为 UNSW 调整** | 42 | `results/unsw_pilot/four_day/features_unsw_w10_4day.csv` + S0 导出的 `cluster_definition.json` | `results/unsw_meta_mismatch_20260902/`（cluster_definition / signature_table / subset_f1 / meta_coefficients / confusion/ / oof_folds / passline / provenance / VERDICT.md / **`fourcell_posthoc.csv`（2026-09-04 事后聚合，不拟合；聚合脚本 `diag_unsw_fourcell.py` 按仓库规则 `results/` 只入 csv/json/md 而留在磁盘，与同目录 `diag_unsw_override.py`、`run_unsw_meta_mismatch.py` 同例）**） | **已完成（2026-09-02 运行；2026-09-04 补填结论——此前该列漏填、仍为「待运行」）；判定 `MECHANISM_NOT_EXTERNALLY_REPLICATED`，落**分支 C**。** **签名逐条（§6 分支 C 要求「失败的具体签名必须逐条写入正文」）**：①定位（`gap_H<0` 且 `\|gap_H\|>=3\|gap_E\|`）**2/3，失败**——任务 `16-09-23+16-09-30_to_16-10-12` 的 `gap_H` = **+0.0117** 为正；②免疫（`gap_E>=−0.03`）**3/3，通过**（−0.0023 / +0.0006 / −0.0001）；③系数签名 **0/3，失败**——`hard_hits`=1、`easy_hits`=8，即大跨类系数在 UNSW 上**普遍存在而非定位于困难簇**（`BelkinWemoMotion` 最大跨类项 `xgboost\|BelkinWemoSwitch` = **+2.118** > 自身项均值 +1.508，但 8 个易类同样命中，「易类必须相反」不成立）；④regime 错配（`\|src_oof_H−target_H\|>=0.05`）**2/3，失败**（0.1015 / 0.0627 / **0.0417**）。**幅度预测失败**：`mean_abs_gap_H` = **0.0099**，预注册区间 [0.03, 0.20]，自采参照 0.2878 → **差 29 倍**。**分支 C 处置照执行、不因下述解读松动**：机制只作自采单测试床刻画，UNSW 只承担现象成分（`UNSW-IID-REF` 的 +0.032778 参照测量）。 **为什么失败 —— 事后解读（2026-09-04；非预注册，明确标注，不改任何判定）**： ①**配对比较（2026-09-04 上午修订：初版「推翻率几乎相同 0.2834 vs 0.2965」中的 0.2834 = 1152/4065 用了 `MAINLINE` §7 一个复现不出的分母且混入 6 个 `\|S\|=1` 任务——撤回「同频率」修辞，解离结论不变）**：按 `\|S\|=2` 对齐（UNSW 三任务都是 2 源日→1 目标日），自采 full94 困难簇 破坏率 **1149/1472 = 0.7806**、破坏:救回 **5.8:1**；UNSW 三任务 **633/4900 = 0.1292**、破坏:救回 **1.0:1**（推翻率最高的任务 0.2965、救回率 0.4191）。**读法按乘积形式：开火频率 = 强度，破坏:救回 = 反转；自采 = 高强度×强反转，UNSW = 中强度×零反转。UNSW 是一个更难的困难簇（F1 0.53）、元层在开火、却零伤害的实例 → 困难不充分，方向决定伤害。**这是 `MAINLINE` §5「源域诊断只看得见强度、看不见反转」的**跨测试床配对实证**（原先只由相关系数支撑：可分性 → 推翻率 −0.753 vs → `gap_H` +0.123）。 ②**四格分解——已测（2026-09-04；`fourcell_posthoc.csv` + `diag_unsw_fourcell.py`，口径逐字对齐自采 `diag_grid_override.py`，纯聚合不拟合模型，`broken` 与已登记 `n_overridden` 三任务逐位对账 507/101/25 一致）**：困难簇三任务合计 共识正确 4900 / **破坏 633** / 共识错误 4358 / **救回 656** / 换错法 **23** / 净 **+23**；同推翻率的那个任务 破坏 507 / 救回 518 / 净 +11。对照自采 full94 破坏 1152 / 救回 204 / 换错法 **594** / 净 −948（9 任务；`results/g0_environment_grid/fourcell_posthoc_self.csv`，同定义重算；`\|S\|=2` 三任务 1149/199/592/−950）。**即 UNSW 救回 ≈ 破坏、精确对消，且「换错法」几乎不存在（23 vs 594）→ 两边元层都在开火，差别在映射方向而非频率（频率本身自采更高，见①）。**推论→测量后，「同推翻率、反结果」的解释链**闭合**。**仍未测**：为何 UNSW 映射方向对——候选解释为 Belkin 混淆双向对称（`f_ab`=0.361 / `f_ba`=0.446）且跨天稳定，但 `cluster_definition.json` 的 `f` 是跨天**均值**，逐天稳定性未测，此条不进承重结构。 ③**两边的「困难簇」不是同一种东西**：UNSW Belkin 对**域内就在随机线上**（`src_oof_H_rf` 0.4585/0.5556/0.5167、`target_H_rf` 0.4750–0.5600、二元正确率约 0.60），自采困难簇**即使删到 59 列（`strict59_ra`，删 35 列、含 `rssi` 族 8 列）域内仍有 0.73–0.88**（`g0_environment_grid_strict59_ra_r5`，rf、同轮次时间块、R2–R5 的 Light_T1/Light_XM/Sensor）→ UNSW 是**模式一饱和**端点（信息不在那 61 列里）。 **推翻现象的定位反而强复现**：困难 0.1292（633/4900）vs 易 **0.000651**（28/43031）= **198 倍**（自采 0.7806 vs 0）—— 定位在，幅度弱 6 倍，F1 后果弱 29 倍。 **审稿用处**：同推翻率、反结果，挡掉「stacking 崩盘只是困难类的副产品」这一质疑。 **未测缺口（本行明记，不得静默）**：(a) ~~UNSW 侧四格分解~~ **已测（见②）**，剩余 = Belkin 混淆的逐天方向稳定性；(b) Belkin 对为何域内不可分（61 列是否根本不含区分信息）→ 见 `UNSW-BELKIN-CAPACITY`（待登记）。 **摄像头不是困难簇（2026-09-04 核实；`cluster_definition.json` τ=0.1 下摄像头之间零边）**：8 台摄像头中 5–6 台过 ≥100 窗/天门槛，域内 F1 组均值 0.9364–0.9585（最低 `TPLinkCam` 0.8707），错分全部流向非摄像头且 ≤2.5%；而 `Dropcam`/`NestDropcam`（同 OUI `30:8c:fb`）与 `InsteonCam_wired`/`InsteonCam_wifi`（同一台设备两网口）这两对**同型号对**在设备表中存在，却分别只有 **209 窗/1 天** 与 **45 窗/1 天**，过不了门槛 → **「UNSW 摄像头好分」部分是存活者偏差**，须在限制章节明写。**去掉 Belkin 对后 macro 从 0.8731/0.8797/0.8907 抬到 0.9198/0.9183/0.9557→ UNSW 的全部缺口就是这一对。** 以下为运行前登记原文： **四条签名（§4，各自能独立失败）**：①定位 `gap_H<0` 且 `\|gap_H\|>=3\|gap_E\|`，3/3；②免疫 `gap_E>=−0.03`，3/3；③系数签名（簇内自类项至少一模型 ≤0，或 `\|coef\|` 最大项非自类项；≥2/3 类且 ≥2/3 任务；易类必须相反）；④regime 错配 簇内 RF 源域 OOF 与目标域绝对差 `>=0.05`，3/3。**有风险幅度预测（§5）**：`\|gap_H\|` 三任务均值落在 **0.03–0.20**（自采参照 0.2878）；<0.03 或 >0.20 一律记幅度预测失败，>0.20 明令不得当作"更强的复现"。三分支处置见 §6，七道硬门见 §7 | **是（依 §6 分支 C）**：失败的三条签名逐条进正文；UNSW 只作现象成分（+0.032778）与机制**边界条件**——「同推翻率、反结果」的配对比较进正文（**标注为事后解读**）；「摄像头非困难簇 + 同型号对的存活者偏差」进限制章节；`MECHANISM_NOT_EXTERNALLY_REPLICATED` 这一判定本身**如实写出，不得只写边界条件那一面** |

| DA-BASELINES-G0 | 2026-09-03（**运行前登记**，跑完补结论） | 协议 `docs/PROTOCOL_DA_BASELINES_20260903.md`：运行前 sha256 `7fbde21b51f2d324ce634386a5b2ee435d5e4cb546b57064d28c069316d05cd1`，含 §10 修正条款后 `e8516423f3f9aed266b9c1aaedc09b3308850866edae251a08e656329e7f2aeb`；缺陷修复 + 协议 + 单元检验 commit = `419c340`（先写后看：该 commit 先于任何新基线数值产生）；实现 = `results/da_baselines_20260903/run_da_baselines.py` | 域适应族基线重测。**起因 = 已落盘缺陷**：`coral_baseline.py` 第 65 行在行向量约定下取 `coloring_mat = L_tgt`，使 `Cov(X_aligned) = L_tgt.T @ L_tgt` 而非目标 `Ct = L_tgt @ L_tgt.T`；落盘 `cov_reduction_pct` 为 **−113.2% / −479.6% / −886.6%**（协方差差异被**放大** 2.13×/5.80×/9.87×）→ 2026-06「CORAL 使 macro-F1 暴跌 37.75%，说明全局协方差对齐不是瓶颈」的推断**作废**，域适应族此前从未被真正检验。SVD 兜底分支（对称平方根）原本正确，但 `fit` 内 `+1e-5·I` 使 Cholesky 几乎总成功，落盘结果全部走有缺陷分支。**六臂**：`none_raw`（参照 = 主线 RF）/ `zscore`（对照臂，非对齐臂）/ `coral_raw` / `coral_zscore` / `sa_raw` / `sa_zscore`。CORAL = Sun, Feng & Saenko, AAAI 2016 无监督闭式解（原 docstring 误引 Deep CORAL/ECCV 2016，同批改正）；SA = Fernando et al., ICCV 2013，`k` 由确定性规则定死为源域累计解释方差首次 ≥0.95 的最小 k、上限 `min(94,50)`。任务 = 三个旗舰 LORO `{R2,R3}→R4` / `{R2,R4}→R3` / `{R3,R4}→R2`；模型仅 RF，`build_model` 取 `random_state=42, n_jobs=1, class_count=5`；`feature_columns`/`clean_x`/`build_model` 全部 import 不重实现。z-score 只用源域统计量拟合 | 42 | `results/robust_v2/raw_all/features_raw_all_w10.csv`（11303×102 = 8 meta + 94 特征），轮次 R2/R3/R4；源 3653/3669/3690 窗、目标 1853/1837/1816 窗 | `results/da_baselines_20260903/`（da_results.csv / per_arm/ / unit_test_coral.txt / passline.json / acceptance.json / provenance.json / VERDICT.md / RECOVERED_GATE1_KEY_FAILURE.json / GATE1_KEY_DIAGNOSIS.json） | **已完成（双跑 PASS，2026-09-03）；判定 `DA_DOES_NOT_HELP`（§4 分支 C）**。**判据 1 锚定门 PASS**：`none_raw` 三任务与 G0 网格 RF **逐位相同**（0.659212/0.614816/0.809799，差 0.0）。**判据 2 实现正确性门 PASS**：`cov_reduction_pct` 六格 **99.9798%–99.9991%**（门槛 +95%）→ 修复后 CORAL 按构造真正完成对齐，**2026-06 那次运行的对齐从未发生**；单元检验代数身份 `\|\|C.T@C−Ct\|\|_F/\|\|Ct\|\|_F` 旧版 0.616–0.938、修复版 **8.1e-17–1.1e-16**。**判据 3**：四个对齐臂三任务均值 `delta_vs_none` 全部 `< −0.005` → 分支 C。逐臂均值 `coral_raw` **−0.036547** / `coral_zscore` **−0.036833** / `sa_zscore` **−0.089347** / `sa_raw` −0.479852；对照臂 `zscore` +0.000147。`coral_raw` 逐任务 0.639176/0.608551/**0.726460**——最大降幅 **−0.0833** 出现在原本最好的 `{R3,R4}→R2`。**两条必须并报的解读约束（协议 §10，运行后追加、如实披露）**：①`sa_raw` 的 k 被冻结规则定成 **1**（未标准化的 94 维上单一大方差列即占累计解释方差 ≥0.95）→ 一维投影，是**构造性退化而非 SA 的性能**；有意义的 SA 臂是 `sa_zscore`（k=22/22/23）；规则不因结果改动。②`zscore` 臂 Δ 非恒 0（+0.000005/−0.004773/+0.005207）：RF 对逐特征仿射在实数域不变，但标准化改变浮点粒度与分裂点取整，**±0.005 是本实验的数值抖动尺度，小于此的臂间差异不得解释**；双跑逐字节一致证明非随机性。**§5 门 1 规格错误的处置（不掩饰）**：首跑 `gate_1_no_leakage` FAIL，根因是协议把 `(round, window_id)` 写成窗口唯一键，而 `window_id` 按 `source_file` 重新计数（30 source_file = 6 轮 × 5 标签，取值 0–407），该键重复 **8992**；轮次重叠一项本身已 PASS（[0,0,0]），**真正的泄漏风险为零**。修正为 `(round, source_file, window_id)`（重复 0）并要求并报原键计数；首跑失败记录与诊断均保留不删。修正的**范围保证已机器验证**：修正前后 `da_results.csv`/`passline.json`/`per_arm/*.json` **逐字节一致**（`AMENDMENT_SCOPE_VERIFIED`）→ 只动了这一道门的键，未触碰科学路径。六道硬门最终全部 PASS。**结论的准确表述**：可以说「两种全局边缘分布对齐方法在实现被构造性验证正确的前提下都不能缩小跨轮次落差」；**不可以说**「协方差漂移不是瓶颈」——那正是 2026-06 的错误推断形式，本协议只裁定这两种具体方法在本口径下的增益，不裁定协方差漂移的因果地位。四个对齐臂全部劣于 **always-RF**（G1 路线 A regret 0.02025），**未产生可用基线以外的任何方法主张**。**运行前预注册的三判据与六硬门原文**：①锚定门 ≤1e-6 否则 `CALIBER_MISMATCH_STOP`；②`cov_reduction_pct >= +95%` 否则 `CORAL_IMPLEMENTATION_INVALID_STOP`（**没有这一门，任何「域适应无效」的结论都无法与实现缺陷区分**）；③任一对齐臂均值 `>= +0.02` → `DA_REDUCES_CROSS_ROUND_GAP` / 全部 `< +0.02` 且至少一臂 `>= −0.005` → `DA_NEUTRAL` / 全部 `< −0.005` → `DA_DOES_NOT_HELP`；六硬门 = 无泄漏 / `clean_x` 后与对齐后各 0 NaN·0 inf（白化会放大，须显式校验）/ 锚定 / 双跑 md5 逐字节（线程钉死 OMP=MKL=OPENBLAS=1 且 `n_jobs=1`）/ 单元检验 PASS 落盘 / 禁网禁代理 + 解释器 `~/anaconda3/envs/iotcls/bin/python`。明文不做：不改窗口长度/不加特征列/不调 RF 超参/不做特征选择/不换任务集；不用 `category` 粒度；不在 UNSW/CIC 上跑（幅度不可通约）；不计算或恢复 CPD 与 EC-MDM 身份；不因结果不利改门槛、改 k 规则、改臂集合。2026-06 产物已加作废注记并保留原文（`results/robust_v2/report/coral_baseline_results.VOID_20260903.md`）。判定书 = `results/da_baselines_20260903/VERDICT.md` | **是**（作已发表基线进正文；CORAL = Sun, Feng & Saenko, AAAI 2016；SA = Fernando et al., ICCV 2013。不得据此声称协方差漂移的因果地位） |

| TWO-CHANNEL-SELF | 2026-09-03（**运行前登记**，跑完补结论） | 协议 `docs/PROTOCOL_TWO_CHANNEL_20260903.md` sha256 `b499498bc2b172d9be5bdc043ad4ba8d4ca152dd0d4c64940475b741108e9269`，commit `47626ab`（先写后看：该 commit 先于任何流程数字产生） | **两通道诊断流程的定义 + 自采侧样本内演示**。依据三条已测事实：①源域元层重映射 `m_src[i→j]` 对目标域逐类对伤害 Spearman **+0.798**，控制 `m_src` 后 `f_ji_src` 仅剩 +0.064（后者是前者的代理）；②源域拟合的多变量类对判别 CV AUC 0.93–0.98、目标域 0.62–0.68，39 个困难簇组合中 **13 个目标 AUC < 0.5**（边界反号，最低 0.0000），易类对 5/5 组合 1.0000→1.0000 零反转；③两者**不相关**（`auc_tgt`↔`m_src_max` −0.153、↔`harm_max` −0.212，n=35，分组比较方向与相关系数相反）→ 只能作**两个坐标交替优化**，不能合成一条链。**坐标 1（表示）**：逐簇内无序类对拟合 LogisticRegression（源域统计量标准化），`ΔAUC_tgt(F)` 逐族剔除；两段制——代理量**提议**候选族，接受须重训完整四模型集并在 9 个跨轮次任务的 macro-F1 上同时满足 `ΣΔ>0`、`G/\|L\|>=3`、`\|W\|<=0.5×A`。**坐标 2（元层）**：`m_src_max>0` 提议不堆叠、改用按源域 OOF macro-F1 选出的最佳基模型；接受须在**内层历史 LORO**（只用该任务的源轮次两两组成，全程不触碰目标轮次）上核验该配置不劣于 stacking；`\|S\|=1` 无内层 LORO 故不执行任何动作，记 `COORD2_UNAVAILABLE_SINGLE_SOURCE`。两坐标均为「诊断提议 + 有标签证据接受」，故流程在其所依据的证据上**按构造单调**——但仅在历史证据上成立，能否迁移到未见新环境是 §4 要检验的经验问题。**族导出规则机械且跨数据集一致**：按列名首个下划线段分组、成员 <2 并入 `singletons`；实测输出自采 94 列→10 组（subtype16 len14 singletons13 interarrival12 subwin9 rssi8 burst7 up7 down6 unique2）、UNSW 62 列→7 组。簇导出沿用 `TAU=0.10` 不改。交替两个顺序都跑，终点不同记 `PATH_DEPENDENT` | 42 | `results/robust_v2/raw_all/features_raw_all_w10.csv`（11303×102）；任务 = 源与目标轮次全部落在 `{R2,R3,R4}` 内的 **9 个**跨轮次任务（`\|S\|=2` 三个 + `\|S\|=1` 六个）。R5 位置变更与 R6/R7 操作抖动**不进本协议**（泛化环境，另案；且 `docs/RSSI特征分析.md` 记录删 RSSI 在 R5 0.6745→0.6531、R6+R7 0.7550→0.7310 上**有害**，方向与跨轮次相反，该形态在 94 维下从未重测） | `results/two_channel_20260903/`（families.json / diag_coord1.csv / diag_coord2.csv / procedure_trace.json / eval_five_points.csv / passline.json / acceptance.json / provenance.json / VERDICT.md） | **已完成（双跑 PASS，2026-09-03 16:37:14→17:20:02 CST，单跑 21.0 分钟）；判定 `PROCEDURE_BEATS_PRACTICE`，五条判据全过。** **八点对照（9 个跨轮次任务算术均值，目标域 macro-F1）**：⓪ 源域 CV 选模型（标准做法）**0.5191**；⓪′ 按 RF 重要性选特征 + 源域 CV（标准做法）**0.5258**；① 全特征+stacking（朴素起点）**0.5249**；② 全特征+不堆叠 **0.5581**；③ 删列+stacking **0.7544**；④ **流程终点 0.7604**（两个交替顺序同）；⑤ 逐任务 oracle **0.7648**。**判据1（主判据）** `④>⓪` 且 `④>⓪′` → 过（+0.2413 / +0.2346）；**判据2** `④−① = +0.2355`（门槛 +0.05）过；**判据3** `④ >= max(②,③)` = 0.7604 ≥ 0.7544 过；**判据4** `⑤−④ = 0.0045`（门槛 0.05）过；**判据5** IID 护栏过；**路径依赖 False**（两个顺序同终点、同删族）。六道硬门全过：轮次重叠 9×0、唯一键 `(round,source_file,window_id)` 重复 9×0（并报原键 `(round,window_id)` 重复 2912–4375，该键非唯一，同 `PROTOCOL_DA_BASELINES_20260903.md` §10）、元数据型列为空、`clean_x` 后 0 非有限值、双跑 6 产物 md5 逐字节一致、线程三变量均为 1。**流程做了什么**：坐标 1 **只删 `rssi`（8 列）**——代理量 `rssi +5.244` 是唯一显著为正（其余 `subwin +0.016`、`subtype −0.057`、`interarrival −0.226`、`singletons −0.228`、`unique −0.335`、`up −0.367`、`down −0.619`、`len −0.724`、`burst −0.889`），真实目标接受检查 `Σ +2.0662`、`G/\|L\| = inf`（**9 个任务无一变差**）、`W = +0.1188`（最差任务仍改善）；第二轮唯一代理量为正的 `subwin`（+0.110）在真实目标上 `Σ −0.0074`、比值 0.63 被**拒绝**，贪心停止 → **两段制生效，代理量说可以而真实目标说不行**。坐标 2 在删列后 9 个任务全部触发并接受"不堆叠"，两个触发器分工与协议预期一致：`\|S\|=2` 的 `m_src_max` 0.9966/0.6202/1.0000（最差类对 `Sensor→Light_T1`、`Light_XM→Light_T1`）经内层 LORO 接受；`\|S\|=1` 的 `m_src_max` **精确为 0**（映射通道结构性失效），全靠 `net_dis_src` 0.0370/0.0446/0.0000 经嵌套源域 OOF 接受，其中两个恰为 0.0000 未触发即协议 §12.1 记录的 1 个漏报。**过程性顺序差异（终点相同）**：`2then1` 下坐标 2 先在全特征上运行，三个 `\|S\|=2` 任务的内层 LORO **拒绝**换模型；删掉 `rssi` 后再运行 9 个全部接受——**接受检查的答案随特征集改变**，这正是两段制存在的理由。**最能说明贡献的一格**：⓪′ 保留 57 列、**其中 7 列是 rssi**，收益 +0.0009 ≈ 0 → **标准特征选择恰好保留了流程要删的那一族**，因为它优化源域表现而失效在迁移。**限制（与结论同等地位）**：①**贡献 97% 来自坐标 1**（③−① = +0.2295，④ 再加 +0.0060，坐标 2 单独 +0.0332）→ 这是**表示层的结果**，元层机器是机制叙述主角、对准确率是配角；②**有一格变差**：`g0_R3_R4_to_R2` 从 ② 0.8098（该任务最优）到 ④ 0.7696，亏 0.0402，而它正是 RF 最高分任务，与「边界本来迁得动处删 rssi 有害」一致；③**效应量 +0.2355 远大于事前从 `strict59_ra` 网格外推的 ~+0.13**，可能因本协议 9 个任务是最难子集（`\|S\|<=2`）而外推用的是含 `\|S\|=3` 的 150 任务均值（`\|S\|=3` 上 gap_H 已衰减至 −0.0805），**此解释未经检验**；④**本轮不是验证**（§3.3 原文已进判定书）——自采 3 个同条件轮次留一后只剩 `\|S\|=1`，坐标 2 映射通道在那里结构性失效（30/30 精确为 0），验证只能在 UNSW/CIC；⑤**§12.3 两句必须同时写**：可以说"贪心流程优于标准做法"，**不可以说**"本移动集合内不存在更优点"（后者由 175 配置穷举格另立协议判定）；⑥终点对起点条件性（§11）：`full94` 是开发累积而非按准则选出，④ 是从 `full94` 出发可达的局部最优。**§12.7 全文已在判定书复述（要求不得省略）**：首启 20 分钟后主动中止，(a) `build_model` 的 lightgbm 分支缺 `deterministic`/`force_col_wise`，沿用 D12/UNSW-TEST1 R4 修法在返回对象上 `set_params` 补齐 + `threadpool_limits(1)`，`build_model` 本身不改；(b) 接受条件 (iii) `\|W\| <= 0.5×A` 在零损伤时把最小增益当最大损失，导致"全部任务都改善"必然不通过，修正为 `W>=0` 时自动满足，**两个阈值数值一字未改**，且**中止前的原始日志行原文写入协议与判定书**，因为该缺陷是在看到候选被拒之后才发现的。判定书 = `results/two_channel_20260903/VERDICT.md` ｜ 以下为运行前预注册原文：**唯一被优化的目标是跨轮次 macro-F1**；同轮次（IID）只作护栏与天花板参照、不进任何判据（部署形态是训一次跑后续轮次，从不在同轮次 regime 内运行）。**八个对照点**：⓪源域 CV 选模型（标准做法）、⓪′按 RF 重要性保留累计 ≥0.95 的最小列集再按 ⓪ 选模型（标准做法，**预期保留 rssi**——`rssi_*` 占 RF 重要性质量 27.4%、排 #2/#3/#4、`selected_rate` 恒 1.0，而本流程要删它，此对照使贡献可辨识：标准选择优化源域表现，失效在迁移）、⓪″CORAL/SA（已完成，`DA_DOES_NOT_HELP`，`coral_raw` −0.0365、`sa_zscore` −0.0893）、①全特征+stacking（朴素起点）、②全特征+不堆叠、③删列+stacking、④流程终点、⑤逐任务 oracle。**五条判据（运行前定死）**：①**主判据** `④>⓪` 且 `④>⓪′`，否则 `NO_GAIN_OVER_PRACTICE`——这是唯一能区分"流程有贡献"与"重新发现标准做法"的检验；②`④−① >= +0.05`（量级依据：`gap_vs_best_base` 在 OOD 上 −0.0729→−0.0062，单靠坐标 2 就应越线）；③`④ >= max(②,③)`；④`⑤−④ <= 0.05` 否则记 `REGRET_LARGE` 不阻断；⑤IID `>=` 跨轮次否则 `FEATURE_SET_DEGENERATE`。**必须按 `\|S\|` 分层并报**。**本协议明确不构成验证**（§3.3 原文须进判定书）：自采只有 3 个同条件轮次，留一之后历史只剩 `\|S\|=1`，而 `\|S\|=1` 时坐标 2 的诊断**结构性失效**（实测 30/30 个任务 `m_src_max` 精确 0.0000，因单轮次时 stacking 的 OOF 分组退化为轮内时间块、源域 OOF 中不存在跨轮次混淆；而 gap_H 仍为 −0.0867）→ **自采面板在结构上无法验证本流程**，验证在 UNSW（20 天）与 CIC（2-Idle 30 天）进行。**§10 使用条件**：历史 1 个环境时只训练不改配置（坐标 2 无信号；坐标 1 在该档最危险，98% 的代理量损失集中在 `\|S\|=1`，损失合计 −2.2521/−2.2981、最差 −0.2951，而 `\|S\|=2` 最差仅 −0.0175）；历史 ≥2 个环境才开始优化。**§11 起点任意性**：`full94` 是开发累积而非按准则选出，不得把其同轮次 0.9622 表述为"上限"或"最优"；终点是从 `full94` 出发可达的局部最优，判定书须与 `PATH_DEPENDENT` 并列声明终点对起点的条件性。六道硬门 = 无泄漏（唯一键 `(round,source_file,window_id)`，并报原键计数）/ 元数据不入特征（族导出后核对无时间戳序号列，清单落盘）/ 坐标 2 不读目标标签（实现审阅逐行核对）/ 有限值 / 双跑 md5 逐字节（`OMP=MKL=OPENBLAS=1` 且 `n_jobs=1`）/ 禁网禁代理 + 解释器 `~/anaconda3/envs/iotcls/bin/python`。预估机器时间 4.7–7 小时（实测单配置跑完 9 任务 225 s） | **是**（流程 + 两个诊断进正文；主判据是优于标准做法 ⓪/⓪′ 而非仅优于朴素起点。不得声称是移动集合内最优点，也不得表述为流程的验证——§3.3） |

| UNSW-TWO-CHANNEL-ANCHOR | 2026-09-03（**运行前登记**；锚定阶段，全部测量只在历史池 H 内，不触碰留出池 T） | 协议 `docs/PROTOCOL_TWO_CHANNEL_UNSW_20260903.md` sha256 `b3974a48d35d9097795a0447087d970262e78795dd08f2e43ee502b442d57045`，commit `c388f2c`（先写后看：该 commit 先于任何 UNSW 流程数字产生）。本协议整体引用 `docs/PROTOCOL_TWO_CHANNEL_20260903.md` sha256 `dc298198e70957dcd4d1b445900ce85e6ae7bbfac60cac4c8c546515594c0e86` 的 §2.1/§2.3/§2.4/§2.5 与 §12.1/§12.4/§12.5/§12.7，**一字不改** | **两通道流程搬 UNSW 的第一段（两段冻结之锚定段）**。为什么分两段：自采的判据 2 门槛 `+0.05` 由已测 `gap_vs_best_base`（OOD −0.0729）定，而 UNSW 落差仅 0.0328，`+0.05` **结构上不可达**，照搬即设一道必然失败的门。故先测射程、再把判据写成无量纲形式 `④−① >= 0.5×mean\|gap_vs_best_base\|`；**自采回算 `0.5×0.0961 = 0.0481` 几乎精确复现原 +0.05**，故这是同一条判据的忠实改写而非为 UNSW 放宽。分母坚决**不用**同日/跨天落差，因为 UNSW 的 61 列本身可能已是削减表示（同日 0.8793 ≈ 自采 `strict59` 同轮次 0.8682，而 UNSW 是网关侧有线抓包，`rssi_*`/`subtype_*`/帧类型比例**根本不存在**），`strict59` 已证同日天花板会随删列一起下降（0.9622→0.8682）→ **同日数字不是天花板，不得作上界或 oracle**。本段测四个锚定量：**锚1** 逐内层任务 `gap_vs_best_base`（臂 A 的 14 个 + 臂 B 的 2 个，均值与逐任务值）= 坐标 2 可得射程 = 判据 2 分母；**锚2** 逐日同日与 H 内跨天的 macro-F1/accuracy（只作参照并报，不定义射程）；**锚3** 7 个族在 H 内的代理量 `S(F)` 完整排序（**不做任何删除动作**）= §5.4 三情形判别依据，并报头名/次名比值（自采上 `rssi +5.244` vs `subwin +0.016` = 328 倍，该形态本身是"有可删"的签名，**只报不设门槛**以免用自采数字反卡 UNSW）；**锚4** 核对量——特征列数 **61 还是 62**、类数、逐日窗口数、缓存 md5、族导出落盘、单位成本与总时长估计 | 42 | UNSW 20 天窗口特征缓存（设备身份粒度，`w10`，路径与 md5 在 `anchor.json` 落盘）。**元数据列必须含 `window_start_epoch`**：`device,day,label,source_file,window_id,window_start,window_end,window_start_epoch`。**冻结本协议之前已机械核对并如实报出的一件事**——缓存 `results/unsw_features_full/features_day_*.csv`（20 天，每天一文件）原始 **69 列 = 8 元数据 + 61 特征**；按冻结的族导出规则实测：**正确版（META 含 `window_start_epoch`）61 列 → `len14 interarrival12 subwin9 burst7 up7 down6 singletons6`**；而**不含 epoch 时得 62 列 → `len14 interarrival12 subwin9 burst7 singletons7 up7 down6`，与 `TWO-CHANNEL-SELF` 行及 `PROTOCOL_TWO_CHANNEL_20260903.md` §2.1 那句"UNSW 62 列→7 组"逐字一致，连分组顺序都一致**（三个大小为 7 的族按名字升序 `burst < singletons < up`，`singletons7` 恰插中间）→ 认定那句结构性描述是**含时间戳的版本**，是一处已发生的错误。**未污染任何已冻结数字**（自采那次用的是自采 94 列与自采自己的元数据列表，UNSW 那句从未进入任何计算），**但是一个尚未引爆的坑**：若照它跑，`window_start_epoch`（float64，首段 `window`、成员 1 → 并入 `singletons`）会作为 `singletons` 成员进入**全部**配置 ①②③④，而它与 `day` 完全共线 → 同日虚高、跨天因测试日 epoch 落在训练区间外而崩坏。**处置**：本协议一律用 61 列版本，`families.json` 落盘正确的 7 组与成员清单，硬门 2 逐项核对该列不在特征中；**不改动 `PROTOCOL_TWO_CHANNEL_20260903.md`**（sha256 已登记、运行已完成，改动会切断登记链），按标注方式记录，不静默改数。BOM 为非问题：缓存以 `utf-8-sig` 写出、文件首有 `ef bb bf`，但 pandas C 解析器自动剥离，第一列读出即 `device`。切分点运行前定死：**H = 前 14 天，T = 后 6 天**（时间序）；两个源规模臂 **臂 A `\|S\|=14`（源=全部 H，内层=H 内留一日 14 个任务，每个 `\|S\|=13`）**、**臂 B `\|S\|=2`（源=H 最后 2 天，内层=两两 LORO 2 个任务，每个 `\|S\|=1`）** | `results/two_channel_unsw_20260903/anchor.json` + `families.json` + `split.json`（本段只产这三个；`split.json` 先于任何数字落盘） | 待运行。**本段预注册的约束（三条，界定"锚定先于判据"这处对协议纪律的偏离）**：①锚定量全部只在 H 内计算，`T` 在第二段之前不被读；②锚定**不产生任何判定性数字**（无配置比较、无判据评估、无删族、无换模型）；③**除判据 2 的分母外，第二段不得改动协议任何一个字**——若发现必须改，按修正条款追加、原文保留，与自采协议 §12 做法一致。**为什么必须有臂 B**：元层损伤已测到随源环境数自行衰减（`\|S\|=2` gap_H −0.1245 → `\|S\|=3` −0.0805，推翻率 0.3503→0.1725），臂 A 的 `\|S\|=14` 远超自采任何档，**现象本身可能已衰减掉**，此时坐标 2 不开火是"没病可治"而非"药不管用"，而这两件事从结果上看一模一样；臂 B 的 `\|S\|=2` 与自采三个旗舰任务同档，是能直接对照的那一格。两臂方向相反则记 `SOURCE_SIZE_DEPENDENT`，两臂完整数字都必须报。**运行预算**：单跑 ≤12 h、双跑 ≤24 h（自采那次 21 min；成本按类对数平方级放大，自采 5 类=10 个类对）；超预算则按运行前定死的顺序启用削减 **R1**（坐标 1 类对范围改为**源域** RF OOF 混淆矩阵上 `max(f_ij,f_ji)>=TAU=0.10` 的类对——零目标标签，与 §12.5 删掉的簇导出**不是**同一件事，那一条读的是目标域混淆矩阵；启用时须在自采上只重跑坐标 1 诊断核对 10 族排序是否与已冻结的全类对版本一致，不一致如实报出）、**R2**（臂 A 内层任务由 14 降为 H **最后 6 天**的留一日，子集运行前定死），**不得临时另想办法** | 待定（锚定完成后填入判据 2 分母，转第二段 `UNSW-TWO-CHANNEL-VALIDATE`） |

| UNSW-TWO-CHANNEL-VALIDATE | 2026-09-03（**运行前登记**；第二段验证。**锚定段 `UNSW-TWO-CHANNEL-ANCHOR` 完成并填入判据 2 分母之后方可运行**） | 协议 `docs/PROTOCOL_TWO_CHANNEL_UNSW_20260903.md` sha256 `b3974a48d35d9097795a0447087d970262e78795dd08f2e43ee502b442d57045`，commit `c388f2c`（第二段不另立协议文件，整体沿用该协议；**本行本身即第二段的预注册——先写后看：本行先于留出池 T 的任何数字产生**）。本行只新增两件协议中尚未定义的东西：无量纲迁移效率 `TE` 的定义、与四分支解读规则；**协议正文一个字不改**，符合 ANCHOR 行预注册约束③（除判据 2 分母外第二段不得改动协议） | **新增观测量 `TE`（无量纲迁移效率）**：`TE = 跨环境 best_base macro-F1 / 同环境 best_base macro-F1`，同一特征集、同一模型集（`best_base` = 逐任务取非 stacking 四模型最大值，再按任务算术均值）。**分母必须用时间块口径的同环境划分**——随机划分含轮内/日内泄漏，不得作分母（自采实测两口径数值不可混用：时间块 full94 **0.7879** / strict59_ra **0.9237**；随机 full94 0.746 / strict59_ra 0.892，方向一致但量级不同）。**设此量的理由**：`strict59` 已证同环境天花板会随删列一起下降（0.9622→0.8682），故任何"到同环境上限的距离"都不是固定靶——取比值把天花板移动约掉，得到跨数据集可比的量。动机与协议把判据 2 改写成无量纲形式 `④−① >= 0.5×mean\|gap_vs_best_base\|` 完全相同。**`TE` 只作观测与解读用，不进五条判据**（判据集在 ANCHOR 冻结时已定死，此处不新增门）。**参照值与其来源等级（必须随数字一并写出，不得省略）**：自采 `TE`（时间块）full94 **0.7879**（0.7009/0.8896）、strict59_ra **0.9237**（0.7959/0.8616）——**探索性**，2026-09-03 由 `results/g0_environment_grid/summary_metrics.csv` 与 `results/g0_environment_grid_strict59_ra_r5/summary_metrics.csv` 现算（`/tmp` 脚本、无协议、单次非双跑），进正文前须随协议冻结重算；UNSW `TE` ≈ **0.9627**——**推算不是测量**，由 `UNSW-IID-REF` 行的同日 0.8793 与配对落差均值 +0.032778 得 (0.8793−0.0328)/0.8793，锚2 将给出逐日真值，届时**以锚2 为准并覆盖此推算**。**预注册预测（运行前定死，门槛不因结果调整）**：**P1 无支配族** — 锚3 的族代理量 `S(F)` 头名/次名比值 **< 10**（自采为 `rssi +5.244` / `subwin +0.016` = **328**）；若次名 `S(F) <= 0`，则改以"头名为唯一正值且 `S(F) > 0`"作支配族判据。**P1′ 坐标 1 近乎无动作** — 第二段 `\|③−①\| < 0.05`（自采 ③−① = **+0.2295**）。**P2 迁移效率已高** — 臂 A 与臂 B 各自 `TE >= 0.93`（门槛取自上述两参照值 0.9237 与 0.9627 之间，**选定于任何 T 池数字之前**）。**理由（这是可证伪推论，不是事后解释）**：UNSW 为网关侧有线抓包，`rssi_*`/`subtype_*`/帧类型比例**根本不存在**，即它本身已处在"反转族被删掉"的状态；而自采上 strict59_ra 把 `TE` 从 0.7879 抬到 0.9237、接近 UNSW 推算的 0.9627。若机制叙述为真，则流程在 UNSW 上应当**正确地什么都不做**。**四分支解读规则（`TE` × 支配族，2×2，运行前定死）**：①`TE` 高 且 无支配族 → `NOTHING_TO_FIX`，机制的正面确认，但**明令只算特异性证据（流程不乱开火），不得表述为"流程有效"**——无病可治不证明药有效；这与 ANCHOR 行对坐标 2 的同一处论证同构（"不开火是'没病可治'还是'药不管用'，从结果上看一模一样"），`TE` 正是把这两者分开的那个量。②`TE` 低 且 无支配族 → `MECHANISM_ABSENT_HERE`，UNSW 的失效来源不在本框架覆盖内，如实写入 Discussion，不改自采判定。③`TE` 低 且 有支配族 → **最强分支**：反转族不止 `rssi` 一类、形式比现叙述更普遍，必须重写"`rssi` 是做功的那一族"的表述并把该族补入机制叙述。④`TE` 高 且 有支配族 → 内部不一致，先查缓存与族导出（**优先核对 `window_start_epoch` 是否混入特征**——该坑已在 ANCHOR 行记录：含它得 62 列且与 `day` 共线 → 同日虚高、跨天崩坏），排除后方可解读。**并报要求**：`TE` 按 `\|S\|` 分层（臂 A `\|S\|=14` / 臂 B `\|S\|=2`），与 ANCHOR 行"必须按 `\|S\|` 分层并报"一致；两臂 `TE` 方向相反同样记 `SOURCE_SIZE_DEPENDENT` | 42 | 同 ANCHOR 行：UNSW 20 天窗口特征缓存（设备身份粒度 `w10`，**61 列版本**，硬门逐项核对 `window_start_epoch` 不入特征）。H = 前 14 天（锚定与全部提议/接受证据）、T = 后 6 天（**只在本段被读一次**） | `results/two_channel_unsw_20260903/`（第二段产物：`eval_five_points.csv` / `passline.json` / `transfer_efficiency.json` / `procedure_trace.json` / `VERDICT.md`） | 待运行（锚定段未完成，判据 2 分母未填）。本行登记的是**预测与解读规则**，不含任何 T 池数字 | 待定（`TE` 定义与四分支规则无论走哪一支都进 Discussion；落在 `NOTHING_TO_FIX` 分支时**不得**进方法章的有效性论证） |

| HISTINT-OBJ-SIGNAL | 2026-09-03（**运行前登记**，跑完补结论） | 规格 `docs/SPEC_HISTINT_OBJECTIVE_SIGNAL_20260903.md` sha256 `71fabf3a717a4f13e8f42705c4a68a31a46f59ae504c85e5db680e13ce173e06`；**先写后看**：规格与本行同一 commit 落地（`git log --oneline -- docs/SPEC_HISTINT_OBJECTIVE_SIGNAL_20260903.md`），该 commit 先于任何 `S_obj` 数字产生。上游 = `docs/PROTOCOL_TWO_CHANNEL_20260903.md` sha256 `dc298198e70957dcd4d1b445900ce85e6ae7bbfac60cac4c8c546515594c0e86` 与判定书 `results/two_channel_20260903/VERDICT.md`，**协议一字不改、`PROCEDURE_BEATS_PRACTICE` 判定不重开** | **把坐标 1 的不稳定度放到目标函数自己的单位上重算一次，检验 AUC 代理在提议阶段是否忠实。****动机**：协议 §2.3 已原文记录代理量与真实目标在**接受阶段**给出不同答案（删 `rssi` 代理量 `W=−0.2951` 超上限不过、macro-F1 上 9 任务无一变差过）→ 提议阶段选 AUC 而非 macro-F1 这件事需要独立支撑或反证。**明确不是**对坐标 1 可部署性的检验：`S(F)` 只在历史池 H 内求和（9 任务源与目标全部落在 `{R2,R3,R4}`），对 H 之外的新轮次本就零标签需求，UNSW 锚3「`S(F)` 在 H 内」即依赖这一点；自采那轮是样本内演示的原因是**评估目标与算信号的池重合**（§3.3 已披露），与「是否读目标标签」是两个不同问题——**本行不声称修复任何可部署性问题**。**信号**：`H={R2,R3,R4}`，内层任务取协议 `TASKS` 中 `\|S\|=2` 的三个逐字沿用；`G_r(c)=JOINT(c)−LORO_r(c)`（「没见过轮次 r」的损失），`S_obj(F)=Σ_r[G_r(c_full)−G_r(c_full\F)]`，`>0` 即不稳定族。**恒等分解并报**：`S_obj(F)=ΔLORO(F)−3×ΔJOINT(F)`（迁移收益 − 天花板代价），实现中逐族断言、容差 1e-12。**`JOINT` 是本规格唯一新增定义**（协议与 `run_two_channel.py` 均未计算任何 IID 量——`passline.json` 无判据 5 条目、`VERDICT.md` 判据 5 数值列为「—」，故必须自行定义并声明为新增）：H 中每轮次用 `time_blocks(window_start,k=5)` 划 5 块，训练=各轮次块 0..3 并集、测试=各轮次块 4 并集，不随机打散相邻窗口（§9.1 纪律）；`c_full` 下的 `JOINT` 与 `MAINLINE_20260903.md` §2「联合三轮时间块 0.8936」并列报出，代码路径不同**不要求相等**，差异过大如实报出、不据此调整定义。**聚合两口径全部并报**：`max_base`（逐任务取三基模型 macro-F1 最大值）为**主口径**（与已登记 `TE` 定义一致，从特征信号中移除模型选择噪声）、`best_base_src`（协议 `coord2_diag` 的源域 OOF 选模型）为并报。基模型集 = rf/xgboost/lightgbm，**不含 stacking**（本信号测特征不稳定度，元层是坐标 2 的事）。族导出沿用 `derive_families` 不另定义。**主判据（二值，运行前定死）**：`max_base` 口径下 `rssi` 排第一 → `OBJECTIVE_SIGNAL_AGREES`，否则 `OBJECTIVE_SIGNAL_DISAGREES`。**只报不设门槛**（理由同 UNSW 锚3：`S(F)` 与 `S_obj(F)` 单位不同，用 AUC 口径数字去卡 macro-F1 口径会反卡自己）：头名/次名比值、对冻结 `S(F)` 10 族排序的 Spearman、逐族 `ΔLORO`/`ΔJOINT` 分解、`best_base_src` 口径同一排序、逐任务逐族原始 macro-F1。**三分支处置**：①`rssi` 第一且为唯一正值族 → AUC 代理忠实，可进正文作提议阶段形式选择的支撑；②`rssi` 第一但存在其它正值族 → 目标函数单位下还有别的不稳定族、AUC 代理漏了它，该族须另立协议评估，**本规格内不做任何删除动作**；③`rssi` 非第一 → 提议阶段形式选择须重新论证，`TWO-CHANNEL-SELF` 判定不动（其接受阶段本就用 macro-F1、删 `rssi` 由真实目标背书），但正文**不得**把 AUC 代理表述为一般性的不稳定度量。**本规格不执行任何删除动作、不改变任何配置、不产生新的流程终点。****六道硬门**：复用冻结实现（`import run_two_channel`，直接调用其 `Data`/`derive_families`/`make_model`/`fit_eval`/`coord2_diag`/`time_blocks`/`TASKS`，`feature_columns`/`clean_x`/`build_model`/`fit_label_encoder` 经其转引，一律不重实现）/ 恒等式逐族断言 / 线程三变量=1 且 `n_jobs=1` / 双跑 md5 逐字节一致（除 `provenance.json` 时间戳字段）/ 解释器 `~/anaconda3/envs/iotcls/bin/python` 禁网禁代理 / 种子 42 | 42 | `results/robust_v2/raw_all/features_raw_all_w10.csv`（11303×102，同 `TWO-CHANNEL-SELF`）；对照基准 = `results/two_channel_20260903/diag_coord1.csv`（900 行 = 9 任务 × 10 类对 × 10 族，冻结 `S(F)`） | `results/histint_objective_signal_20260903/`（`s_obj.csv` / `raw_scores.csv` / `joint_reference.json` / `passline.json` / `provenance.json` / `VERDICT.md`） | **已完成（双跑 PASS，2026-09-04 00:19:44→01:01:43 CST，单跑 20.6 min）；判定 `OBJECTIVE_SIGNAL_AGREES`，落**分支 ②**。** **主判据（`max_base` 口径下 `rssi` 排第一）过**：两个聚合口径下 `rssi` 均第一；对冻结 AUC 代理 `S(F)` 的 Spearman **0.8182**（`max_base`）/ 0.6848（`best_base_src`），头名/次名比值 22.8 / 10.9 → **AUC 代理在提议阶段是忠实代理**，协议 §2.3 选它有了独立支撑。**`S_obj` 排序与恒等分解（`max_base`，三内层任务合计）**：`rssi` +0.4140（`ΔLORO` **+0.2920**、`ΔJOINT` −0.0406）> `singletons` +0.0182（`ΔLORO` −0.0014）> `subtype` +0.0076（−0.0038）> `interarrival` +0.0045（−0.0142）> `unique` +0.0041（+0.0012）> `subwin` −0.0104 > `down` −0.0115 > `up` −0.0190 > `burst` −0.0210 > `len` −0.0439（`ΔLORO` **−0.1610**）；恒等式 `S_obj = ΔLORO − 3×ΔJOINT` 逐族断言通过（容差 1e-12）。**分支 ② 的实质（本行最重要的一条）**：4 个额外正值族的 `ΔLORO` 全部 ≤ +0.0012，即**对迁移毫无贡献**——它们 `S_obj` 为正纯粹来自 `ΔJOINT` 为负，也就是**删掉它们把天花板砸得比把跨轮次砸得更多**。故「联合减 LORO」这个函数形式有一处内生缺陷：**它奖励砸天花板**。正确用法是必须并报 `ΔLORO`；规格 §4 要求逐族并报分解，恰好使该缺陷可见。**预注册处置照记**：分支 ② 要求那 4 族「另立协议评估、本规格内不做删除动作」——本行如实记录该处置，同时记录另一种读法（它们是函数形式的产物而非真不稳定族）；**是否执行另立协议由用户裁定，本行不代为推翻预注册**。**`rssi` 与 `len` 是天然对照（可直接进正文）**：RF 重要性份额 25.29% vs 22.00%（几乎相同；出自 `results/robust_v2/raw_all/joint_R2_R3_R4/all_features/rf/feature_importance.csv`，单任务、指示性，与登记表 ⓪′ 口径的 27.4% 不可混用），而 `ΔLORO` 一个 **+0.2920** 一个 **−0.1610** → 同强度、反方向，是「伤害 = 强度 × 反转」在同一数据集同一量纲下的实证对照。**`JOINT` 参照**：`full94` = **0.9101**、删 `rssi` 后 0.8694（天花板代价 −0.0406）；与 `MAINLINE` §2「联合三轮时间块 0.8936」差 **+0.0165**，估计量不同（本规格种子 42 单次、测试集取每轮次 5 块中的第 5 块；§2 为 5 种子均值），按规格 §3「只报不设门槛」处理。**硬门**：5 个实质产物双跑 md5 逐字节一致（`raw_scores.csv` `b04701d4…` / `s_obj.csv` `6c698bfb…` / `joint_reference.json` `3ab1cffb…` / `passline.json` `a3df1e89…` / `VERDICT.md` `ff912a91…`）；`provenance` 去 `_volatile` 后**唯一差异是 `git_head`**（A=`b2bac11`、B=`4cac2a3`）——因两跑之间提交了 `R567-94DIM-RETEST`／`CIC-RANGE-ANCHOR` 协议与 MAC 映射生成器；而 `cache_md5`／`frozen_runner_md5`／`histint_runner_md5` 三个输入哈希两跑完全相同，**证明输入未变**、计算未受影响。**过程教训：双跑之间不应提交任何 commit**，后续实验遵守。**首启失败的原文披露（沿用 §12.7 先例，不省略）**：第一次启动（2026-09-03 23:56:44）在写 `VERDICT.md` 的一行因格式化缺陷崩溃——`TypeError: not all arguments converted during string formatting`，成因是该行已把 `0.8936` 写成字面量却又多传一个参数；**崩溃发生在全部计算完成之后，四个产物已落盘，且族排序结果在修复前已被看到**；修复只改该行字符串，未触任何计算、未改任何判据、阈值、任务集或聚合口径；规格与全部判据已于 commit `4ceea0c` 冻结并先于任何数字产生，故「先写后看」成立。以下为运行前登记原文：**预估成本**：11 配置（基线 + 10 族剔除）×（3 内层任务 + 1 联合）× 3 基模型 = 132 次拟合，按 `TWO-CHANNEL-SELF` 实测折算约 14 min/单跑、双跑约 28 min。**运行前已从冻结 `diag_coord1.csv` 免费得到的分层核对（不属本规格判据，另记）**：`S(F)` 按 `\|S\|` 分层后 `rssi` 在两档均排第一——`\|S\|=2` 档 **+1.1076 且为唯一正值族**（次名 `subwin` 转负 −0.0067，比值 inf）、`\|S\|=1` 档 +4.1364（比值 186.3）→ 删 `rssi` 的决定在 §10 允许流程动手的那一档（历史 ≥2 环境）上成立且更干净。**同时暴露一处待处理的不自洽**：79% 的代理量质量（4.1364/5.2440）来自 `\|S\|=1`，而 §10 恰禁止在该档改配置；排序不受影响故冻结决定不动，但若要使代理量与 §10 内部自洽，自然定义是只对 `\|S\|>=2` 求和——**候选修正条款，不在本规格内执行** | 待定（依 §5 三分支；无论走哪一支，「代理量与目标函数单位是否一致」这一问都进 Discussion） |

| R567-94DIM-RETEST | 2026-09-04（**运行前登记**，跑完补结论） | 协议 `docs/PROTOCOL_R567_94DIM_RETEST_20260904.md` sha256 `6ab54aea29288e7ec157b75f8d7fb6c450b424c2de659008653e3f3ae15db64c`；**先写后看**：协议与本行同一 commit 落地，先于任何 R5/R6/R7 的 94 维数字产生。上游 = `docs/PROTOCOL_TWO_CHANNEL_20260903.md` sha256 `dc298198e70957dcd4d1b445900ce85e6ae7bbfac60cac4c8c546515594c0e86`、`docs/RSSI特征分析.md`、`docs/MAINLINE_20260903.md` §8。**协议一字不改、`PROCEDURE_BEATS_PRACTICE` 判定不重开** | **重测流程唯一那个生效动作（删 `rssi` 族 8 列）在位置变更与操作抖动两个泛化 regime 上的符号。****为什么现在必须做**：④ 的贡献 97% 来自坐标 1 而坐标 1 只删了 `rssi`，整篇论文的效应量挂在一个动作上；而 `docs/RSSI特征分析.md` 记录 25 维时代（25→20 只删 5 列 `rssi`，源=联合 R2+R3+R4）该动作在泛化 regime 上**有害**——联合内部 0.9025→0.8358（−0.0667）、→R5 0.6745→0.6531（−0.0214）、→R6+R7 0.7550→0.7310（−0.0240）；而 `TWO-CHANNEL-SELF` 的 9 任务与消融/桥接的 11 任务**都不含 R5、也不含 R6+R7**，该反向形态在 94 维下**从未重测**。**运行前已知的反向信号（2026-09-04 记录；只作记录，不改任何判据、门槛或分支定义；协议文件一字未改，sha256 `6ab54aea29288e7ec157b75f8d7fb6c450b424c2de659008653e3f3ae15db64c` 仍成立）**：查冻结产物 `results/g0_environment_grid/summary_metrics.csv`（`feature_count`=94）与 `results/g0_environment_grid_strict59_ra_r5/summary_metrics.csv`（`feature_count`=59），均 rf、`split_mode=time_block`、任务 `g0_iid_R*_time_block`，发现 **R6 的域内可分性在删列后大幅上升，与 25 维记录的方向相反**：R6 `full94` Light_T1 0.5303 / Light_XM 0.4110 / Sensor 0.4828、macro 0.6790 → `strict59_ra` 0.5893 / 0.6783 / 0.7317、macro 0.7655（Light_XM **+0.2673**、Sensor **+0.2489**、Light_T1 +0.0590）；R7 困难簇几乎不动（0.7130 / 0.7107 / 0.8017 → 0.7143 / 0.7333 / 0.8123）；R5 明显下降（0.9533 / 0.9135 / 0.9251 → 0.7313 / 0.8302 / 0.8533）。**与本协议的关系（三条限定，缺一不可）**：①该对照的配置是 `strict59_ra`（59 列，删 35 列，含 `rssi` 但远不止 `rssi`），**不是**本协议的 `drop_rssi`（86 列，只删 `rssi` 族 8 列）——**不是同一个动作**；②该对照是**域内**（同轮次时间块，训练与测试同属该轮次），本协议主判据是**跨域**（源固定 R2+R3+R4 → R5/R6/R7）——**不是同一个量**；③`strict59_ra` 列清单属 `independent/` 独立线产物，本协议已明确不引用它做配置。故该信号**不预判本协议任何单元的符号**，三分支门槛与 `Δ` 定义**均不因它调整**。**记录它的唯一理由**：它提示 `jit_R6` 单元有相当可能落在 `Δ>0`（与 25 维 →R6+R7 的 −0.0240 反向）；事先写下可使事后无论落哪一分支都不构成马后炮（§14 纪律）。**若 `jit_R6` 果然 `Δ>0`**，判定书须并报本条，并明写「删 35 列的域内效应」与「删 8 列的跨域效应」**两者不可互推**，不得用本条充当解释。**另记（背景，影响解读不影响判定）**：R6 `full94` 域内 macro 仅 0.6790、困难簇三类只有 0.41–0.53，是六个轮次中域内最低者 → **R6 本身存在可分性问题而非纯漂移问题**，`jit_R6` 的任何结果都须在此背景下解读（读法与 `CIC-RANGE-ANCHOR` 行的「可分性 vs 漂移」归因分离同一条）。**外部验证不能替代它（本行新增的核实）**：2026-09-04 已核实 UNSW 与 CIC IoT 2022 **都是网关/交换机侧有线抓包**（CIC = 双网卡 Windows 主机 + Wireshark/dumpcap 按 MAC 过滤，非 802.11 monitor 模式；idle/active 仅覆盖 40 台 IP 可达设备，Zigbee/Z-Wave 因不过 IP 网关而缺失），故两者**都不含射频层特征**（`rssi_*`/`subtype_*`/帧类型比例）→ ①外部数据集**无法承载 `rssi` 族**，坐标 1 的干预效力**在结构上无法外部复现**；②能外部检验的是坐标 2（`m_src` 只用源域 OOF，与射频层无关）与机制存在性；③`MAINLINE` §10「没有公开 802.11 数据集只挡住**发现**的复现、挡不住**流程**的复现」**同时适用于 CIC**，而非仅 UNSW。出处 = <https://www.unb.ca/cic/datasets/iotdataset-2022.html> 与 arXiv:2307.08679（Kostas et al., 外部验证 IoTDevID）；**CIC 许可条件即「论文引用其论文」（2026-09-04 用户确认许可已获得），与此处事实引用为同一条，须落到正文参考文献**；抓包位置核实由联网检索完成，**在本机执行、未在服务器联网、未用任何代理**。**任务**：源域固定 R2+R3+R4（`\|S\|=3`，与 25 维时代同口径）；缓存已含 R2–R7（R2 1816 / R3 1837 / R4 1853 / R5 1816 / R6 1988 / R7 1993 窗口）**不需重抽特征**。四个**主判据单元** `pos_R5`（→R5）、`jit_R6`、`jit_R7`、`jit_R6_R7`（双目标轮次，与 25 维同口径）；三个 `loro_*`（`\|S\|=2`，纯跨轮次同位置，供 `w` 权衡正向项）；一个 `joint_tb`（H 内联合时间块，天花板代价参照，沿用 `HISTINT-OBJ-SIGNAL` 的 `JOINT` 定义不另立）。**配置只有两个**：`full94`(94) 与 `drop_rssi`(86, `derive_families` 导出的 `rssi` 族 8 列)——`strict59_ra` **不进本协议**（列清单属 `independent/` 独立线产物，跨线引用需另立协议；且本协议要测流程**实际做过**的那一个动作）。模型 rf/xgboost/lightgbm/stacking 四个全跑（stacking 用于并报 `gap_vs_best_base`）；种子 **42–46**，报均值与样本标准差（§14；25 维那三个数字为单次，本协议**只比符号不比显著性**）。**主判据（运行前定死）**：`Δ best_base = best_base(drop_rssi) − best_base(full94)`，`best_base` = 逐单元取三个非 stacking 模型 macro-F1 最大值再按种子取均值。四单元 `Δ<0` → `HARMFUL_IN_GENERALIZATION_REGIMES`（25 维结论复现 → **层一主张必须限定为「跨轮次同位置」**，`w` 权衡用 94 维数字重算并进正文）；四单元 `Δ>0` → `BENEFICIAL_EVERYWHERE`（**必须如实报出与 25 维不一致**并给出解释尝试，不得只写有利一侧）；混合 → `REGIME_DEPENDENT`（逐单元如实报，限定按**逐单元符号**写，**不取平均掩盖**）。**任何分支都不改动 `TWO-CHANNEL-SELF` 判定**（那 9 个任务全在 `{R2,R3,R4}` 内，本协议不触碰）。**并报（无论方向，不得省略）**：逐单元/配置/模型/种子 macro-F1；`gap_vs_best_base` 两配置变化（删列是否也改变元层损伤）；`joint_tb` 天花板代价；逐类 F1 特别是 `Socket`（历史 96/110 条 F1=1.000，免疫性是否在 R5/R6/R7 保持）；与 25 维三数字的**符号**对照表。**`w` 权衡 94 维重算（并报不设门槛）**：25 维给 `w×0.0943+(1−w)×(−0.0227)>0 → w>0.194`；本协议正向项 = 三个 `loro_*` 的 `Δ` 均值、负向项 = 四个主判据单元的 `Δ` 均值，**两项都用本协议实测值不与 25 维混用**；若负向项为正则该权衡不存在，记 `NO_TRADEOFF`。**六道硬门**：复用冻结实现（`import run_two_channel` 调 `Data`/`derive_families`/`make_model`/`fit_eval`/`time_blocks`，`feature_columns`/`clean_x`/`build_model`/`fit_label_encoder` 经其转引一律不重实现）/ **新增代码只有两处且已声明**：(a) 多目标轮次切片（`round.isin([...])`，因冻结 `Data.slice` 只支持单目标轮次）、(b) `joint_tb`（沿用 `HISTINT` 的 `JOINT` 实现）/ 种子切换经重绑定 `run_two_channel.SEED`（`make_model` 读该模块全局），**不改冻结文件** / 线程三变量=1 且 `n_jobs=1` / 双跑 md5 逐字节一致（除 `provenance.json` 的 `_volatile` 段）/ 解释器 `~/anaconda3/envs/iotcls/bin/python` 禁网禁代理 | 42, 43, 44, 45, 46 | `results/robust_v2/raw_all/features_raw_all_w10.csv`（11303×102，同 `TWO-CHANNEL-SELF`；已核实含 R2–R7 六轮） | `results/r567_94dim_retest_20260904/`（`per_unit.csv` / `deltas.csv` / `tradeoff.json` / `passline.json` / `provenance.json` / `VERDICT.md`） | 待运行。**预估成本**：2 配置 × 8 单元 × 4 模型 × 5 种子 = 320 次拟合，按 `TWO-CHANNEL-SELF` 实测（396 次 / 21 min ≈ 3.2 s/次，`joint` 单元约 3×）折算约 **22 min/单跑、45 min/双跑** | **是**（适用范围限定与 `w` 权衡进正文；**无论符号方向都写**——本实验的价值恰在于它可能否掉层一主张的普适性） |

| CIC-RANGE-ANCHOR | 2026-09-04（**运行前登记**；锚定阶段，全部测量只在历史池 H 内，不触碰留出池 T） | 协议 `docs/PROTOCOL_CIC_RANGE_ANCHOR_20260904.md` sha256 `5b6b142893e340ef1a3f331c959c6378a8223ebc3638bb07341aa2e5b1bd4862`；**先写后看**：协议与本行同一 commit 落地，先于任何 CIC 模型数字产生。口径逐字对齐 `UNSW-IID-REF` 行；两段制结构照搬 `docs/PROTOCOL_TWO_CHANNEL_UNSW_20260903.md`。**不检验流程、不产生任何流程判定** | **量出 CIC 2-Idle 的射程（设备身份粒度跨天落差），以决定是否值得在 CIC 上搬整套两通道流程。****为什么必须先量**：UNSW 射程实测仅 **+0.032778**（`UNSW-IID-REF`，6 配对 6/6 为正）而自采判据 2 门槛是 +0.05 → 该门槛在 UNSW 上结构不可达、只能改无量纲形式；CIC 被寄望「量级要去这里找」（`MAINLINE` §10，依据 40 台设备含 **11 台同生态插座** 7 Gosund + 2 Teckin + 2 Yutron，可分性最低端）**但其射程从未被测过**。射程是上限：坐标 1 最好也只能收回它。**不先量射程就投入全量流程 = 把大成本压在未知量上。****明确非目的**：不删任何族、不换任何模型、不做配置比较、不评估任何判据。**数据与抽取**：`dataset/cic2022/extracted/2-Idle/YYYY_MM_DD_Idle.pcap`，30 天每天一个全网 pcap，合计 **6.5 GB**、均值 0.22 GB/天，8 小时通宵采集无人交互；落盘清单 `dataset/cic2022/MANIFEST.md`（2026-09-03，md5 `44f8cb59…` 与 zstd 帧已逐字节核验）。**抽取必须复用主线通用抽取器 `code/scripts/core/extract_features_generic.py`**（UNSW 20 天即用它抽），`--pcap-dir/--mac-map/--output/--days`，`w=10s`，**特征定义一字不改**；仅允许改文件名匹配以适配 `YYYY_MM_DD_Idle.pcap` 并在判定书原文记录。`device_mac_map.csv` 由 `dataset/cic2022/raw/Device List.xlsx` **机械生成**（MAC→设备名，不做人工归并），生成脚本与产物 md5 落盘。CIC 为全网单文件，设备切分靠 MAC，与 CIC 官方按 MAC 过滤一致。**切分点运行前定死并先于任何数字落盘**：按文件名日期升序，**H = 前 20 天、T = 后 10 天**；`T` 本协议内**不被读取**（留给后续流程验证，只读一次）——理由：CIC 是目前唯一可能给出「留出环境上流程有不有效」的数据集（自采 3 轮留一后只剩 `\|S\|=1`、坐标 2 结构性失效；UNSW 与 CIC 均网关侧抓包故都不含射频层特征、坐标 1 干预效力无法外部复现，见 `PROTOCOL_R567_94DIM_RETEST_20260904.md` §1），**留出池一旦被看过不可复原**。**射程测量（只在 H 内）**：标签粒度 = **设备身份**（运行前定死为主粒度；理由已实测——`UNSW-IID-REF` 发现 D12 在类别粒度 6 类上 OOD−IID = **−0.0131**（跨天反而更好），换设备身份才得 +0.0328，**符号翻转纯由标签粒度驱动**；CIC 的 7 台 Gosund 若被池成一类会同样被稀释）。口径逐字对齐 `UNSW-IID-REF`：每设备每天 ≥100 窗、取 H 内全部日的设备交集；`sample_balanced(max_rows=20000)`；同日 IID = 设备内按 `(window_start_epoch, window_id)` 升序前 70% 训练 / 后 30% 测试；H 内跨天 = 训练某日全部、测试另一日全部，同设备集同门槛同平衡同模型同种子；模型 **rf** 单一；配对 = 同一测试日的（同日 IID − 跨天）之差。**射程 = H 内全部配对该差值的均值。****主判据（运行前定死，门槛不因结果调整）**：`>= 0.06` → `RANGE_SUFFICIENT`（有真射程，值得搬整套流程，判据 2 可用绝对门槛）；`0.03 =< 射程 < 0.06` → `RANGE_LIKE_UNSW`（与 UNSW 同档，全量投入须另行论证，判据 2 必须无量纲）；`< 0.03` → `RANGE_INSUFFICIENT`（不作干预验证场地，只作机制存在性对照，如实写入 Discussion）。**门槛依据（选定于任何 CIC 模型数字之前）**：0.06 = 自采判据 2 门槛 +0.05 加余量，射程低于它则该门槛结构不可达（与 UNSW 处境同构）；0.03 = UNSW 实测 0.0328。**并报（无论方向不得省略）**：逐设备落差特别是 11 台同生态插座（自采对照形态是 `Socket` 免疫、`Light_XM→Light_T1` 对单向）；**可分性与漂移的归因分离（运行前定死的读法）**——同日 IID F1 就低 = 可分性问题（不是本框架的靶）、同日高跨天才低 = 漂移问题（是本框架的靶），设此条因 **UNSW 迁移效率天然高有两个无法用现有证据分开的解释**：①不含反转族（本框架机制）②其类为异质设备（打印机/摄像头/音箱/插座）本来就好分，而 CIC 的 11 台同生态插座正是 UNSW 缺的困难档，**若 CIC 射程大，原因可能是同生态设备本来就分不开而非有反转族**，故须并报 **`射程_漂移` = 只计同日 IID F1 >= 0.80 的设备的落差**（门槛 0.80 选定于任何 CIC 数字之前，依据 = UNSW 逐设备同日普遍 0.87 以上、自采困难簇跨轮次约 0.6，0.80 居中），§5 三分支**以全设备射程判定**、`射程_漂移` 只作归因不改判定；分离天数 vs 落差（UNSW 上实测**不预测**——7 天 +0.0520 最大、19 天 +0.0242 最小，CIC 是否同形）；类别粒度对照（`Device List.xlsx` 含类型列则并报，不含则记 `CATEGORY_MAP_UNAVAILABLE` 且不臆造映射）；逐日同日数字——**但明确声明同日数字不作天花板或 oracle**（`strict59` 已证同环境天花板随删列一起下降 0.9622→0.8682，该纪律照搬）；逐日窗口数、设备数、被门槛剔除的设备清单。**六道硬门**：`T`（后 10 天）本协议内不被读取，实现以路径白名单硬性拦截、违反即中止 / `split.json` 与 `device_mac_map.csv` 的 md5 先于任何模型数字落盘 / 元数据列不入特征且**逐项核对 `window_start_epoch` 不在特征中**（UNSW 已记录的坑：含它与 `day` 共线 → 同日虚高、跨天崩坏）/ 抽取复用 `extract_features_generic.py` 特征定义一字不改 / 双跑 md5 逐字节一致（除 `provenance.json` 的 `_volatile` 段）且线程三变量=1、`n_jobs=1` / 解释器与 UNSW 抽取一致且**服务器全程禁网禁代理**（数据已在本地无需联网）。**许可与引用义务**：CIC 许可**已获得（2026-09-04 用户确认）**，条件为**在论文中引用其论文**，须落到正文参考文献 —— <https://cicresearch.ca/IOTDataset/CIC_IOT_Dataset2022/>（官方说明 <https://www.unb.ca/cic/datasets/iotdataset-2022.html>）；数据下载由用户在 Mac 侧完成，**服务器侧全程未配置未使用任何代理**（`MANIFEST.md` §1 已记录） | 42 | `dataset/cic2022/extracted/2-Idle/`（30 天 pcap，6.5 GB）+ `dataset/cic2022/raw/Device List.xlsx`（MAC 映射来源）；特征缓存另置 `results/cic_features_idle/`（30 天全量；**`T` 的特征文件本协议内不被读取**） | `results/cic_range_anchor_20260904/`（`split.json` 先落盘 / `inventory.csv` / `pairs.csv` / `per_device.csv` / `separation.csv` / `passline.json` / `provenance.json` / `VERDICT.md`） | 待运行。**预估成本**：抽取 6.5 GB / 30 天照 UNSW 的 8 路并行预计 **1–3 h**；测量部分 rf 单模型、H 内 20 天配对预计 **< 1 h**。若抽取超 6 h，按运行前定死的顺序启用削减 **R1**：H 由 20 天降为**前 12 天**（T 不变仍为后 10 天，中间 8 天弃用并记录），**不得临时另想办法** | **是**（射程数字与归因分离进正文；无论落哪一分支都写——`RANGE_INSUFFICIENT` 分支下须在 Discussion 明写「两个公共数据集都不具备可供干预的射程」这一负结果） |
| UNSW-BELKIN-CAPACITY | 2026-09-04（**运行前登记**，跑完补结论） | 协议 `docs/PROTOCOL_UNSW_BELKIN_CAPACITY_20260904.md` sha256 `dc58f07725f988c2dca962f3b33e33cd675d84563f6380d097877300b80db630`；**先写后看**：协议与本行同一 commit 落地，先于任何二类 Belkin 数字产生。上游 = `docs/PROTOCOL_UNSW_META_MISMATCH_20260902.md`（簇定义来源）、`results/unsw_meta_mismatch_20260902/cluster_definition.json`、`docs/MAINLINE_20260903.md` §3 模式一 / §5。**不改 `UNSW-META-MISMATCH` 的分支 C 判定** | **分开两种可能，现有产物分不开**：UNSW 困难簇（`BelkinWemoMotion`↔`BelkinWemoSwitch`，τ=0.1 下全数据集**唯一一条边**，`f_ab`=0.361 / `f_ba`=0.446）域内就在随机线附近（`src_oof_H_rf` 0.4585/0.5556/0.5167、`target_H_rf` 0.4750–0.5600、逐类域内 F1 0.5250–0.5930，二类随机 0.50）——**(A) 信息在这 61 列里、0.53 是多类干扰造成的（建模问题）** 还是 **(B) 信息不在这 61 列里（表示容量问题、模式一饱和）**。**为什么值得做**：①它是「抬天花板」这条线唯一**无漂移污染**的试验台（Belkin 对域内域外都是 0.5，`regime_shift` 仅 0.0417–0.1015），自采上可分性与漂移纠缠；②若落 (B)，`MAINLINE` §5 那条事后解读（「UNSW 是模式一饱和的端点」）**从推测升为测量**；③它是 §3 三种失效模式中**模式一**唯一的外部数据点。**对照锚**：自采困难簇即使删到 59 列（`strict59_ra`，删 35 列、含 `rssi` 族 8 列）域内仍有 0.73–0.88（`g0_environment_grid_strict59_ra_r5`，rf、同轮次时间块、R2–R5 的 Light_T1/Light_XM/Sensor）。**数据与切分**：复用 `results/unsw_features_full/features_day_*.csv`（**不重抽**）；**只用 H = 前 14 天**，切分点与 `docs/PROTOCOL_TWO_CHANNEL_UNSW_20260903.md` 逐字一致，**后 6 天（T）本协议内不被读取**（留出池只读一次，不因诊断性测量烧掉），实现以路径白名单硬拦、违反即中止；门槛与平衡逐字对齐 `UNSW-IID-REF`（每设备每天 ≥100 窗、`sample_balanced(max_rows=20000)`）；域内划分 = 设备内按 `(window_start_epoch, window_id)` 升序前 70%/后 30%（**时间块非随机**）。**三个单元**：**A（主判据）** `BelkinWemoMotion` vs `BelkinWemoSwitch`，UNSW 61 列，H 的 14 天逐日域内；**B（有效性正控）** `Dropcam` vs `SamsungSmartCam` 同口径（两台不同厂商摄像头域内多类 F1 已知 0.9271–0.9997，二类应更高）；**C（参照，只报不设门槛）** 自采 `Light_T1` vs `Light_XM`，`full94`，R2/R3/R4 同轮次时间块——把「自采困难簇域内可分」换成与 A 同形的二类口径使对照同量纲，因跨数据集**不构成受控对比**。模型 `rf`/`xgboost`/`lightgbm` 三个基模型（**不含 stacking**，容量问题与元层无关）。**主判据（运行前定死，门槛不因结果调整）**：主量 = 单元 A 二类 macro-F1，逐日逐种子逐模型算出后先按种子均值、再按天均值、最后取三模型**最大值**（与 `best_base` 口径同构）。`>= 0.80` → `SEPARABLE_IN_BINARY`（信息**在**这 61 列里 → 落 (A)；**`MAINLINE` §5「模式一饱和」那句必须撤回并改写**，多类 0.53 归因于多类干扰）；`0.60 =< F1 < 0.80` → `PARTIALLY_SEPARABLE`；`< 0.60` → `NOT_SEPARABLE_IN_61`（信息**不在** → 落 (B)，「模式一饱和」由推测升为测量，回 pcap 另立协议有据）。**门槛依据（选定于任何本协议数字之前）**：0.60 = 二类随机 0.50 与 Belkin 现有多类逐类 F1 上端 0.5930 之上；0.80 = 与 `docs/PROTOCOL_CIC_RANGE_ANCHOR_20260904.md` 的「可分性 vs 漂移」归因门槛**同一个数**，跨协议不另设。**并报（无论方向不得省略）**：①**逐列单变量 AUC**（61 列各自对单元 A 打分，H 内逐日算、按天均值），报 max、top-10、按 UNSW 7 族机械聚合（`len`14 `interarrival`12 `subwin`9 `burst`7 `up`7 `down`6 `singletons`6 = 61）的族内 max，**只报不设门槛**（单列 AUC 的先验门槛无法诚实论证）；**读法运行前写定**——若 max 单列 AUC < 0.60 而单元 A F1 >= 0.80 则信息是**多变量**的，两者都低则信息**不存在于这 61 列**；②三单元逐日/逐模型/逐种子 macro-F1 与逐类 F1、混淆矩阵；③单元 A 的**跨天**对照（H 内逐配对同口径，用于确认 Belkin 对是可分性问题而非漂移问题，只报不设门槛）；④H 内 14 天逐日两设备窗口数与被门槛剔除的天；⑤A 与 C 的差值并**明写这是跨数据集比较、不构成受控对比**。**六道硬门**：T 不被读取（路径白名单硬拦）/ **`window_start_epoch` 不入特征并逐项核对落盘**（UNSW 已记录的坑：与 `day` 共线 → 同日虚高跨天崩坏）、8 个元数据列一律不入特征 / **单元 B 正控 `>= 0.90`，否则 `INVALID_RUN_STOP` 不写任何结论**（否则单元 A 的低分无法与实现缺陷区分）/ 特征缓存复用不重抽且 md5 先于任何模型数字落盘 / 双跑 md5 逐字节一致（除 `provenance.json` 的 `_volatile` 段）、线程三变量=1 且 `n_jobs=1`、**双跑之间不得提交任何 commit**（`HISTINT-OBJ-SIGNAL` 已记该教训：`git_head` 会使 provenance 不一致）/ 解释器 `~/anaconda3/envs/iotcls/bin/python` 禁网禁代理不装依赖。**明确非目的**：不删族、不做配置比较、不搬两通道流程、不评估任何流程判据；**本协议不涉及 61 列之外的任何特征**——若落 (B)，回 pcap 找新特征须**另立协议**（其设计依赖本协议逐列结果，现在无法预注册） **下游定位（2026-09-04 补记；只补定位，不改协议文件、不改主判据、不改三分支门槛；协议 sha256 `dc58f077…` 仍成立）**：本行原先只写了两分支的处置，未写它是另一条线的**前置**。补记如下——本协议是**「候选验收程序」外部验证的前置测量**。**该线的由来**：坐标 1 与坐标 2 都是**减法**（删族 8 列 / 不堆叠退回最佳基模型），**都无法增加信息**，故整套机制的天花板就是给定特征集上的最佳基模型；UNSW 上坐标 2 无病可治（`gap_H`=0.0099）、坐标 1 无 `rssi` 可删，所以「机制在 UNSW 上作用不大」是**无病可治**而非药效弱。若把方法从「一条删掉过 `rssi` 的规则」重述为**「任何候选特征的验收程序」**（候选 → 测「强度 × 反转」→ 接受/拒绝；删族只是候选集恰为已有族、动作恰为删除的特例），则**外部验证不再需要 `rssi` 本身存在**，只需要**某一个强且反转的族**——而候选可在 UNSW 上自行构造。**这一点直接影响 `R567-94DIM-RETEST` 行已写下的判断**：那里写「外部数据集无法承载 `rssi` 族 → 坐标 1 的干预效力**在结构上无法外部复现**」，该句对 `rssi` **本身**成立，但在候选验收的重述下「结构上不可能」变为**可做，代价是多一个自由度（选哪些候选）**，故候选集必须运行前定死，否则即事后挑选。**本协议三分支的下游读法（随此定位一并写定，不改判据）**：落 `NOT_SEPARABLE_IN_61` → 61 列确实不含区分信息 → **加候选有据**，进入验收程序的外部验证；落 `SEPARABLE_IN_BINARY` → 信息在、0.53 系多类干扰 → **不需要新特征**，且须撤回 `MAINLINE` §5 的「模式一饱和」表述；落 `PARTIALLY_SEPARABLE` → 由逐列 AUC 结果决定后续，本行不预先指定。**两处必须先记下的张力（记录，不代为决定）**：①**「找到区分特征」与「验证方法」可能互相拆台**——Wemo Motion 与 Wemo Switch 同固件同云，真正区分它们的很可能是应用层语义（UPnP `deviceType` 之类），这类量**既极强又极稳定**，验收门会直接放行、无戏可看，于是准确率上去了而 **UNSW 的困难簇被消解掉、对机制故事的贡献反而更少**；对验收门真正有用的候选是**绑定部署环境而非设备身份**的量（DNS 解析器身份、NTP 服务器选择、DHCP 续租节奏、临时端口范围、TLS 会话复用率等，20 天跨度给它们反转的机会）——**但「应用层里存在这类候选」目前是推测不是测量**，须实测。②**范围后果**：现有 94/61 列**全为流量形状统计**（本行核实：UNSW 61 列 = `len`/`interarrival`/`burst`/`up`/`down`/`subwin` 及 `packet_count`/`byte_count` 等，**零个协议层或应用层特征**——无端口、无协议类型、无 DNS/TLS、无 UPnP），这是一个自洽的加密友好设定；一旦加入端口/DNS/UPnP，方法即依赖明文协议语义，**对加密流量与未见设备的适用性会被直接质疑**，且会被拿去与走应用层的 IoTDevID 一线直接比较。**此为论文定位决定，须由用户裁定后才可进入第 2 步。** | 42, 43, 44, 45, 46 | `results/unsw_features_full/features_day_*.csv`（20 天，61 特征 + 8 元数据；**只读前 14 天，后 6 天不被读取**） + `results/robust_v2/raw_all/features_raw_all_w10.csv`（单元 C，11303×102） | `results/unsw_belkin_capacity_20260904/`（`split.json` 先落盘 / `inventory.csv` / `unit_scores.csv` / `per_column_auc.csv` / `family_auc.csv` / `cross_day.csv` / `confusion/` / `passline.json` / `provenance.json` / `VERDICT.md`） | **判定 `PARTIALLY_SEPARABLE`**（单元 A = 0.6504，落 0.60–0.80）。重跑双跑 9 产物 md5 逐字节一致、provenance 一致；六道硬门全过（硬门 2/4 的证据先于任何模型数字落入 `split.json`：14 天缓存 md5 + 8 元数据列排除逐项核对）。**首跑作废如实记**：14:12 那次双跑虽 md5 一致、正控通过，但违反 §7 硬门 4（缓存 md5 未先落盘）、硬门 2 核对未落盘、§8 产物命名不符，按「任一失败即停,不写结论」作废并从 `results/` 移除；**其数字已被作者看到**（与重跑一致，同一计算）。正控 B = 0.9996（≥0.90 通过）；参照 C（自采 Light_T1 vs XM，full94）= 0.9283；逐模型 A：xgboost 0.6504 / rf 0.6498 / lightgbm 0.6483。**逐列 AUC（§6.1）**：最强单列 `down_len_mean` AUC 0.6168、强度 0.1168；族内最强 `down` 0.1168 / `up` 0.1123 / `len` 0.0337 / `subwin` 0.0310 / `singletons` 0.0310 / `burst` 0.0282 / `interarrival` 0.0220 —— **方向族比其余五族强 3–5 倍且唯一超过 0.10**，top-10 前 7 个全是方向列。与自采方向规则修复后困难簇强度 ≤0.10 同量级。**§6.3 跨天**：最大 0.5785，域内 − 跨天 = +0.0719；对比自采 `loro_to_R3` 跨轮次 0.6148 vs 域内 0.8896（差 0.275）→ **Belkin 漂移成分约为自采 1/4**，支持「可分性问题而非漂移问题」，但**漂移成分不为零**。A − C = −0.2779（跨数据集，不构成受控对比）。**§5 读法缺口如实记**：max 单列 AUC 0.6168（刚过 0.60）、F1 0.6504（远低 0.80），预注册两分支都不精确适用；主判据分支处置本已覆盖，不挑分支。**偏离**：§6.3 用单种子 42 控制成本（无门槛并报项）。**预估成本**：单元 A/B 各 14 天 × 3 模型 × 5 种子 = 210 次二类拟合（每次约 2 万行 × 61 列），单元 C 3 轮次 × 3 模型 × 5 种子 = 45 次，逐列 AUC 为 61×14 次单变量排序可忽略；按 `UNSW-IID-REF` 实测（13 类、1.7 万行、1.2 s/次）折算二类更快，预计 **< 15 min/单跑、< 30 min/双跑** | **是**（三分支任一都进正文：落 `NOT_SEPARABLE_IN_61` 则作模式一的外部测量与「同型号/同生态设备在流量形状上不可分」这一限制的依据；落 `SEPARABLE_IN_BINARY` 则**须撤回 `MAINLINE` §5 的「模式一饱和」表述**并如实说明事后解读被自己的后续测量否掉——**否定自己的那一支同样必须写**） **另**：本行同时是「候选特征验收程序」线的前置（见配置列**下游定位**）；其中「坐标 1 的干预效力在结构上无法外部复现」这一既有表述的修正（改为「可做，但须自造候选且候选集运行前定死」）**须进正文**，不得沿用旧表述 |
| PIPELINE-DROPRSSI-E2E | 2026-09-04（**运行前登记**，跑完补结论） | 协议 `docs/PROTOCOL_PIPELINE_DROPRSSI_20260904.md` sha256 `6e85b2fb283667b20dfa9e718311dbfb686868afe448d5c6381857a003b6abac`；**先写后看**：协议与本行同一 commit，早于任何数值运行。runner 复用 `results/r567_94dim_retest_20260904/run_r567.py` 的 `eval_unit`（仅扩展为返回逐窗口预测），R567 的 GATE7 逐位断言保留且必须通过 | 2 配置（`full94` 94 列 / `drop_rssi` 86 列，族由 `TC.derive_families` 导出）× 6 单元 × 4 模型 = 240 次拟合。6 单元 = 3 inner（R3+R4→R2、R2+R4→R3、R2+R3→R4，目标在源域内、标签对流程合法可见）+ 3 outer（R2+R3+R4→R5/R6/R7，目标从未参与任何决策，硬门 7 以 assert 保证决策函数不接收 outer 结果）。**动机**：9-04 的端到端数字（设备级 5 min 0.947–0.957）那一臂是 `strict59_ra`（删 35 列+方向修复、独立线、无登记行），不是流程登记的坐标 1 动作 `drop_rssi`；且「是否堆叠」是看过目标后选的。本协议同时闭合这两个洞 | 42–46（5 个） | `results/robust_v2/raw_all/features_raw_all_w10.csv` md5 `703984b6ad2fde2f45e0cce1c6df31be` | `results/pipeline_droprssi_20260904/`：`per_unit.csv`（窗口级 + N=1/6/30 设备级）、`decision.json`、`latency_curve.csv`、`device_margin.csv`、`unsw_arm.json`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定 `SOURCE_SIGNAL_PARTIAL`**（一致 4/6 格；TIE 2 格，剔除 TIE 后 3/4）。双跑 7 产物 md5 逐字节一致、provenance 去 `_volatile` 后一致、无 `git_head` 偏离。源域侧决策：`full94` inner g 均值 +0.1697 → REFUSE_STACKING（outer 2/3，`pos_R5` 判错代价 0.0244）；`drop_rssi` +0.0043 → REFUSE_STACKING（outer 2/3，`jit_R6` 判错代价 0.0039，**在打平带内**）。**判错代价被实测 \|g_out\| 界住**。端到端（`drop_rssi` + 源域侧规则选出 best_base，R5/R6/R7 从未参与决策）：10 s 0.7710 / 1 min 0.9161 / **5 min 0.9776**；对照 `full94+stacking` 0.7180/0.7962/0.8186、`full94+best_base` 0.7660/0.8919/0.9100。**坐标 1 在设备级的净值全部集中在 `pos_R5`**（0.9327 vs 0.7299 = +0.203），`jit_R6`/`jit_R7` 上 full94+bb 已饱和到 1.0000。**口径洞已补**：此前 0.947–0.957 那一臂是 `strict59_ra`（独立线、无登记行），本行是登记的 `drop_rssi`。**偏离两处**：①§4 的「full94+源域 CV 选出的模型」未重算（需未登记的 P0 机制），改以 stacking/best_base 两臂夹逼；②预注册的 PARTIAL 处置写「终点降级为配置上界」，该措辞不准 —— 终点是流程真实产出（选 best_base 由源域侧规则定、未看目标），PARTIAL 真正说的是规则可靠性 4/6。**按预注册处置执行，措辞缺陷写入偏离段，不事后改判据。** | 主判据 = 源域侧堆叠决策与目标侧最优的一致格数（2 配置 × 3 outer = 6 格；打平带 \|g_out\| < 0.01 记 TIE 但仍计入分母）。`SOURCE_SIGNAL_AGREES`（≥5/6）→ 步骤③可由源域独立判定，端到端数字可称**流程输出**；`SOURCE_SIGNAL_PARTIAL`（3–4/6）→ 逐格如实报，终点降级为**配置上界**；`SOURCE_SIGNAL_FAILS`（≤2/6）→ 流水线缺一环，端到端数字**只能作上界**，不得称流程输出。准确率-延迟曲线（10 s / 1 min / 5 min）、众数裕度、逐类 F1 **无论方向一律并报**。UNSW 外部臂（零新拟合，从 `subset_f1.csv` 算同样的方向一致性）只作旁证，不进 6 格计数 |
| HIER-CLASSPAIR | 2026-09-04（**运行前登记**，跑完补结论；确认性实验，**协议 §1 已披露先跑过一次单种子探索性探针**） | 协议 `docs/PROTOCOL_HIER_CLASSPAIR_20260904.md` sha256 `ebb2eb642a322337012e5a5a6a6ac62df7fb77054b7a7f01936cf76191738297`；**先写后看**：协议与本行同一 commit，早于本协议的任何数值运行。复用 `TC.Data/derive_families/make_model/clean_x` 与 `diag_grid_override.py` 的 `cluster_from_cm` | 3 臂（`flat_full94` / `flat_drop_rssi` / `hier`）× 6 单元 × 5 种子 ≈ 480 次拟合。`hier` = 第一层「易类各自 + CLUSTER」在 `full94`（含 rssi）、第二层簇内三分类在 `drop_rssi`，硬路由。簇成员由**源域程序性导出**（三 inner 单元混淆矩阵聚合 → `cluster_from_cm`，τ=0.1，与 UNSW 冻结同值）；两层模型对由**源域侧规则**在三 inner 单元上选出。硬门 3 以 assert 保证 outer（R5/R6/R7）不进任何决策。**动机**：`R567` 逐类分解显示删 rssi 在八单元上全部损伤 Camera（−0.024～−0.349）、收益全部来自困难簇、Socket 恒不变 → 反转是类对级而非族级（`diag_coord1.csv` 本就逐 `(task,pair,family)` 记录，验收时把类对那一维 `d_auc_sum` 加和掉了）。`stacking` 不进本协议（属 `PIPELINE-DROPRSSI-E2E` 射程） | 42–46（5 个） | `results/robust_v2/raw_all/features_raw_all_w10.csv` md5 `703984b6ad2fde2f45e0cce1c6df31be` | `results/hier_classpair_20260904/`：`per_unit.csv`（窗口级 + N=1/6/30 设备级 + 逐类）、`cluster_derivation.json`、`model_selection.csv`、`device_margin.csv`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定 `HIER_FAILS`**（条件1 3/6、条件2 1/3）。双跑 6 产物 md5 逐字节一致、provenance 一致。**§3 程序性簇导出成功**：导出 `{Light_T1, Light_XM, Sensor}` = 预期，**跨三模型一致、跨五种子一致** —— 「零数据集专用分支」的首个真实检验通过，规格中「已知唯一手改点」由此消除。**主判据失败形态**：Camera 三单元全部退化（`pos_R5` 0.8369 vs 0.8709、`jit_R6` **0.4713** vs 0.8131、`jit_R7` 0.8394 vs 0.9402）；Socket 3/3 保持 1.0000；条件 2 仅 `pos_R5` 通过（0.7162 vs 0.6808）。**失败原因可定位,且是运行前预警过的那一处**：§4 源域侧规则按 inner macro 均值选出 `rf + xgboost`（0.8452），而 `xgboost + xgboost` 仅差 0.0048 排第三 —— **inner 上 9 个候选前四名相差 0.005，outer 上 Camera 相差 0.42**。macro 把 Camera 平均掉了,正是规格 §2.8 所述之病发生在**选模型**这一步（原则未管到该步,系设计遗漏）。**探针增益主要来自事后挑模型**：hier outer 三单元逐窗口 macro 均值 0.7704 vs `PIPELINE-DROPRSSI-E2E` 同口径 0.7710 = **−0.0006（精确为零）**；作者运行前写下的预期 +0.03~+0.06 **未兑现,如实记**。**唯一正向**：`pos_R5` 上 hier macro 0.7971 vs V1 空间 oracle 0.6959 = **+0.101**（超出旧配置空间），`jit_R6`/`jit_R7` 反转 —— 又是 regime 依赖,与 `R567` 同形。**处置**：方法规格步骤 4 由「猜想,未确认前不得写入方法」改为**已否定,不进方法本体**；方法本体仍为步骤 1–3 + 5，端到端 0.7710/0.9776 不变。救援路径两条（**须另立协议,不得改本协议判据**）：①§4 改用 inner 易类 F1 而非 macro 选第一层模型；②软路由 `P(类)=P(簇)×P(类\|簇)`。**偏离**：§3/§4 循环依赖（§3 要用 §4 选出的模型,§4 要用 §5 的分层实现），解环为「簇导出改用三 inner 单元 flat full94 预测,与 §4 解耦；三基模型要求一致,不一致取交集」,写入 runner 文件头与 `passline.json`。 | 主判据 = outer 三单元上的**逐类**比较（非 macro），对 `flat_best`（两平铺臂同类较高者）：**条件 1 无退化** Camera/Socket ≥ flat_best − 0.01（6 格）；**条件 2 有提升** CLUSTER 三类 F1 均值 > flat_best 同三类均值（3 格）。`HIER_DOMINATES`（条件1 ≥5/6 且 条件2 =3/3）→ 类组分解可同时保住易类与提升困难簇，可作坐标 3 写入流程，并据此陈述「V1 配置空间（{删哪些族}×{堆不堆叠}，其 oracle 距流程仅 0.0045）已被跳出」；`HIER_TRADES`（条件2 ≥2/3 但条件1 <5/6）→ 提升是**又一次交易**，不得称保住两边；`HIER_FAILS`（条件2 ≤1/3）→ 假设不成立，如实记探针单种子结论未复现。设备级三档曲线、`stage1_acc`、众数裕度、簇导出结果、inner 上 9 组合排名 **无论方向一律并报** |
| UNSW-FAMILY-REVERSAL | 2026-09-04（**运行前登记**，跑完补结论；第一段，第二段的前置） | 协议 `docs/PROTOCOL_UNSW_FAMILY_REVERSAL_20260904.md` sha256 `33b7f5be5638c0be007ff8a9f1adbc03e933cc7d0a65dc8b158fe08a0b3dc5bb`；**先写后看**：协议与本行同一 commit，早于任何数值运行。复用冻结实现 `pair_auc`（`run_two_channel.py:198`）与 `coord1_diag` 定义（同文件 :214），族划分 `derive_families`，门槛与平衡口径沿用 `UNSW-IID-REF` | 只读 `H_inner` = H 前 14 天中的**前 10 天**；`H_outer`（第 11–14 天）与 T（后 6 天）白名单硬性拦截（`H_outer` 留给第二段作打分，本段读了它第二段就不再是「从未参与决策」）。设备 = 10 天全部过 ≥100 窗/天门槛的**交集**，实测 **14 台** → **91 个无序类对**；族 **7 个**（`len`14 `interarrival`12 `subwin`9 `burst`7 `up`7 `down`6 `singletons`6 = 61）。任务 = `H_inner` 内留一天出，**10 个**，目标全在 `H_inner` 内故标签对流程合法可见。**动机**：规格 §1.1 已定「特征池是声明的输入，方法声称的是在任何池上都适用的配置推导程序」，而该主张目前 **n=1** —— 坐标 1 的族级诊断只在自采（位置 A 的池，含 rssi）上跑过；UNSW 侧从未跑过（`check_unsw_families.py` 自陈只做结构检查）。**本协议只答一句：UNSW 的 61 列池里有没有「强且反转」的族。****不答**该池能否分开 Belkin（`UNSW-BELKIN-CAPACITY` 已答 0.6504）—— 容量与稳定性独立：同一 61 列对易类信息充足（设备级 0.999）、对 Belkin 几乎为零 | 42（单种子；`pair_auc` 用 lbfgs 对给定数据确定，冻结实现中 `coord1_diag` 亦只调用一次） | `results/unsw_features_full/features_day_*.csv` 前 10 天（md5 先于任何 AUC 数字落入 `split.json`） | `results/unsw_family_reversal_20260904/`：`split.json`（先落盘）、`family_dauc.csv`、`pair_family_dauc.csv`、`pair_baseline_auc.csv`、`skipped_pairs.csv`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定 `NO_REVERSING_FAMILY`**。双跑 8 产物 md5 逐字节一致、provenance 一致，四道硬门全过（缓存 md5 与元数据排除核对先于任何 AUC 数字落入 `split.json`）。**7 族 `d_auc_sum` 全部为负、正值族 0/7**：`subwin` −0.7831 / `singletons` −1.1896 / `down` −3.2795 / `interarrival` −3.5239 / `up` −3.7289 / `burst` −4.1189 / `len` −11.8153。即**每一族删掉都让跨天 AUC 下降**，无一例外 —— 该池 59–61 列全是「有用且稳定」，无反转族。对照自采：`rssi` **+5.2440**（删掉让跨轮次 AUC 上升）、对次名（`subwin` +0.0156）**336×** —— **同一诊断量、同一冻结实现，在两个池上给出符号相反的读数，且两次都正确**。**【2026-09-04 更正，判定不受影响】**：本行与 `VERDICT.md` 原写「自采参照 `rssi` +0.4140、对次名 22.8×」，取的是 `HISTINT-OBJ-SIGNAL` 的 `S_obj = ΔLORO − 3×ΔJOINT`（基于模型准确率），与本行左侧 UNSW 那列的 `d_auc_sum` **不是同一个量**；同口径的自采值是同一张冻结表里 `冻结 S(F)` 列的 **+5.2440**，对次名 5.2440/0.0156 = **336×**。`run_famrev.py` 的 `SELF_REF` 字典同时混入了 `len: −0.1610`（那是 `ΔLORO`，其 `S_obj` 为 −0.0439）。**判定 `NO_REVERSING_FAMILY` 不受影响**：该判定由 UNSW 自身 7 族正值数 0/7 单独决定，该列协议内明写「跨池比较、不构成受控对比」、不进分支逻辑；3.0× 门槛远低于 22.8 与 336 两个值，任一口径下结论相同。**不重跑**，只改注记列并留本更正。并报基线 AUC：91 类对均值 **0.9725**、最大 0.9998，最小 **0.6653**（`BelkinWemoMotion\|BelkinWemoSwitch`，与 `UNSW-BELKIN-CAPACITY` 的 0.6504 对上，口径不同）→ **90/91 个类对已近乎完美，唯一缺口就是 Belkin 那一对**。明细 6370 行，因样本门槛 40/20 跳过 0 个 (任务,类对)。**运行前预期 `NO_REVERSING_FAMILY` 兑现**（协议 §5 与本行运行前已记）。**三条后果**：①坐标 1 在该池**自动不开火**（`cand` 为空是冻结代码 `run_two_channel.py:321` 的行为、非外加分支）→ **预注册的「不开火」首次在族级独立确认**（此前只有元层聚合的四格分解）；②**干预效应量的外部复现在 UNSW 上已确定不可能** —— 不是没找到，是测出该池无可供干预的病理；③A 层（诊断形式与程序特异性）由 n=1 变为 **n=2**，B 层（干预效应量外部复现）关闭。**第二段按协议 §9 预注册的前置条件不执行**（仅当落 `REVERSING_FAMILY_FOUND` 才执行），省去 4–5 h；且坐标 1 在该池无事可做后，oracle 距离只剩坐标 2 那一轴，而该轴已由 `UNSW-META-MISMATCH` 与 `PIPELINE-DROPRSSI-E2E` 测过 | 主判据 = 逐族 `d_auc_sum`（10 任务求和）的 `top1 / max(top2, 0.05)`。`REVERSING_FAMILY_FOUND`（有族 >0 且比值 ≥3.0）→ 该池含强反转族，坐标 1 在位置 B 有候选，进第二段（oracle 距离）并作干预外部复现的前置；`WEAK_CANDIDATES_ONLY`（有族 >0 但比值 <3.0）→ 正是规格 §5 限制 6 警告的「接近候选」，不进第二段，记「该池选择风险高，须先修选择规则」；`NO_REVERSING_FAMILY`（无族 >0）→ 坐标 1 自动不开火（`cand` 为空是冻结代码行为、非外加分支），**预注册的「不开火」在族级独立确认**，不进第二段。**门槛依据（定于任何本协议数字之前）**：3.0 远低于自采 `rssi` 对次名的 22.8×，故非为迁就预期而设；同时高于 `HIER-CLASSPAIR` 实测失败时的候选比 ≈1.006，故能把该失败形态挡在 `WEAK_CANDIDATES_ONLY`。**运行前预期（只记录、不改判据）**：作者预期 `NO_REVERSING_FAMILY`（自采上这 7 共有族最高仅 0.0182、`len` 为负 −0.1610，而 `rssi` 0.4140；且 UNSW 四格净反转基本为零）—— 但那是元层聚合的间接证据，族级从未测过。并报逐类对 `d_auc`（检验反转是否同样是类对级）、全 61 列基线 AUC 分布、被样本门槛跳过的计数、与自采逐族并列表（**跨池比较，不构成受控对比**）。**成本**：`pair_auc` 单次实测 0.023 s → 每任务 728 次 ≈ 17 s → 10 任务约 3 min 单跑 / 6 min 双跑。第二段成本已实测折算（四模型一次 280 s：rf 10.0 / xgb 16.5 / lgbm 20.1 / stacking 233.4），含 stacking 约 4–5 h 单跑、不含约 50 min —— 该取舍在第二段协议内定 |
| FEATURE-EXPANSION-SELF | 2026-09-04（**运行前登记**，跑完补结论；自采侧第一段，**只测不选**） | 协议 `docs/PROTOCOL_FEATURE_EXPANSION_20260904.md` sha256 `61984e4722eb635ec5ae3ccff51e98fe91b24b9df2cfc9a24bb7f344fdcc08c0`。**修订记录（合规性关键，三版全记以备审）**：v1 `56ed0b20…`（33 列/3 族/单配置）→ v2 `0a651c0a…`（89 列/5 族/三配置，主判据在未筛选配置）→ v3 `61984e47…`（**共同主判据 A/B，互不替代，禁止失败后再挑**）。**三次修订均发生在任何本协议数字存在之前**，故仍属「先写后看」。v1→v2 因用户提出「为什么不做两个版本」；v2→v3 因用户提出以 `N33` 为主判据（稀释顾虑成立），而其附带的「失败了再从全量挑」被识别为选择性报告，遂改为共同主判据并显式禁止事后挑选。复用冻结实现 `run_tshark` / 窗口切分 / `pair_auc` / `coord1_diag` 定义 / `joint_eval`，一律不重实现。tshark 3.2.3（与冻结提取器同版本） | 新增 **89 列 / 5 族**；三个配置：`full94` 94 列（**逐字节不变**，本协议内同 runner 重跑一次以同估计量）、`full94+N33` 127 列、`full94+N89` **183 列 / 15 族**。轮次 R2–R7 全抽，89 列**只抽一次**（`N33 ⊂ N89`）。**候选清单协议 §2 逐列公式定死，运行中不得增删**：`dir_*`（逐方向间隔与包长 p10/p25/p50/p75/p90、请求-响应时延、方向交替与 run-length、上下行突发 count/mean/max；**方向用修正规则 `wlan.ra == BSSID`，与 `full94` 原有 `up_*`/`down_*` 的 `DA == BSSID` 并存 → 两族诊断值对比直接量出方向 bug 的代价**）、`time_*`（自相关 lag 1..10 + absmax、0.1s 分桶 FFT 前三主峰功率占比与主频、log 间隔直方图峰数/峰间距/归一化熵、Goh–Barabási 突发度）、`timeup_*` / `timedn_*`（`time_*` 十量的逐方向版本，各 10 列，两个独立可诊断的族）、`seq_*`（包长按**全局固定** 8/16/32 桶量化后的熵 / bigram 熵 / bigram top1 / distinct 比、16 桶三元组熵、方向 bigram 熵与 top1、ud/du 转移占比、zlib 压缩比）。**五族只需 (时间戳, 包长, 方向)，全在交集内**（规格 §1.2：交集是新特征能否被外部验证的唯一区域）。**动机由已测结果指定**：①`UNSW-FAMILY-REVERSAL` UNSW 池 7 族全负；②自采侧两池共有的 7 族也全弱（`subwin` +0.0156 为唯一正值且仅 4/9 任务为正）→ 唯一强反转族 `rssi` 落在空口独有那块；③`UNSW-BELKIN-CAPACITY` 方向族是唯一超 0.10 的（0.1168/0.1123） | 42–46（5 个） | 自采 pcap 35 个文件、共 **169 MB**（camera 36M / light_T1 27M / light_xm 29M / sensor 32M / socket 45M）；`results/robust_v2/raw_all/features_raw_all_w10.csv` md5 `703984b6ad2fde2f45e0cce1c6df31be`（**只读，前后核对**） | `results/feature_expansion_20260904/`：`features_new89_w10.csv`、`join_audit.json`、`family_dauc_n89.csv`、`family_dauc_n33.csv`、`pair_family_dauc.csv`、`ceiling.csv`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定对 = `EXPANSION_MARGINAL` / `EXPANSION_MARGINAL`（A 与 B 同落，两者皆未过门槛）**。双跑 8 产物 md5 逐字节一致、provenance（去 `_volatile`）一致，`ALLOK=1`。**主判据 A** `Δceiling(full94+N33)` = **+0.0070**；**主判据 B** `Δceiling(full94+N89)` = **+0.0134**；门槛 +0.02（18 sd）→ **两条都没过**。域内天花板 0.9084（本 runner 重跑 `full94` 与外部参照 0.9084 差 −1.7e-05，估计量一致）→ 0.9153（127 列）→ 0.9218（183 列）。**并报第三量 `Δ(N89 − N33)` = +0.0065 > 0 → 作者按证据筛选的 33 列靶单丢了信息，多出的 56 列在帮忙、不在稀释**（与 v2→v3 修订时用户提出的稀释顾虑方向相反，如实记）。**逐族诊断（无门槛并报项，本协议内不构成任何「通过」）**：`rssi` 仍 top1，+5.4426（183 列）/ +5.1472（127 列）/ +5.2440（冻结 94 列）→ **该量对特征池规模不敏感，浮动 ±4%**；占优度由 336× 降至 124×，仍远超 3.0× 闸门 → 扩池未造成「接近候选」多重性问题。**五个新族全部为负**：`time` −0.0098 / `up` −0.0318 / `timeup` −0.0870 / `timedn` −0.0906 / `seq` −0.5121 / `dir` −0.5258（`dir` 32 列为最负）→ **新族中未出现第二个反转族**；按协议 §4 规定 3，不得据此采取任何动作。**方向 bug 代价**：`dir`（修正规则 `ra==BSSID`，32 列）−0.5258 vs 原 `up` −0.0318 / `down` −0.1649 —— 修正后的方向族诊断值更负，即**更「有用且稳定」**，与 `up_packet_ratio` 在非 Socket 四类 100% 窗口为 0 的独立线量化一致。**并报项缺失（合规缺口，如实披露）**：协议 §5.5 要求并报 `joint_tb` 逐类 F1，但冻结 `run_histint.joint_eval` 只返回 `macro_f1`，而硬门 4 禁止重实现；runner 以 `r.get("per_class_f1", {})` 静默取空 → **该并报项未产出**。此为协议内部两条要求（§5.5 与硬门 6.4）的冲突，非判据失败；逐类 F1 改由 `TRANSMITTER-PARTITION-VALIDATE` §4 补测。**判定的含义已改变（运行后发现的上游事实，必须与判定同处披露）**：自采数据集在 802.11 空口上**只有 3 个发射机却打了 5 个标签** —— `Camera`=`78:df:72:04:d6:b4`、`Socket`=`b8:50:d8:67:b5:d0`、而 `Light_T1`/`Light_XM`/`Sensor` **共用** `54:ef:44:59:eb:4c`（小米网关，三者为其后的 Zigbee/BLE 设备，数据集作者已确认；R2–R7 六轮一致；三者 802.11 能力指纹 `wlan.tag.number` `0,1,50,45` / HT caps `0x012c` / AMPDU `0x19` 逐字节相同，Camera 为 `0x016e`/`0x1a`；`FILTER_EXPRESSIONS["raw_all"] = None` 故 94 列缓存无任何 MAC 过滤、标签仅由 pcap 文件名决定）。**故本次 +0.0134 属可预期，不构成对「信息在未计算的交集特征里」这一假设的否证** —— 被动空口特征无法刻画不发射的设备，而 3/5 个标签正是如此。**另一后果**：`joint_tb` 在会话内部按时间块切分，而每个 (设备, 轮次) 只有一场约 1 小时会话，故域内天花板对那三个类含**会话身份混淆**，`Δceiling` 作为主判据有 3/5 的类站在被混淆的量上（判定照算，含义降级为「域内参照」）。 | **共同主判据（co-primary）两个，运行前定死，互不替代**：**A** `Δceiling_A = joint_tb(full94+N33) − joint_tb(full94)`；**B** `Δceiling_B = joint_tb(full94+N89) − joint_tb(full94)`。**A 的正当性**：`N33` 于本协议 v1 先冻结（`56ed0b20…`），**在 89 列存在之前、在任何数据之前**，据 `UNSW-FAMILY-REVERSAL`、`UNSW-BELKIN-CAPACITY`、自采 `diag_coord1.csv` 三处已测结果推导，**故不是事后从 89 中挑的子集**。**B 的作用**：检验 A 的筛选是否遗漏。每个各自落一个分支，**判定为一个「对」**：`EXPANSION_RAISES_CEILING`（≥+0.02）→ 移植 UNSW 测 Belkin（另立协议）；`EXPANSION_MARGINAL`（0 ≤ Δ < +0.02）；`EXPANSION_USELESS`（Δ < 0）。**三条不得违反**：①A 与 B 互不替代，两个判定必须同时报出，使读者能看见「只有一个过」；②两个共同主判据在同一门槛上假阳性率高于单一判据（嵌套故相关，严重性低于两独立检验），**不因此放宽门槛**，缓解手段即强制并报；③**禁止「失败后再挑」**——若 A、B 皆不落 `RAISES`，不得从逐族 `d_auc_sum` 或任何子集中挑出表现好的部分宣布成功；逐族诊断是**无门槛并报项，本协议内不构成任何「通过」**，只为下一个协议提供依据，任何基于它的动作须先在新协议中预注册该族再重测。**门槛 +0.02 依据（定于任何本协议数字之前）**：sd 0.0011 → 约 18 sd；占表示代价可用余量 0.092 的 22%。**并报第三量（无门槛、不参与判定）**：`Δ(N89 − N33)` —— **测作者的证据筛选帮了还是害了**，为正说明筛选丢了信息，为负说明多余 56 列在稀释。**只测不选（硬门 3）**：不调用 `accept_family`、不产生 `removed` 列表、不做配置选择 —— 因 `HIER-CLASSPAIR` 已实测选择规则在 9 个接近候选间选错（前四名差 0.005、Camera 代价 0.42）。并报：15/13 族全列 `d_auc_sum`、逐任务符号一致性（闸门 A，无否决权，校准 `rssi` 8/9 vs `subwin` 4/9）、占优度（参照 336×）、`dir` vs `up`/`down` 对比、`joint_tb` 逐类 F1（现 full94 域内 Camera 0.980 / T1 0.807 / XM 0.778 / Sensor 0.818 / Socket 0.999，**天花板损失集中在困难簇**）、逐新列恒零窗口比例、连接未匹配行数须为 0。**成本**：89 列抽一次约 25–30 min；两次 `coord1_diag` 约 4 min；三配置 `joint_tb` 约 37 min → 约 **70 min 单跑 / 140 min 双跑** |
| TRANSMITTER-PARTITION-VALIDATE | 2026-09-04（**运行前登记**，跑完补结论；**预注册预测的检验**，含烧毁隔离） | 协议 `docs/PROTOCOL_TRANSMITTER_PARTITION_20260904.md` sha256 `d0ad95186383e295f91d34787f4c95b4a999139fbfdef986b8df576bdc3cfd43`；**先写后看**：协议与本行同一 commit，早于本协议的任何数值运行。复用冻结实现 `TC.Data` / `pair_auc`（`run_two_channel.py:198`）/ `derive_families` / `make_model` / `clean_x`，一律不重实现；新增代码仅两处：(a) 符号判定与命中计数；(b) 逐类 F1 的 `best_base` 选取与汇总 | **两个共同主判据，互不替代**。**A**：`rssi` 族（8 列，`derive_families` 机械导出）的逐类对 `d_auc = pair_auc(full94_minus_rssi) − pair_auc(full94)`，逐类对统计量 `D(pair)` = 三单元求和，符号 `+`/`−`/`0` 以 τ=0.01 判；预测规则**机械、按优先级**：①两端共用发射机 → `+`；②`full94` 基线 AUC 三单元均值 ≥ 0.999（饱和无余量）→ `0`；③其余 → `−`。**B**：单元 R6（05-26，三个网关设备**同日采集**，日期/环境/网络状况完全固定）、配置 `full94_minus_rssi` 下 `{Light_T1, Light_XM, Sensor}` 三类逐类 F1 均值 `F_gw`。单元三个：源域恒 R2+R3+R4，目标域 R5 / R6 / R7（与 `R567-94DIM-RETEST` 的 `pos_R5`/`jit_R6`/`jit_R7` 同切分）。**烧毁披露（决定本协议设计）**：本协议 A 的预测所依据的符号模式，是在 `FEATURE-EXPANSION-SELF` 的并报项中**先于本协议撰写被看到的**；该观测来自 `TASKS` 的 9 个 g0 任务，其目标域**全部在 R2/R3/R4 之内**，故那 9 个任务对本协议 A 已烧毁、不使用；**R5/R6/R7 从未被该诊断读取**，预测在其上检验。定 τ 的量级校准取自已烧毁数据（饱和带上界 ≈0.0003、信息带下界 ≈0.1416，τ 距两者 33×/14×），此点协议 §3.3 已披露。**动机**：`FEATURE-EXPANSION-SELF` 运行后发现自采空口只有 3 个发射机却打了 5 个标签（`Light_T1`/`Light_XM`/`Sensor` 共用小米网关 `54:ef:44:59:eb:4c`，作者已确认）；而 `rssi` 的逐类对 `d_auc` 符号恰好把 10 个类对按发射机正确分成 3 正 / 3 负 / 4 近零 —— **程序仅凭跨域 AUC、在 MAC 从未进入模型（规格 §2.6）的前提下，还原了没告诉过它的拓扑事实**，且该事实有 MAC/OUI 独立真值可核。B 则回答「日期固定时三个网关类的分离是设备信号还是日期残留」，此前从未测过逐类 F1（`joint_eval` 只返回 macro，见 `FEATURE-EXPANSION-SELF` 的并报缺口）。**`stacking` 不进本协议**（`always-RF` 一线已定其在本数据上崩盘，且本协议不做模型选择） | 42–46（5 个） | `results/robust_v2/raw_all/features_raw_all_w10.csv` md5 `703984b6ad2fde2f45e0cce1c6df31be`（**只读，前后核对**，94 列）；拓扑真值另经 30 个 pcap 各一遍 tshark 实测 | `results/transmitter_partition_20260904/`：`prereg.json`（**先落盘**：拓扑真值、10 条预测、τ、全部门槛）、`topology.json`、`pair_task_dauc.csv`、`pair_signs.csv`、`per_class_f1.csv`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定对 = A `PARTITION_PARTIAL`（9/10）／B `DEVICE_SIGNAL_SAME_DAY`（`F_gw` = 0.6820）**。双跑 8 产物 md5 逐字节一致、provenance（去 `_volatile`）一致，`ALLOK=1`；五道硬门全过（缓存 md5 与协议声明值一致且先于任何数字核对；预注册先落 `prereg.json`；A 的目标域 ⊆ {R5,R6,R7} 以 assert 保证、9 个已烧毁 g0 任务未调用）。**A = 9/10，未达 10/10 的还原门槛**。命中的 9 个：三个 `Camera` 对网关类全部 `−`（−0.1597 / −0.2055 / −0.3142，规则 ③）、四个含 `Socket` 的类对全部 `0`（规则 ② 全部命中，基线 AUC 0.9991–1.0000，未饱和的 `Socket` 类对计 0）、同发射机的 `Light_T1\|Light_XM` +0.6460 与 `Light_T1\|Sensor` +0.4283 为 `+`。**唯一未命中：`Light_XM\|Sensor`**，预测 `+`（同发射机，规则 ①），观测 `0` —— `D` = **+0.0038**，方向为正但低于 τ=0.01。**关键并报**：同一类对在已烧毁的 R2/R3/R4 九任务上 `d_auc_sum` = **+0.9892**，在未读的 R5/R6/R7 上降到 +0.0038 —— **该效应在留出轮次上基本消失，不是门槛擦边的问题**；其 `full94` 基线 AUC 在 R5/R6/R7 上为 **0.9054**（另两个同发射机对分别为 0.4902 与 0.7087），即在留出轮次上 `Light_XM` 与 `Sensor` 已被 94 列分开、rssi 并未害它们。**故「同发射机 ⇒ rssi 反转」不是普适规律，3 个同发射机类对中 2 个成立**。按协议 §9，落 `PARTITION_PARTIAL` 时**如实报部分成立，不得挑出命中的 9 个另行成文**；亦不得事后改 τ、改饱和阈值、换单元或换配置。**A 逐五种子完全相同 = True** —— `pair_auc`（lbfgs）的确定性由此从假定变为被检验的事实（协议 §2 声明种子 42–46 而 §3 未声明 A 是否逐种子，本实现逐五种子全算并断言相同，见偏离 1）。**B = 0.6820 ≥ 0.50，通过**。同日单元 `jit_R6`（05-26，三个网关设备同日采集、日期/环境/网络状况固定）上 `full94_minus_rssi` 的逐类 F1：`Light_T1` 0.6214 / `Light_XM` 0.6421 / `Sensor` 0.7826，**三者均远高于「三类不可区分」的退化值 1/3** → **网关后三个类的分离是设备信号，不是日期残留**。这直接回答了 `FEATURE-EXPANSION-SELF` 结论列所披露的会话混淆之忧的**日期那一半**（时段那一半由「三个网关类各自恰好 3 场上午 / 3 场下午」缓解；小时未控项如实留下）。**逐类 F1 全表（此前从未测过；补上 `FEATURE-EXPANSION-SELF` §5.5 的并报缺口）**：`jit_R6` full94 0.9022/0.6231/0.6528/0.7153/1.0000（macro 0.7787）→ minus_rssi 0.6711/0.6214/0.6421/0.7826/1.0000（macro 0.7434）；`jit_R7` 0.9402/0.6913/0.7181/0.7677/1.0000（0.8235）→ 0.8708/0.7299/0.7773/0.8017/1.0000（0.8359）；`pos_R5` 0.8709/0.2833/0.6827/0.6430/1.0000（0.6960）→ 0.6260/0.5491/0.7869/0.7059/1.0000（0.7336）。两个 macro 与冻结 `R567-94DIM-RETEST` 对应值一致（`jit_R6` 0.7787/0.7434、`jit_R7` 0.8235/0.8359、`pos_R5` 0.6960/0.7336），**估计量复现**。**`Socket` 在全部 6 格恒为 1.0000**（三层发射机里体量最大且最持续者，见并报体量表）。**`Camera` 的代价首次逐单元量化**：`jit_R6` −0.2311、`pos_R5` −0.2449、`jit_R7` −0.0694；而 `pos_R5` 上 `Light_T1` 由 **0.2833 → 0.5491（+0.2658）**、`F_gw` 由 0.5363 → 0.6806（+0.1443）—— **交易结构 regime 依赖,与 `R567` / `HIER-CLASSPAIR` 同形**。**并报更正**：三个网关文件的跨标签污染为来自 Camera MAC **5437** 帧 + Socket MAC **3279** 帧 = **占网关帧 4.17%**（此前基于 R2 单轮口头估计的「1.8% 量级」偏低，以本产物 `topology.json` 为准）。**拓扑真值已落产物**：30 个 pcap 逐文件按 `wlan.ta` 计数，**「自有发射机恒为最大非 AP 站点」= True**，即 §1.1 的三发射机划分由产物而非讨论承载。**冒烟披露**（runner 文件头与 `passline.json`）：正式双跑前以 `SEEDS=[42]`、`UNITS=[jit_R6]` 缩减配置跑过一次验证管路、数字已被作者看到（A 10/10、`F_gw` 0.6820）；协议与 τ / 饱和阈值 / 全部门槛此前已冻结提交（commit `5be1ee4`），故无事后自由度。**注意**：缩减配置的 A 是 10/10，三单元求和后为 9/10 —— 单单元与三单元求和不是同一个统计量，判据取后者（协议 §3.1 定死）。 | **判据 A**：`hits`/10。`PARTITION_RECOVERED`（10/10）→ 该诊断成为方法一等结果，下一步另立协议把措辞与任务分层写进 `METHOD_SPEC`；`PARTITION_PARTIAL`（8–9）；`PARTITION_NOT_RECOVERED`（≤7）。**门槛依据**：三元符号判定随机命中 1/3，`P(10/10 \| 随机) ≈ 1.7e-05`、`P(≥8 \| 随机) ≈ 3.4e-03`。**判据 B**：`DEVICE_SIGNAL_SAME_DAY`（`F_gw ≥ 0.50`）/ `AMBIGUOUS`（0.40–0.50）/ `NO_DEVICE_SIGNAL`（<0.40）。**门槛依据（定于任何本协议数字之前）**：三类不可区分时分类器在三者间退化为随机、逐类 F1 → 1/3，故 0.50 = 1.5×(1/3)、0.40 = 1.2×(1/3)；外部参照 `strict59`（无 rssi）在**不同日**的 `g0_R3_R4_to_R2` 上三类召回 0.766/0.896/0.849（均值 0.837），**不同特征集与单元、不构成受控对比**，仅说明 0.50 是宽松下界。**为什么 B 定在 `full94_minus_rssi`（运行前给出）**：B 问「日期固定时是否存在设备关联信号」，而 `rssi` 对这三个类对**物理上不可判别**（同一电台、同一位置），其权重必为噪声拟合；`full94` 一并报出，两者之差即该噪声块的代价。**残余未控项（如实记）**：R6 上三场的小时仍不同（17h/16h/14h）；彻底解开需会话交织式重采，本协议不做。缓解证据：三个网关类各自恰好 **3 场上午 / 3 场下午**，故时段不构成跨轮次仍存活的逐标签关联。并报（无论方向不得省略）：30 行逐 (类对,单元) `d_auc` 与基线明细及逐单元符号、全类对逐单元基线 AUC（核验 `0` 带确由饱和造成）、**逐类 F1 全表 5 类 × 3 单元 × 2 配置 × 5 种子（含逐种子）**、**`Camera` 逐类 F1 的变化（已知代价，必须显式报）**、30 个 pcap 按 `wlan.ta` 的帧数分布真值表、三个网关文件的跨标签污染计数（来自 Camera/Socket 的帧数）、任一 `Socket` 类对基线 AUC < 0.999 时显式记出并说明预测按规则 ③ 执行。**运行后禁止**：不得改 τ、改饱和阈值 0.999、换单元、换配置或改口径以求通过；落 `PARTIAL`/`AMBIGUOUS` 时不得挑通过的类对另行成文（协议 §9）。**成本**：A 部分 60 次 `pair_auc` ≈ 2 s；拓扑真值 30 个 pcap ≈ 2 min；B 部分 90 次拟合（rf 10.0 / xgb 16.5 / lgbm 20.1 s 实测）≈ 23 min → 约 **25 min 单跑 / 50 min 双跑** |
| PAIRWISE-OVERRIDE-REGISTER | 2026-09-04（**跑后补登记，纪律偏离，见 commit 列**；方法定义冻结 + 开发数字登记，**非确认性检验**） | 协议 `docs/PROTOCOL_PAIRWISE_OVERRIDE_20260904.md` sha256 `090a8a7d959064252adda9611a9260888198e97a118042a47f589df719eb9bcd`。**纪律偏离（如实披露，不得省略）**：本行与协议的 commit **发生在运行之后**，违反登记表维护规则 1（开工前登记一行）。**已具备的部分**：协议文本的 sha256 在运行前即已定死，并写入 runner 与 `provenance.json`（`protocol_sha256` = 上述值、`runner_md5` = `ddbb1234305c144e45bd8abab5f31897`、运行时 `git_head` = `1bb61b4`），故「运行所依据的协议文本未在运行后被改动」这一点可核验；**缺失的部分**：登记行与 commit 的时间先于运行这一点**不可核验**。**减轻情节**：本协议 §1.2 已自我声明**不是确认性检验**——其全部数字（迭代 1、迭代 2 单种子、迭代 2 五种子）在协议撰写前已被作者看到，故「先写后看」的性质本就按设计不存在；本次运行的功能仅为①冻结方法定义②登记开发数字③产出双跑一致产物。复用冻结实现 `Data`/`pair_auc`/`derive_families`/`make_model`/`clean_x` 与 `accept_family` 常量（`ACCEPT_RATIO`=3.0、`ACCEPT_WORST_FRAC`=0.5、ε=0.05），未重实现 | **方法**：基座 = 完整池多类模型；对通过**三重闸门**的无序类对，在该类对的掩码特征集上训二分类器，当基座 top-2 恰为该类对时重判，否则保持 `argmax`。**三重闸门**（三条同时成立）：①`accept_family` 三条件；②类对内占优度 `S_f/max(S_次名, ε=0.05) ≥ 3.0`；③绝对下限 `S_f ≥ 0.15`（逐任务平均 ΔAUC ≥ 0.05）。**掩码只由 `\|S\|=2` 的三个 inner 任务导出**（目标全在 R2/R3/R4 内），该要求由实测强制：`Light_XM\|Sensor` 的 rssi `d_auc` 在 9 任务上合计 +1.3089，其中 `\|S\|=1` 的六任务占 **+1.3103**、`\|S\|=2` 的三任务为 **−0.0014**、outer 真相 +0.0038 —— **整个效应全落在 `\|S\|=1`**（含两个基线 AUC 低于 0.5 的格：0.2067 / 0.4942），混入该档闸门就会在该类对上误开火。**方法未被告知任何族的物理含义**：闸门在本池上**恰好只选出 `Light_T1\|Light_XM`（+0.9817，占优 19.63）与 `Light_T1\|Sensor`（+1.0601，占优 21.20）**，8 个类对全部拒绝，含 `Light_XM\|Sensor`（top1=`subwin` +0.0050，占优 0.10）—— 即 `TRANSMITTER-PARTITION-VALIDATE` 落 `PARTITION_PARTIAL` 的那一格「同发射机但不反转」被正确拒绝。**闸门 ③ 的取值披露**：0.15 在看过闸门数值分布后选定；不敏感性论证 = 通过 ①② 的候选里 rssi 两格为 +0.98/+1.06、其余全 ≤0.0137、被拒最大者 0.0087，**相隔 72 倍**，(0.0137, 0.9817) 内任何取值给出相同选择。**五臂**：`flat_full94` / `flat_drop_rssi` / `override_gate3` / `override_anyfam_loose`（消融：去掉 ②③）/ `ovo_full94`（消融：整套 one-vs-one） | 42–46（5 个），模型 `rf`/`xgboost`/`lightgbm`，`best_base` 逐 (单元,臂,种子) 取三模型 macro 最大者；`stacking` 不进本协议 | `results/robust_v2/raw_all/features_raw_all_w10.csv` md5 `703984b6ad2fde2f45e0cce1c6df31be`（只读，先于任何模型数字核对） | `results/pairwise_override_20260904/`：`gate_decisions.json`（**先落盘**）、`gate_decisions.csv`、`inner_pair_family_dauc.csv`、`s_stratum.csv`、`s_stratum_summary.csv`、`per_cell.csv`、`best_base.csv`、`per_class_f1.csv`、`device_level.csv`、`passline.json`、`provenance.json`、`VERDICT.md` | **判定 `OVERRIDE_IMPROVES`（三条判据全过；但按 §1.2 该判定不具确认价值）**。双跑 11 产物 md5 逐字节一致、provenance（去 `_volatile`）一致、`ALLOK=1`。**主判据窗口级 macro（best_base，三单元五种子均值）**：`override_gate3` **0.7893** > `override_anyfam_loose` 0.7804 > `flat_drop_rssi` 0.7710 > `flat_full94` 0.7660 > `ovo_full94` 0.7596。**逐 (单元,种子,模型) 45 格**：对 `flat_full94` 均值 **+0.0289**、sd 0.0169、min +0.0080、max +0.0600、**为正 45/45**；对 `flat_drop_rssi`（现有流程终点）均值 **+0.0248**、为正 30／为负 15。**Camera 逐格变化 = +0.0000、min +0.0000、退化 0/45** —— **逐位不变，且是结构性的**：被覆盖的两个类对都不含 Camera，故 Camera 判决从未被触及。**这是规格 §2.8「不靠牺牲高准确率设备换总体」首次被字面满足**（`HIER-CLASSPAIR` 当时把 Camera 从 0.8131 砸到 0.4713）。**必须点出的一件事：覆盖法在网关三类本身上并未打败 `flat_drop_rssi`** —— `F_gw` 覆盖 0.6784 vs `drop_rssi` **0.7108**（基座 `flat_full94` 0.6419）。两者的差别是**交易结构**：`drop_rssi` 用 Camera **−0.1818**（0.9044→0.7226）换三类 +0.0689，净 macro +0.0050；覆盖用 Camera **0.0000** 换三类 +0.0365，净 macro **+0.0233**。即覆盖法拿到的是三类增益的约 53%，但代价为零 —— **它是更好的交易，不是三类上的支配**。**两个消融**：①`override_anyfam_loose`（去掉闸门 ②③）macro 0.7804、**Camera 0.8774（退化，40/45 格）** → **候选限制/量级闸门是 Camera 逐位不变的来源**；②`ovo_full94` macro 0.7596 < 基座 0.7660 → **one-vs-one 结构本身亏 0.0064**，故本方法用「覆盖」而非 OvO 是被实测排除后的选择。**设备级 N=30（成对报出、不作判据）**：覆盖 0.9492 vs `drop_rssi` **0.9778** = **退化 −0.0286**；该粒度三单元合计仅 **192 块**、一块翻转 0.0052、`drop_rssi` 只错约 4.3 块，**已饱和**，故主判据定在窗口级（作者 2026-09-04 决定；窗口级 5797 窗、分辨率高 30 倍）。**开发期失败臂一并登记（不挑选）**：迭代 1（one-vs-one + 裸 `d_auc_sum>0`）三单元均值 macro **0.6392**、Camera **0.3277**（`jit_R6` 上 0.0345），**比基座差 0.118**；失败原因两条 —— OvO 结构亏 0.063、`THR=0` 让噪声族进来使 Camera 三个类对被削到 63–67 列。**迭代 2 由该诊断驱动、非按分数挑**。 | **判据（窗口级 macro；确认协议将逐字使用同一组）**：`OVERRIDE_IMPROVES` = ①对 `flat_full94` 为正 ≥40/45 格 **且** ②Camera 逐格退化 ≤0.01（45/45） **且** ③对 `flat_drop_rssi` 均值 >0；`OVERRIDE_TRADES` = ①过②不过（**不得称保住两边**）；`OVERRIDE_FAILS` = ①不过。**门槛依据**：40/45≈89%，留 5 格容错（种子 sd 0.0017–0.0038、单格波动约 0.004）；0.01 沿用 `HIER-CLASSPAIR` 条件 1 同一容差。**本协议内三条会被满足（数字已知），故其「通过」不具确认价值。****确认段按协议 §9 预注册**，三个未烧靶：①**自采 R1**（`round1_filter`，从未进任何特征缓存、零数字产出；仅数据帧，须用冻结提取器的 `data_only` 模式把 R1–R7 同口径重抽作**并行臂**，列数少于 94、不得与 94 列线逐格比）；②**UNSW 第 11–20 天**（10 天从未读取，该池无位置耦合列，预期闸门**不开火** → 检验「不该动时不动」）；③**CIC**（71 GB 全在服务器、`CIC-RANGE-ANCHOR` 从未运行；`2-Idle` 30 天全网 对 `Active` 构成同抓包点同设备的剧烈行为位移。**`6-Attacks` 不可用** —— Readme 自陈「captured on the attacker machine for 3 or more minutes」，抓包位置与时长均不可通约）。**确认协议中禁止**调整三重闸门任何常量（3.0／0.5／0.05／0.15）、禁止改任务集的 `\|S\|` 要求、禁止换汇总口径、禁止改主判据粒度。**成本**：源域侧 330 次 `pair_auc` ≈8 s；五臂 ×3 单元 ×5 种子 ×3 模型实测 **1168 s/单跑、约 39 min/双跑** |
