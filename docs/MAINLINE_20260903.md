# 主线（2026-09-03 重建）

**性质**：这是 2026-09-03 一整天讨论与测量重建出的主线，**取代**
`PROJECT_STATUS_FOR_EXTERNAL_REVIEW_20260902.md` §1 的四层链条叙述。
本文只写今天确立的东西；历史判定不重开，历史文档不删改。
文中数字凡标「探索性」者产自 `/tmp` 下的诊断脚本、无协议，进论文前须随协议冻结重算。

## 0. 一句话

跨轮次识别率下降不是一个失效，而是**三个**，其中两个可诊断；诊断都是数据集内的无量纲量，
所以**流程可以跨数据集搬，答案不能**——每个数据集得到自己的答案，这是唯一干净的外部检验方式。

## 1. 真实的发现顺序（叙述必须照这个走）

```
想提高识别率  →  RSSI 消融（第一个实验，docs/RSSI特征分析.md）
              →  跑完 7 个冻结任务 PRIMARY_TASKS
              →  发现 stacking 低于它自己的基模型      ← 触发追因的那个异常
              →  追因
```

四层链条（表示 → 簇 → 元层，CPD 作旁支）是**从表示往下推**的构造，读起来像先有理论再验证；
真实过程是反的。**按真实顺序写更难被质疑成事后编故事。**

三处身份纠正：

| 对象 | 原来的说法 | 今天定的 |
|---|---|---|
| always-RF | 要打败的基线 | **本实验测出的最优解**，是流程的产出而非先验已知 |
| RSSI 单独消融 | 未做的欠账 | **项目的第一个实验**，25 维时代已做（见 §7） |
| `meta_soft_cls` | 候选方法贡献 | **机制的因果消融**，不进流程动作（打不过 always-RF） |

## 2. 现象：不是一个悬崖，是一个阶梯

自采 full94、5 种子均值：

```
同轮次随机划分     0.9622    R2 0.9694  R3 0.9655  R4 0.9516
同轮次时间块       0.9296    R2 0.9541  R3 0.9559  R4 0.8789
联合三轮内部       0.9200
联合三轮时间块     0.8936
跨轮次 LORO        0.6979    0.6631  0.6114  0.8192

逐级差   −0.033 轮内时间漂移（R4 独占 −0.073）
        −0.010 混入另外两轮
        −0.026 联合的轮内漂移
        −0.196 ← 悬崖：从未见过这个轮次
```

前三级合计 −0.069，最后一级 −0.196。**「同轮次 90+」的主要成分不是时间稳定性，是"见过这个轮次"。**

第二个现象，在跨轮次设定内部：**stacking 低于它自己的最佳基模型。**
9 个跨轮次任务 `gap_vs_best_base` 均值 **−0.0961**（`|S|=2` 三任务 −0.1741、`|S|=1` 六任务 −0.0571）。

## 3. 三种失效模式与损伤归因

```
窗口
├─ 基模型自己就错                          ← 模式一：表示层
├─ 基模型一致且正确，元层推翻它              ← 模式二：元层·映射
└─ 基模型意见分歧，元层仲裁选错              ← 模式三：元层·仲裁
```

**模式一 · 表示层（探索性）**：源域拟合的多变量类对判别，CV AUC 0.93–0.98，目标域 0.62–0.68；
39 个困难簇组合中 **13 个目标 AUC < 0.5**（边界反号，最低 0.0000）；易类对 5/5 组合 1.0000 → 1.0000、零反转。
`auc_tgt → 基模型错误率` **−0.554**（四分位 0.7207 → 0.5410 → 0.3846 → 0.3832 单调）；
`auc_tgt → 元层损伤率` **+0.078**（无）。

**模式二 · 元层·映射（探索性）**：共识推翻率——三基模型一致且正确的窗口中 stacking 改判的比例。
三旗舰任务 **1149/1472 = 78.06%**，易类 **0/2101**。三种权重分配全落在最佳基模型附近
（`meta_soft_cls` 三任务均值 −0.0058），只有加跨类项的那档塌到 **−0.1741** → 是**映射**不是权重。
有符号类对矩阵：五个簇内类对**全部净负**，害益比 4.2:1 到 12.1:1（199 次救回真实存在但被压倒）。
`m_src → 元层损伤率` **+0.494**，`m_src → 基模型错误率` **−0.253**。

**模式三 · 元层·仲裁（探索性）**：`|S|=1` 六任务 gap 均值 −0.0571，损失几乎全在分歧窗口，
而 `m_src_max` 在那里**精确为 0**（30/30 个 `|S|=1` 任务）。

**关键：模式一与模式二不相关**（`auc_tgt ↔ m_src_max` −0.153、↔ `harm_max` −0.212，n=35）。
原因是**它们消耗不同的窗口**——模式一先扣掉窗口，模式二只能作用在剩下的：

```
              组合数  目标AUC  基模型错误率  元层损伤率  基共识正确占比
边界保持       26    0.9161   0.4356    0.1898    0.4238
边界反转       13    0.1770   0.6604    0.1100    0.2397
```

所以是**误差预算里的先后关系**，不是两个独立现象。

## 4. 统一形式：伤害 = 强度 × 反转

两个层面上各自实测到同一形态：

```
伤害  =  强度        ×  反转
        源域可测        源域不可见

层面一（特征/基）  rssi 在源域逐类对判别力强（|d_src| 均值 0.1029），方向反转
                   多变量边界 0.9877 → 0.0001（不是掉到 0.5，是掉到 0）
层面二（元层）     m_src 强度 0.1323，其中 69% 在源域是有益的
                   同一映射在目标域以 0.1209 的比例造成伤害
                   开火率几乎不变（0.1323 → 0.1209），变的只有对错
```

**跨域保率、反号。** 两个层面都说：失效是**符号反转**而非幅度衰减。

支撑这一点的两组对照：

```
特征层面   loss（幅度流失）失败：rssi 均值 −0.0177（方向还相反）
           flip（符号反转）有效：rssi 反转率 0.2628，为 transferable-59 的 3.6 倍
元层层面   f_ji_drift（错配幅度）+0.339
           f_ji_src（强度）      +0.593
           m_src（元层自身强度） +0.798，控制它后 f_ji_src 只剩 +0.064
```

## 5. 为什么 CPD 与 EC-MDM 失败：函数形式错了

```
CPD_y     行归一化混淆矩阵的漂移量     测「变了多少」   形式对（矩阵、类对分辨），算在 RF 上、无符号
EC-MDM    源元层与 oracle 目标元层之差  测「差多少」     有元层、C 级类条件，但要 oracle 标签、无符号
```

两个都是**幅度/差值**。而两个独立层面都显示幅度是较弱的形式。
**所以它们不是测了个不存在的东西，是把乘积的两个因子都错过了。**

这也解释了项目最大的挫折——「预警量能预测会不会发生，不能预测掉多少分」
（可分性 → 推翻率 −0.753，可分性 → `gap_H` 只有 +0.123）：**源域诊断只看得见强度，看不见反转。**
要预测幅度必须知道反转，而反转需要目标标签。

**UNSW 提供了这条论断的配对实证（2026-09-04 补记；事后解读，非预注册，不改任何判定）**：
`UNSW-META-MISMATCH` 落**分支 C**（`MECHANISM_NOT_EXTERNALLY_REPLICATED`；四条签名中
①定位 2/3、③系数 0/3、④regime 2/3 失败，②免疫 3/3 通过；幅度 `mean_abs_gap_H` = 0.0099
落在预注册区间 [0.03, 0.20] 之外）。但**失败的形态恰好是本节论断的直接证据**：

```
                            困难簇推翻率          结果
自采 full94（9 任务）        1152/4065 = 0.2834    净 −947，害益比 1152:205 = 5.6:1
UNSW 09-23+09-30→10-12        507/1710 = 0.2965    gap_H = +0.0117（stacking 略优）
```

**推翻率（强度）几乎相同，结果相反。**这把本节原本只由相关系数支撑的论断
（可分性 → 推翻率 −0.753 vs → `gap_H` +0.123）换成一个**配对观测**：
决定伤害的不是元层开火的频率，而是开火的**方向对不对**。

为什么 UNSW 的推翻是净中性的——**已测**（2026-09-04，`results/unsw_meta_mismatch_20260902/fourcell_posthoc.csv`，
口径逐字对齐 §7 自采四格 `diag_grid_override.py`；纯聚合、不拟合模型；`broken` 与已登记的 `n_overridden`
三任务逐位对账 507/101/25 一致）：

```
困难簇                  共识正确   破坏   共识错误   救回   换错法    净     破坏率   救回率
自采 full94（9 任务）     4065   1152     4884     205     594   −947    0.2834   0.0420
UNSW（3 任务合计）        4900    633     4358     656      23    +23    0.1292   0.1505
UNSW 任务 09-23+09-30→10-12   1710    507     1236     518       8    +11    0.2965   0.4191
```

**UNSW 救回 656 ≈ 破坏 633，净 +23，精确对消**；同推翻率的那个任务里破坏 507、救回 518。
而 `换错法`（stacking 把一个错误共识改成**另一个**错误）UNSW 只有 **23**，自采 **594**——
自采那个「无差别开火」的行为在 UNSW 上几乎不存在。**所以两边的元层都在开火，开火频率相同，
差别只在方向**：自采的映射把对的改错（1152）远多于把错的改对（205），UNSW 的映射两者相抵。

**仍是推论的部分**：为何 UNSW 的映射方向对——候选解释是 Belkin Motion↔Switch 混淆**双向对称**
（`f_ab` = 0.361 / `f_ba` = 0.446）且跨天稳定，但 `cluster_definition.json` 的 `f` 是跨天**均值**，
逐天稳定性**未测**；此条不进正文承重结构。

**另一层界线：两边的「困难簇」不是同一种东西。**UNSW Belkin 对**域内就在随机线上**
（`src_oof_H_rf` 0.4585 / 0.5556 / 0.5167，`target_H_rf` 0.4750–0.5600，二类互混 0.361/0.446
即二元正确率约 0.60）；自采困难簇**即使删到 59 列（`strict59_ra`，删 35 列、含 `rssi` 族 8 列）域内仍有 0.73–0.88**
（`g0_environment_grid_strict59_ra_r5`，rf、同轮次时间块、R2–R5 的 Light_T1/Light_XM/Sensor；**只删 `rssi` 族 8 列的域内代价**由 `HISTINT-OBJ-SIGNAL` 实测为 `ΔJOINT` −0.0406，即只删 `rssi` 时域内更高）。
故 UNSW 是**模式一饱和**的端点（信息不在那 61 列里），自采是模式一未饱和、模式二有作用空间的情形。

**对审稿的用处**：这直接挡掉「你们的 stacking 崩盘不就是困难类的副产品吗」——
**同推翻率、反结果**说明困难本身不充分，还必须映射方向错。分支 C 的处置
（机制只作自采单测试床刻画、UNSW 只承担现象成分 +0.032778）**不变**；本条只补「为什么失败」，
而 §6 分支 C 本就要求「失败的具体签名必须逐条写入正文」。

## 6. 通过四条要求的诊断量

一个够格的预警量要同时满足：①不需目标标签 ②含元层 ③类对分辨 ④有符号。
历史三个候选各缺不同的：可分性缺 ②③④，`CPD_y` 缺 ①②④，EC-MDM 缺 ①④。

**融合量四条全过**（今天找到的）。EC-MDM 的 oracle 需求只在 `F`（目标重拟合元层）那一半，
`S`（源域拟合元层）零目标标签就在 `model.joblib` 里；给 `S` 加上类对分辨与符号即可：

```
m_src[i→j]       基共识为 i 的源域 OOF 窗口中，元模型改判为 j 的比例      映射强度
rescue_src[i→j]  其中「共识 i 错、真值 j、元模型改对」的比例              源域内有益
break_src[i→j]   其中「共识 i 对、元模型改成 j」的比例                    源域内就有害
```

对目标域逐类对伤害：`rescue_src` +0.808、`m_src` +0.798、`break_src` +0.777。

**第三种模式的诊断（仲裁）必须嵌套，否则整体失效**：

| 版本 | Spearman → 目标域分歧组 Δ错误 | 触发 | 命中 | 漏报 |
|---|---:|---:|---:|---:|
| 样本内（`final_estimator_` 作用于自身拟合过的 OOF 行） | **−0.219** | 0/9 | 0 | 7 |
| 嵌套（样本外） | **+0.692** | 7/9 | 6 | 1 |

只算有害侧（不减去有益侧）+0.051，**无信号**——必须用有符号净值。
两个元层通道的信息需求不同：**映射通道需 `|S|>=2`**（单轮次源域 OOF 里不存在跨轮次混淆）；
**仲裁通道 `|S|=1` 也能测**（它测的是元层过拟合，不需要跨轮次位移）。

## 7. 干预已经做过一次，而且证据形态很强

94→59（严格说是 strict59_ra = 删 35 列 + 方向修复），150 个 OOD 任务：

```
模型        OOD 均值    为正任务      IID 均值
rf         +0.0591    96/150      −0.0562
xgboost    +0.1357   125/150      −0.0392
lightgbm   +0.1464   127/150      −0.0372
stacking   +0.1617   145/150      −0.0395
```

**分解**：RF 没有元层，`+0.0591` 是纯表示效应；`stacking − RF = +0.1026` 只有带元层的模型拿得到，
是「修正错误映射」那一份。**带元层的模型收益大于不带的，这个差异效应本身就是分解的证据。**

配对四格分解（full94 vs strict59_ra，9 个跨轮次任务，困难簇，探索性）：

```
表示            共识正确   破坏   共识错误  救回  换错法    净     破坏率
full94           4065   1152    4884   205   594  −947   0.2834
strict59_ra      7821     58    2170    58    10     0   0.0074
```

**共识正确接近翻倍（暴露变大），破坏率降 38 倍，净收益从 −947 到精确 0（残余映射净中性）。**
`换错法` 从 594 塌到 10——那个"无差别开火"的行为基本停了。
**暴露变大不是负面结果**：单位暴露的伤害率同时降了 38 倍，所以 stacking 同时拿两份收益。

`gap_vs_best_base` 在 OOD 上 **−0.0729 → −0.0062**（降 91.5%），IID 几乎不动（−0.0030 → −0.0047）。
注意 gap<0 的任务数 115 → 117：**符号没修掉，量级修掉了。**

## 8. RSSI：三条独立路径指向同一族

```
① 最初的消融（25→20，只删 5 列 rssi，docs/RSSI特征分析.md）
② 逐任务符号模式在两个时代重演
③ 多变量边界迁移诊断（今天）
```

② 的证据：

| LORO 任务 | 25→20 删 rssi | 94→59 删 35 列 |
|---|---:|---:|
| R2+R3→R4 | +0.0465 | +0.0371 |
| R2+R4→R3 | +0.2717 | +0.1874 |
| R3+R4→R2 | −0.0353 | −0.0739 |

**只删 5 列就复现了删 35 列的整个逐任务模式，幅度还更大。**

③ 的证据：逐族剔除的 ΔAUC_tgt 均值 `rssi(8)` **+0.1638**，其余五族全部 ≤ +0.0004 或为负；
`transferable(59)` 是 −0.0992（38/39 组合为负）。易类对上删 rssi 均值 **0.000**、最大 +0.0002。

**两条必须写进限制的反向证据**：

```
删 rssi 在别的部署 regime 上有害（25 维时代，联合训练 R2+R3+R4 为源）
  位置泛化 →R5      0.6745 → 0.6531   −0.0214
  抖动泛化 →R6+R7   0.7550 → 0.7310   −0.0240
```

R5/R6/R7 **也是跨轮次**，只是多了一项改动，而现实部署里设备被挪动、用法变化是常态。
所以它们是**更真实的档次**，其代价是可权衡的：设纯跨轮次占部署期比例 w，
`w × 0.0943 + (1−w) × (−0.0227) > 0 → w > 0.194`，**只要约 19% 以上是纯跨轮次，删列净赚**。
但这两个数字是 25 维时代、`|S|=3` 源配置的，**在 94 维下从未重测**——待办。

## 9. 流程：两段制不是工程妥协，是分解的直接后果

```
提议阶段   用源域可测的强度识别暴露        m_src / net_dis_src / 逐类对边界判别力
接受阶段   用历史有标签轮次估计反转率      内层 LORO（|S|>=2）/ 嵌套源域 OOF（|S|=1）
```

因为「伤害 = 强度 × 反转」，第一个因子源域可得、第二个只能从历史证据估。
两个坐标都改成这个形式之后，流程在其所依据的证据上**按构造单调**——
任何一步都不会被接受，除非它在该证据上改善跨轮次 macro-F1。

**所以「只要不在（本移动集合下的）局部最优，应用它可能改善；若已在，它什么都不做」是构造上成立的。**
三处必须同时声明的边界：

1. 「局部最优」相对于**移动集合**（10 个族最多删 3 个 + 堆叠/不堆叠），不是无条件的局部最优。
2. 单调**只在历史证据上**成立，不保证迁移到未见新环境（R5/R6/R7 就是反例档）。
3. 贪心不等于集合内最优——175 个配置的穷举格（约 11 小时）才能判定，另立协议。

**唯一优化目标是跨轮次 macro-F1**；同轮次（IID）只作护栏与天花板参照，不进判据——
部署形态是训一次跑后续轮次，从不在同轮次 regime 内运行，所以那不是权衡。

**使用条件**：历史 1 个环境时坐标 1 与 `m_src` 触发器不可用（且坐标 1 在该档最危险，
98% 的代理量损失集中在 `|S|=1`，最差 −0.2951，而 `|S|=2` 最差仅 −0.0175）；
`net_dis_src` 触发器可用。历史 ≥2 个环境才完整可用。环境越多元层损伤自身也在衰减
（`|S|=2` gap_H −0.1245 → `|S|=3` −0.0805）。

## 10. 跨数据集：搬流程，不搬答案

**不需要找一个跨数据集通用的特征。** 每个数据集用同一套流程得到**自己的**答案，
只比较"它自己有没有变好"。这使 61 列不可通约（34/61 分布重叠 <0.20、
`interarrival_p10` 差 13,700 倍）在构造上不适用，也使「没有公开 802.11 数据集」
只挡住**发现**的复现、挡不住**流程**的复现。

族导出规则机械且跨数据集一致（按列名首个下划线段分组、成员 <2 并入 `singletons`）：
自采 94 列 → 10 组，UNSW 62 列 → 7 组。

```
UNSW 20 天    设备身份粒度 同日 macro-F1 0.8684–0.8907（accuracy 0.9047–0.9229）
              跨天 0.8164–0.8674，落差 0.0233–0.0520（6/6 为正，均值 0.0328）
              修迁移的射程 = 0.0328；剩余 ~0.12 是同日内在难度，本流程不针对
              角色 = 流程的通用性（诊断会不会正确开火、会不会造成损伤）
              加一条排序判据：诊断能否预测哪些日对退化最重（落差差 2.2 倍）
CIC 30 天完整  40 台设备含 11 台同生态插座（7 Gosund + 2 Teckin + 2 Yutron）
              可分性最低端 → 量级要去这里找。许可条款未确认
```

## 11. 状态清单

**已测（探索性，需随协议冻结重算）**：三种失效模式的归因；两个层面的强度/反转对照；
融合量四条要求全过；嵌套仲裁诊断；配对四格分解；三条路径指向 rssi。

**未测**：流程从未端到端跑过（协议 `PROTOCOL_TWO_CHANNEL_20260903.md` 正在跑）；
8 列能否达到 35 列+方向修复的效果；穷举格；R5/R6/R7 在 94 维下的重测；
所有相关系数缺按任务聚类 bootstrap 的不确定性陈述。

**结构上不可测**：自采 3 个同条件轮次无法验证流程（留一后 `|S|=1`）——验证只能在 UNSW/CIC。

**已知会被审稿人打的地方**：单测试床；`|S|=1` 占 6/9 个任务而坐标 1 在那里最危险；
`m_dis_src` 的样本内版本先失败后修好（先后顺序已在协议 §12.1 原文披露）；
`transferable(59)` 那一族无法再细分（族级搜索无法定位 `singletons` 里的单列元凶）。


