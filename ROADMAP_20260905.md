# 到论文的剩余工作（2026-09-05 夜盘点）

**状态**：科学上想明白了约 70%；经得起检验的证据约 30%（今晚全部是烧毁数据上的探索性实验，
零确认性证据）；论文 0%。

**最大风险不是做不完，是方法刚换过形状而所有纪律资产还挂在旧形状上。**
`METHOD_SPEC` 描述的「逐类对特征掩码」今晚实测是四维里最不值钱的一维（Δ≈0）；
真正 work 的是**同流时间平滑 + 逐类对 (模型, 时长) 定点修 + 相对在位者的可判决区闸门**。
现有冻结协议、登记表条目、`METHOD_SPEC` 都要跟着改，否则前面产出的数字没有制度分量。

---

## 当前证据一览

```
                          base     +平滑    +定点修   平滑增益   方法增益   seed
UNSW（10 类）             0.8379   0.9054   0.9802   +0.068    +0.075     5
CIC 型号级（28 类）        0.7961   0.9253   0.9250   +0.129    −0.0003    2
CIC 实例级（36 类，废弃）   0.6029   0.6926   0.6943   +0.090    +0.002     5
自采（override_183，旧方法） flat_94 0.7656 → override_183 0.8210          5
```

- **安全性（不赔）**：两个池成立，且机制可核对（换闸门就从 −0.0096 变 −0.0003）
- **有效性（能涨）**：一个池，且是方法被塑形的那个池 → **样本内，只能算假设**

---

## P0 —— 决定方法成不成立

### 1. 自采数据上跑新方法（探索性先行）
**为什么**：唯一能给"有效性"提供第二个证据的靶，且是 out-of-sample（方法在 UNSW 上塑形）。
形状对得上——错误质量 86.5% 集中在网关三类，而那三类是**真正不同的设备**（不是同型号实例，
不适用型号级合并，见 `three-labels-share-one-transmitter`）。采集方式也完全独立
（802.11 空口 vs 网关旁路）。
**预检**：先测同域 macro + 逐类分布 + 「inner 上是否存在能显著打赢在位者的配置」。
**判死条件**：若 inner 上网关三类的最好配置打不赢在位者 +0.02，方法在自采上也不会开火，
"有效性"就仍是 n=1。
**成本**：数据已在本地，约 1–2 h。

### 2. 修 `pair_auc`：诊断量改用部署模型
**为什么**：`run_two_channel.pair_auc` 是标准化 LogisticRegression，部署是 rf/xgboost/lightgbm，
逐类对 AUC 差 **0.15–0.86 且方向相反**（GlobeLamp LR 0.9997 / xgb 0.1431；
Netatmo LR 0.4194 / RF 0.9934）。**此前所有基于 `pair_auc` 的分型结论都建在错的模型上**，
包括 `override_183` 的推导依据、`d_auc_sum`、三重闸门的全部输入。
**成本**：改动小，重算面大，约 2–3 h。
**注意**：这是**更正**不是优化，要在登记表里写清哪些既有结论受影响。

---

## P1 —— 决定证据够不够硬

### 3. UNSW 第 11–20 天：样本外检验
**为什么**：唯一能测「在 UNSW 上塑形出来的方法，在同一数据集的新日子上还成不成立」的干净靶。
比换数据集更能隔离"过拟合到 UNSW 的具体几天"这一风险。
**成本**：提取约 1–2 h + 实验 1 h。

### 4. 四个配置维度的消融
**为什么**：审稿人必问「哪一维在起作用」。今晚已知平滑占一半以上，但四维各自的边际贡献没测过。
维度 = (特征子集, 模型, 观测时长, top-K)，两个池都要。
**已知**：特征子集 ≈ 0；模型与时长是主力；top-K 在 UNSW +0.005、CIC +0.003。
**成本**：约 2 h。

### 5. 冻结新 `METHOD_SPEC` + 写确认性协议 + 双跑
**为什么**：**现在这套方法一条确认性证据都没有。** 按项目纪律：协议冻结 → 登记表同 commit
先写后看 → 双跑 md5 一致 → 填判决。
**未烧靶**：自采 R1 的 `data_only` 并行臂、UNSW 11–20 天、CIC 12 个受保护配对日
（**但 CIC 已知无缺口可作用，不建议烧在这上面**）。
**成本**：协议撰写 2–3 h + 双跑约 2 h。

---

## P2 —— 加分项

### 6. 同设备检测器（用户提出）
**论点**：当一对类**域内明确可分、跨捕获退化到随机、且任何配置都修不动**时，
数据不支持这个标签区分——它们在网络行为意义上不是两个设备。
**验证方式**：CIC 命名自带真值（Gosund×6、Yutron×2、Teckin×2、EchoDot×2 → 粗粒度 18/630 同型号对），
盲跑全部 630 对算精确率/召回率。
**安全阀已验**：自采网关三类（同域 0.86 / 跨轮次 0.68）不触发 `cross ≤ 0.60` 的门槛——
判据不会把"困难但真实不同的设备"误判为同一台。
**价值**：把今晚自己踩的坑（用实例级标签评估 CIC）变成一个可自动检出的能力。
**状态**：2026-09-05 夜运行中。

### 7. 表征层：给 ESP 系加特征
**为什么**：CIC 型号级剩余 0.073 缺口是表征极限，不是配置问题——
`GlobeLamp|GosundPlugSocket` 最好配置在 inner 上也只到 0.71。
**方向**：跨捕获稳定的量（TCP 时间戳时钟偏斜、TTL、DHCP/DNS 指纹、TLS 指纹）。
**注意**：这已超出「配置导出」的方法身份，属于另一个贡献点，不要混进主线。

---

## 写作

**0%，没有草稿。** 结构建议（按今晚的证据）：

- **主张**：跨环境部署下，提升来自降低对源域的拟合程度，而不是提高容量；
  方法的作用是逐类对地识别出「在哪儿该退到更简单的东西上」。
  证据：stacking 崩盘 / 容量选择 ρ=−0.10 / 源域内选模型 −0.0047 /
  零参数平滑打赢全部复杂机制 / 逐类对选模型几乎从不选最高容量的
  （v2 的 44 对里 xgboost 只中 1 次，v3 的 3 对里 0 次）。
- **两半必须拆开报**：平滑不要标签、不要 inner 环境，任何人立刻能用；
  定点修要 ≥2 个有标签环境 + 逐类对搜 (模型, 时长) + 闸门。两半都属于本方法，
  但代价不同，读者要据此决定「只做便宜那半够不够」。
- **负结果与适用条件是贡献的一部分**，不是缺陷：CIC 上方法正确地不动手，
  且能说清为什么（三层账：标签空间错 / 真实水平 / 表征极限）。
- **CIC 一律用型号级评估**，明写理由与合并表；数字不可直接与按实例级报结果的文献比。

---

## 今晚证伪的路径（勿重走）

极性校正、逐列贪心掩码、减法救强反转对、源域内选模型、阈值/先验校正、
inner 与 outer 位移同型假设、top-2 覆盖够不着假设、修复不跨日迁移假设（被标签空间错误取代）。
详见本目录 README 与 `out/` 下各 log。
