# 探索性脚本与运行日志（2026-09-05 → 09-06）

**为什么在这里**：这些脚本原先在服务器 `~/cic_probe/`，**仓库外、无版本控制**。
2026-09-06 外部审阅指出：产出 C3 确证结果的 `c3_confirm.py` 在结果落盘 90 分钟后
被就地修改，**as-run 版本因此不复存在**，"确证测的就是代码做的事"无法核对。

**⚠ 不要放进 `code/`**：`.gitignore` 第 8 行排除 `/code/`（服务器上 `code/` 是独立仓库）。
第一次补救就是放进 `code/scripts/` 而被静默忽略，由同一次审阅的第二条指出。

## 承重脚本

| 文件 | 产出 | 证据等级 |
|---|---|---|
| `c3_confirm_ASRUN_reconstructed.py` | C3 确证（**重建版**，见文件头） | 【确证】 |
| `c3_confirm.py` | 同上，含审计修正 1/8/9 的当前版 | — |
| `c3_pilot.py` / `c3_pilot2.py` | 已烧天轻度验证（硬替换 / 融合形态） | 【留出】 |
| `smoke_audit.py` | 审计修正后的烟测 | 【留出】 |
| `blend_smooth.py` | 缺陷 7：专用侧平滑的 w 曲线 | 【留出】 |
| `shrink_decomp.py` | 缺陷 5：3×3 分离选择偏差与 headroom | 【留出】 |
| `gate_diag.py` | 闸门记账缺陷的诊断 | 【留出】 |
| `why_stage2.py` | 第二段为何拿不到东西 | 【留出】 |
| `audit_overlap.py` | 缺陷 1：区重叠严重性 + 顺序置换 | 【留出】 |
| `unsw_ledger2.py` | UNSW 45 对账本 | 【留出】 |
| `density_probe.py` | 窗内包数 → 特征族可用性判据 | 【探索】 |
| `dlink_*.py` / `xy_probe.py` | D-Link 的 IP 字符串长度泄漏 | 【探索】 |

`*.log` 为对应的运行日志，与脚本同时取自服务器。

## ⚠ 烟测日志里的对照选错了基线（2026-09-06 更正）

`smoke_audit.log` 把结果与"改动前已烧天试跑 Δ=+0.0024"比 —— 那是**硬替换形态**的
`c3_pilot`。正确对照是**融合形态**的 `c3_pilot2`：同两天给出 `修后 0.9270 / Δ=+0.0103`，
**与烟测逐位相同**。

**正确结论**：审计修正 1/8/9 在这批数据上**未产生任何差异** —— "无连带"最强的形式。
**此前口头说的"缺陷 8 单独带来 +0.0079"是错的**：那 +0.0079 来自融合修正，
早在 `c3_pilot2` 里；缺陷 8 无效果的原因是选中配置为 `lr/掩lenhist`（LR 而非 RF），
去掉 RF 的双重加权不影响该配置。
