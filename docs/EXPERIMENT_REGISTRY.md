# 实验登记表

协议 §19.1 要求：每次实验一行，字段为实验 ID、commit/hash、配置文件、种子、输入路径、输出路径、结论、是否进入正文。
新建实验前先在此登记，跑完填「结论」列。历史实验（协议冻结前）以补录形式登记，只列进入正文候选的关键运行。

## 登记表

| ID | 日期 | commit / code hash | 配置 | 种子 | 输入 | 输出 | 结论 | 进正文 |
|---|---|---|---|---|---|---|---|---|
| HIST-robust_v2 | 2026-06-22 | `1f81219`（前后） | `configs/research_experiments.json`, test_size=0.3, w=10s | 42 | `features_raw_all_w10.csv` (11303×102) | `results/robust_v2/`（110 条） | 主线基线；含 r=-0.630 等已降格结论 | 是（作历史基线） |
| HIST-gpu_capacity | 2026-07-03 | — | 同上 | 42 | 同上 | `results/gpu_capacity_full_20260703/`（48 条） | 深度模型历史对比 | 是（作历史基线） |
| HIST-cnn_contrast | 2026-07-07 | — | — | 42 | 同上 | `results/cnn_architecture_contrast_20260707/`（40 条） | 架构对比 | 部分 |
| P0-R630 | 2026-08-26 | `code/` @ 见 `r630_sensitivity.py` mtime | 脚本内置：n_boot=10000, n_perm=10000 | 42 | `results/robust_v2/report/controlled_cpd_data.csv` (n=11) | `results/p0_audit/` | r=-0.630 降为探索性（LOTO 3/11 显著；Spearman 不显著） | 是（降格结论） |
| P0-CPD-CORE | 2026-08-26→27 | `code/` @ 见文件 | `test_cpd_core.py`，TOL=1e-10 | 42 | 各历史 confusion_matrix.csv | 15/15 PASS，exit=0 | 三历史值 0.8397/0.1521/0.801 逐位复现并归因 | 是（回归门） |
| E1-ARMS-VERIFY | 2026-08-28 | `code/` @ `6a3f678` | `max_rows=∞`, rf+stacking, all_features | 42 | `features_raw_all_w10.csv`，任务 `loro_R2_R4_to_R3` | `/tmp/regression_out`, `/tmp/arm_a_out`（临时，结论已录此处） | RF 与 A 臂（random OOF）逐位复现历史（Δ=0）；B 臂（grouped OOF）macro_f1 0.5455→**0.4907**，gain −0.0693→**−0.1241**，崩溃**加深约 1.8 倍** | 是（E1 预验证） |
| E1-FULL | 2026-08-29 | `code/` @ `1fa5bcc`（结果入库 `3952388`） | `e1_oof_arms.py --tasks all --seeds 42,43,44,45,46`，三臂 A/A′/B，A′ 折数动态对齐 B，all_features | 42–46 | `features_raw_all_w10.csv`，11 个主线任务 | `results/e1_oof_arms/`（e1_arms_raw.csv / e1_decomposition.csv / e1_arms.json） | B 臂崩溃在全部 OOD 任务持续并**加深**（gain 5 种子均值，A→B）：旗舰 `loro_R2_R4_to_R3` −0.066→−0.106；`loro_R2_R3_to_R4` −0.004→−0.223；`loro_R3_R4_to_R2` −0.024→−0.181；`joint` −0.001→−0.104；jitter 三任务 ≈−0.005→≈−0.08；IID 三任务两臂无差（≈−0.002）；唯一例外 `position→R5` 反转为 +0.024。折数效应（A→A′）小而偏正 → 加深源于分组效应。**按 §12 预注册规则走第一分支：现象基础成立，按 X2 继续机制检验**。结论文档：`results/e1_oof_arms/E1_CONCLUSION.md`（含论文引用规则改为 B 口径、R5 反转与 joint 构造性代价两条专门记录） | 是 |
| G0-GRID | 2026-08-29 | `f740362` | `environment_grid_experiment.py` 全量：150 OOD（\|S\|=1/2/3 → 30/60/60 任务）+ 12 IID（6 环境 × random/time_block，§8.4 时间块对照）；模型 rf/xgboost/lightgbm/stacking；stacking 按 §9.1 grouped | 42（脚本常量 SEED，§14 单种子） | `features_raw_all_w10.csv`（缓存复用，无 tshark 重抽） | `results/g0_environment_grid/`：summary_metrics.csv（648 行 / 162 任务）、env_topology_matrix_rf.csv（RF macro-F1 6×6）、逐任务 CM / pred_proba / oof_meta（stacking） | 网格与时间块 IID 对照完成；同质拓扑矩阵已由 D4-TOPOLOGY 行交付。§8.6：网格不用于显著性结论。**2026-08-29 追记（时间块 IID 对照结论与处置，审阅方核数后审定）**：random − time_block 的 macro-F1 差（4 模型跨度）：R6 **+0.155~+0.206**（均值 +0.192；时间块口径下 R6 "IID" 仅 ≈0.67，说明 R6 轮内时间漂移强）、R4 +0.052~+0.068、R7 +0.031~+0.043、R2/R3/R5 ∈ [−0.015, +0.018]。**处置 = §18 预案原文执行**：随机划分 IID 一律标注为 session 内上界（§8.4），正文主结论基于环境外推；历史 110 条**只加注不重跑**——G0 已提供全 6 环境时间块数值，重跑徒增版本漂移风险（当日两起浮点事件为证）。README 历史基线表已同步加注 | 支撑（§8.6） |
| D4-TOPOLOGY | 2026-08-29 | `1038f3b` | `six_env_confusion_similarity.py`（§20.2 重写）读 G0 \|S\|=1 + 双 IID 参照；口径 = `cpd_y(ref=CM_iid(i), tgt=CM_{i→j})`，见 EXECUTION_PLAN D4 | —（确定性分析） | `results/g0_environment_grid/raw_all/` 下 30 个有序对 + `g0_iid_R*_{time_block,random}` 的 rf/all_features 混淆矩阵 | `env_topology_cpd_y_ref_{time_block,random}_rf.csv`、`env_topology_macro_f1_from_cm_rf.csv`、`env_topology_cpd_dir_*`、`TOPOLOGY_MATRIX_NOTE.md` | 同质 6×6 替代废弃六环境矩阵。macro-F1 核对 30/30（最大偏差 1.1e-16）；审阅方独立重算抽查逐位一致；CPD_dir 在 n_err≥20 下仅 14/30 有定义（缺失主要来自 IID 参照侧误差过少，与 P0 §6 同向）；test_cpd_core 15/15、test_oof_modes 17/17 全绿。旧 `six_env_off_diag_frobenius_rf.csv` 原地保留作 0.1521 回归锁定源（legacy 迁移废止，见 CPD_DEFINITIONS §5.2）。**2026-08-29 追记**：5 个拓扑 CSV 在 canonical 环境 iotcls 下重生成（初版产自 anaconda base；ULP 差异 9/30 格、max 2.22e-16，根因 numpy 1.23→2.4 fro-norm 归约路径变化；1e-6 硬门两版均过，同解释器逐位可复现）——裁定与环境政策见 EXECUTION_PLAN v1.3 D9 追记 | 支撑（§8.6） |
| S1-DEEP-5SEED | 2026-08-29 | 起跑于工作区 @ `042fa03`（实验脚本零改动，仅 CLI 参数） | §14 入口 + 三项必要 CLI 偏离：`--output-root results/s1_deep_5seed_20260829/seed{N}`（默认路径会因 metrics.json 已存在而静默短路复用 seed 42 → std=0）、`--cnn-v5-source legacy/results/extreme_capacity_1p2m_20260706/raw_all`（默认源已迁 legacy）、解释器 `~/anaconda3/envs/iotcls/bin/python`（torch 2.5.1+cu121；base 无 CUDA 会静默跑 CPU）；`PYTHONPATH=code/scripts/analysis` | 42–46。预检通过：splits 逐字节复制（diff 无差异）、`--random-state` 经 set_seed 影响初始化 + DataLoader 批次顺序，**不影响划分**——语义记为"划分固定、训练随机性（初始化+批序）变"，符合 §14 意图（其补救条款仅针对影响划分的情形） | `results/robustness_scaling_20260706_v2/splits`（固定划分） | `results/s1_deep_5seed_20260829/seed{42..46}/`（各 40 行 = 8 任务 × 5 模型）+ 聚合 `s1_multiseed_summary.csv` / `s1_all_seed_rows.csv` / `s1_aggregation_metadata.json`；聚合脚本 `code/scripts/analysis/s1_aggregate_multiseed.py`；日志 `results/s1_deep_5seed_logs/` | **已完成（5 种子全部 EXIT=0）**。锚点：seed 42 的 single_round_R2 三候选复现 7 月历史值至 4 位小数。**聚合警告已实测证实**：`cnn1d_v3` 与 `cnn1d_v5` 逐任务跨种子 std **恒为 0**（脚本 `copy_existing_result` 路径复制的单种子参照），聚合脚本以此为判据自动剔除其 ±std 并标注 `single_seed_reference_copied`——直接对全表求 std 会得出"v3/v5 极稳定"的错误结论。真 5 种子候选仅 `cnn1d_convnext` / `cnn1d_inception` / `cnn1d_tcn`，**种子噪声量级（跨任务平均 std）分别为 0.0163 / 0.0102 / 0.0110 macro-F1**——该量级是解读架构差异的必要尺度（多数任务上三者差异在 1 std 内）。逐任务 mean±std 见聚合表 | 是（§2.2 支撑实验；架构比较须带种子噪声尺度） |
| E1-G0-GRID | 2026-08-29 | 运行时工作区 @ `b9a4ddb`（`e1_oof_arms.py` 扩展与产物随本行入库；原版备份 /tmp 供 diff） | `e1_oof_arms.py --grid all --seeds 42`，3 分片 × n_jobs=8（实测 22.4 核，54.6 分钟）；150 个 G0 OOD 任务 × 三臂 × all_features；B 臂折叠按训练轮次数判定（\|S\|≥2 轮次分组 / \|S\|=1 时间块，`SINGLE_ROUND` 名单已删除）；任务定义 import 自 G0 生成器；§19.2 provenance（git、argv、包版本、逐任务折叠记录、耗时）落盘并在合并时跨分片强校验 | 42（追加 43–46 **裁定不做**：单种子已满足 §14 G0 行覆盖语义——见协议 §23 2026-08-29 澄清行；且输出 stem 不编码种子，追加会覆盖现有产物） | `features_raw_all_w10.csv` + G0 任务生成器 | `results/e1_oof_arms_g0/`：e1_arms_raw_all.csv（450 行）、e1_decomposition_all.csv、e1_arms_all.json、provenance_all.json、g0_consistency_all.json、shards/ | smoke 门禁 8/8 逐位通过（含与主线 loro_R2_R4_to_R3 三臂逐位一致 → 主线路径零回归）。全量一致性 597/600 ≤1e-6（基模型 450/450，max 5.6e-17）；**3 个 \|S\|=3 任务 stacking 差 3.3e-4–2.0e-3，审定接受**：诊断为 LightGBM 折内 ~1e-15 线程序浮点差（G0 与 E1 线程拓扑不同）经 lbfgs 元学习器放大；E1 同配置重跑逐位一致，**§18"同配置复跑不一致"未触发**；G0 落盘值仍为 G0 权威、E1 三臂同环境内部自洽；跨线程拓扑 stacking 复现界 ~2e-3 记为可复现性事实（非 §22.1 容差放宽），任何精确复现声明须钉死 num_threads。结果（描述性，§8.6）：A→B 加深在 \|S\|≥2 全网格成立（gain 均值 \|S\|=2：−0.039→−0.093；\|S\|=3：−0.034→−0.054）；\|S\|=1（时间块 B）无加深（−0.076→−0.070）→ 加深特异于跨轮分组 OOF；源环境越少加深越深。详见 `results/e1_oof_arms/E1_CONCLUSION.md` §8 | 支撑（§8.6）；E2/G1 输入 |
| P2-UNSW-PILOT | 2026-08-29 | 工作区 @ `0e5e791`（`extract_features_generic.py` 与 pilot 脚本随本行入库） | §16.3 五问 gate。4 天 pcap（09-23 / 09-30 / 10-11 / 10-12，约 11GB，直连 ~10MB/s 计约 12 分钟）；提取器 61 维通用特征（94 − 33 项 802.11 专属 = 61，对账闭合）；RF LORO import 主线 `build_model` / `sample_balanced`（§7/§16.4，不重实现）；脚本在 `code/scripts/analysis/unsw_pilot/` | 42 | `dataset/unsw/pcap/*.pcap` + `dataset/unsw/device_mac_map.csv`（入库副本见 `results/unsw_pilot/PROVENANCE_NOTE.md`） | `results/unsw_pilot/`：PILOT_FIVE_QUESTIONS.md、FEATURE_ALIGNMENT.md、INVENTORY.md、loro/、four_day/、BELKIN_PROBE.md、CSV_CROSSCHECK.md。特征缓存 `features_*.csv` 为可再生派生物且合并档 124/168MB 超 GitHub 单文件硬限，按 .gitignore 注释排除（各 run_meta.json 入库） | **五问全部通过（gate PASS，无需换候选）**：① MIT-0，论文使用与派生特征发布均可；② MAC 映射完整、四天零未知设备（唯一未列 MAC 为网关兄弟接口 `…:e9`，仅作 dst）——**限制进正文：23 个 IoT MAC 仅 11 个天天在线、10 个天天 ≥100 窗（测试床后期下线所致，10-11 全天对照排除短抓包与标签污染两种解释），§16.4 扩展必须按日交集取类并逐任务报实际类别数，不得笼统写 18 类**；③ 09-30 与 §16.1 预核数字逐项一致（20 活跃 / 18 ≥100 窗 / 18 ≥300 窗 / Dropcam 8640；CSV 清点交叉核对 673,414 / 86,398 / 84,291 逐位复现）；④ pcap 时间戳微秒级（唯一率 1.000000 vs CSV 0.125），61 特征 0 NaN / 0 inf；`side_/other_packet_ratio` 两列 Ethernet 上恒 0 仅作列对齐，**禁止进入跨数据集特征重要性比较**；⑤ 6 有序日对 LORO macro-F1 0.8164–0.8674（均值 0.8466，任务实际 10–14 类），Belkin Switch↔Motion 混淆经判别检验排除标签互换（域内即最难两类、双向混合形态，证据 BELKIN_PROBE.md）。执行决定：10-12 实为 14.48h 抓包，保留（真实末日、最大跨度）并加录 10-11 全天对照。相邻日对分数最高（0.8901/0.8778）、远隔 19 天对最低的梯度已记录不解读（属 §16.4 检验 1）。提取成本 ~11 min/天（窗口级 pandas 开销），20 天全量前需按天并行化 | 是（P2 gate 交付物） |
| D8-VOTING | 2026-08-29 | `c70fab1`（脚本+产物入库；运行时工作区 @ `b9a4ddb`，run_metadata 记 dirty）→ `1db263c`（NOTE 自核订正） | `voting_baselines.py` 纯后处理（无重训、无随机数）：§7-4 hard voting（多数票，平局 `argmax(votes + 0.5·mean_proba)`，平局实测占窗口 0.696%）、§7-5 等权 soft voting、§7-6 温度校准 soft voting（每任务×每基模型在**源域 OOF** 上以 NLL 拟合标量 T，486 个全收敛、0 触界，测试侧只施加；函数签名结构性无泄漏）；指标 5-class / 4-class / 最差类 F1 / accuracy（§10）；`nice -19` 单进程 15.4s，双跑 md5 相同 | 无（全流程确定性，`random_seed=null`） | G0 全部 162 任务的 predictions / pred_proba / oof_meta | `results/g0_environment_grid/voting_baselines.csv`（1296 行 = 162 × 8 method，`method_kind` 区分投票/对照行）、`voting_calibration_temperatures.csv`（486 行审计）、`VOTING_BASELINES_NOTE.md`、`voting_baselines_run_metadata.json` | 自检 648/648 逐位一致（**加严**至含 stacking 与逐类 F1，偏差恒为 0.0；超差即退出不写产物）；独立重算 soft-equal 抽查逐位一致（收尾会话与 Fable 各一次，后者 `g0_R2_R4_to_R3`=0.548005 逐位吻合）。§7 基线 4–6 补齐，X2/X4/G1 必备对照就位。OOD 150 任务 5-class 均值：RF 0.6870 > 校准 soft 0.6670 > hard 0.6595 > 等权 soft 0.6559 > Stacking 0.6280（oracle 最佳单模 0.7009）。gain_vs_best_base 均值（n_sources=0/1/2/3）：stacking −0.003/−0.070/−0.093/−0.054；**校准 soft −0.003/−0.067/−0.029/−0.023**；hard −0.002/−0.073/−0.034/−0.033；等权 soft −0.002/−0.081/−0.037/−0.035——三投票法均不超 oracle，但 \|S\|≥2 上一致优于 stacking（校准 soft 逐任务 101/162 胜，均值 +0.036）；校准>等权稳定（119/162，+0.010）。**G1 内层景观（任务级，非 G1 判据）**：三候选事后最优分布 RF 67 / soft_equal 48 / stacking 35（非"永远 RF"退化）；固定策略平均 regret：always-RF 0.0203 / always-soft 0.0514 / always-stacking 0.0793。校准 T 观察（留给 X2/G1）：lgb/xgb 源域 OOF 系统性过自信（T 中位 5.24 / 2.63，162/162 任务 T>1），RF 近校准（0.96）。审阅裁定（Fable 追认收尾会话三项处置）：①对照行保留（`method_kind` 防重复计数，NOTE §7.1 警示）；②最差类 F1 双口径均保留（§10 未定死，出表再选）；③NOTE 初稿一处精度表述订正（"T=1 还原 3.6e-12"→落盘精度限制下 ~1e-7）合规，脚本未改以保持产物溯源。**待决（G1 规格 D10 定死，接触任何外层评估前）**：G1 候选"soft voting"取校准版 / 等权版 / 或按 §8.3 折内内层选择 | 是（§7 基线 + G1 候选动作数据源） |
| E2-CONDITIONAL | 2026-08-29（**运行前登记**） | 规格 = EXECUTION_PLAN v1.2 D9（先写后看：本行与 D9 的 commit 先于任何 E2 数值产生） | §13 分层回归 M0/M1/M2。DV = E1-G0-GRID 的 B 臂 gain（150 任务，seed 42；3 个 ~1e-3 差异任务用 E1 值）；M0 = RF 逐类 recall 变化（参照 = 源集 G0 time_block IID 均值）+ L2 + max；M1/M2 加 CPD_y / CPD_dir（ref 同参照，多源构造沿用 0.8397 历史口径，只经 cpd_core；CPD_dir 按 §4.3 n_err≥20，M2 与其 M0 对照同子集拟合）；双通道 = 标准化 OLS 增量 R²（环境聚类 bootstrap B=10000）+ 留一目标环境 CV 的 MSE 改善；敏感性 = A 臂 DV / random IID 参照 / 三基模型均值 recall | 42（bootstrap） | `e1_arms_raw_all.csv` + G0 各任务与 IID 的 rf confusion_matrix.csv + `cpd_core` | `results/e2_conditional/`（回归表、逐环境结果、M2 覆盖率、bootstrap 明细、provenance、E2_RESULTS_NOTE.md） | **已完成并判定（2026-08-29，判定书 `results/e2_conditional/E2_CONCLUSION.md`）**。验收四门全过（M0 手算 0 偏差；CPD_y 与 D4 拓扑 30/30 逐位；bootstrap 双跑 md5 相同；行数 9/9）。终值（primary）：M1(+CPD_y) ΔR²=+0.0066、f²=0.008、β −0.245 [−0.757,+0.320] 跨零、LOEO-CV 变差 4.6% → **CPD_y 增量解释力消失**；M2(+CPD_dir, lenient n=119) ΔR²=+0.0748、f²=0.091、β −0.296 [−0.556,−0.175] 稳健但 LOEO-CV 变差 3.9% → 条件关联成立、**不满足机制性量门槛**（strict n=26 全数秩亏仅并列）。**判定走 §13 降级分支**：贡献 2 降为"失效模式的结构化描述"，主线转贡献 3（部署方法），H2 强形式不成立、CPD_y 上判否条件触发。敏感性要点：A 臂（历史口径）DV 下增量虚高（M1 +0.267 / M2 +0.482）——旧口径抬高 CPD-gain 表观强度，进方法学讨论 | 是（判定 + 结构化描述素材；引用规则见判定书 §3/§4） |

> **修订记录（2026-08-28）**：本行首次登记时误记 B 臂 macro_f1 为 0.5498（"崩溃略软"）。
> 该数字来自 `_splitter` 的一个缺陷：多轮分组分支缺 `return`，执行掉进单轮时间块分支并覆盖
> GroupKFold 结果，实际跑的是时间块 OOF 而非协议 §9.1 的轮次分组。修复后真实值为 0.4907，
> **方向与原记录相反**。修复见 commit `6a3f678`，严格化测试 `code/scripts/analysis/test_oof_modes.py`（11 项断言）。
>
> **注意（待你决定）**：`loro_R2_R4_to_R3` 只有 2 个源轮次（R2、R4），`GroupKFold` 的
> `n_splits` 因此退化为 **2**——每折用单轮训练、另一轮验证。这是协议 §9.1「group=round」的
> 直接后果，但元特征噪声显著大于历史的 5 折。E1 正式跑之前需确认这是否是期望口径。
>
> **决定（2026-08-29，Fable 5 审定，详见 `EXECUTION_PLAN_20260829.md` D1）——本待决项关闭**：
> 确认 2 折退化为协议 §9.1 的期望口径。理由：① 2 轮条件下按轮分组是唯一无泄漏实现；
> ② 折数混淆由 A′ 臂（折数动态对齐 B）显式控制，E1-FULL 实测折数效应 ≈ +0.008（小而偏正），
> 崩溃加深全部来自分组效应；③ 轮内时间块凑折数的替代方案混合两种分组语义，偏离 §9.1 文本。
> E1-FULL 结果按此口径有效。



| G1-ROUTE-A | 2026-08-29 登记 / 2026-08-30 判定 | 规格 = EXECUTION_PLAN v1.4 D10（先写后看：本行与 D10 的 commit 先于任何 G1 数值；含 §17.1 红线重读结论——E2 降级不改变任何判据，一字不调） | 嵌套 LOEO（§9.2）：外层 6 fold × 25 任务（目标环境 = e_out）；内层池 = **严格全含** 70 任务/fold（源与目标均不含 e_out）；候选 = RF / Stacking(B 口径) / soft voting（等权 vs 校准由内层选，§8.3）；选择器 = **UDS 单调双阈值**（唯一预注册家族，[Stacking, soft, RF] 风险序），内层穷举 (变体, t1, t2) 最小化内层平均 regret（并列打破：内层最差环境 regret → 更保守）；判据 = §17.1 五条，regret 聚合主口径 = 环境等权；条件 4 = ≥1 外层任务选非 RF 且 regret ≤ **τ_repro = 2e-3**（v1.5 运行前修订，τ 取自本表 E1-G0-GRID 行实测复现界，动因 = 精确零判定与复现界相撞：三候选 top-2 差 <2e-3 占 8.0%/5.3%；τ=0 严值并报）；UDS 只经 cpd_core.uds、不读标签（判据 5 审计） | 无（全流程确定性，零重训） | G0 + D8 落盘产物（predictions / pred_proba / oof_meta / voting_baselines.csv）+ `cpd_core.uds` | `results/g1_route_a/`（逐任务明细、逐环境 + 汇总 regret、win/loss、各 fold 阈值与变体、无泄漏审计、provenance、G1_RESULTS_NOTE.md） | **已完成并判定（2026-08-30，判定书 `results/g1_route_a/G1_VERDICT.md`）：路线 A 未通过 → 转 §17.3 路线 B**。五条判据：①外层平均 regret 0.02469 > 0.01 **失败**；②最差环境 R7 0.03611 > 0.03 **失败**；③vs always-Stacking(0.07925) 改善但 **vs always-RF(0.02025) 反而差 +0.00444 失败**；④非 RF 且 regret ≤ τ_repro 共 7 任务（τ=0 为 6，落 R4/R7）**通过**；⑤UDS 无标签审计（12 函数 0 标签 token）**通过**。失败性质：各 fold **内层最优** regret 0.0081–0.0238，5/6 fold 样本内即达不到 0.01 → 是家族/信号问题而非外层泛化；选择器相对 always-RF 逐任务 8 胜 16 负、125/150 选 RF；但 oracle 分布 RF 63 / soft 49 / stacking 37 / 并列 1 且 oracle 均值高于 always-RF → **头顶空间真实存在、UDS 抓不住**（H3 判否触发，H4 半failed 半passed，如实并列）。验收硬门全过（候选 F1 300 项逐位、元数据三源 450 项、fold 结构自检、确定性双跑）。移交路线 B 的关键事实：真实竞争对手是 always-RF（0.02025，其最差环境 0.03182），比协议通过线 RF−0.005 暗示的更严苛 | 是（负结果进正文/附录；贡献 3 改为路线 B） |

| ROUTE-B | 2026-08-30 登记 / 2026-08-31 判定 | 规格 = EXECUTION_PLAN v1.6 D11（先写后看：本行与 D11 的 commit 先于任何路线 B 数值） | §17.3 环境稳健加权聚合，**全局逐基模型权重**（纯洁性条款：禁止用 M2 事实卡的类别条件结论改造设计，类别条件版本归路线 C）。骨架复用 D10 嵌套 LOEO（外层 6×25 / 内层严格全含 70）；权重 = `exp(lb/τ)·max(1−γρ,0)` 后向均匀收缩 λ，`lb` = 源域 OOF 各留出折 macro-F1 最小值、`ρ` = 源域 OOF 错误指示相关均值；内层网格 180 组、目标 = 内层环境等权平均 5-class macro-F1（非 regret，因 §17.3 通过线为 F1 口径）；source-only（权重不含任何目标侧量，审计门） | 无（全流程确定性、零重训） | G0 落盘 `oof_meta.csv` / `pred_proba.csv` / `metrics.json` + D8 `voting_baselines.csv` | `results/route_b/`（task_detail / env_summary / fold_params / weights / passline / acceptance / provenance / NOTE） | **已完成并判定（2026-08-31，判定书 `results/route_b/ROUTE_B_VERDICT.md`）：未通过 §17.3**。五道硬门全过（λ=1 逐位复现 D8 等权 soft 162/162、偏差 0.0；候选 F1 300/300；双跑 5/5 md5 一致；fold 自检；source-only 审计）。三条通过线：①环境等权平均 macro-F1 **0.661379 vs RF 0.687025，缺口 −0.020646**（容差 0.005 的四倍）→ **失败**；②最差环境 R5 0.617007 > Stacking 0.594361 → 通过；③5/6 环境 vs Stacking 正增益（仅 R3 为 −0.0072）→ 通过。排名：RF 0.6870 > 校准 soft 0.6670 > **路线 B 0.6614** > hard 0.6595 > 等权 soft 0.6559 > Stacking 0.6280；路线 B regret 0.0459 = always-RF（0.0203）的 2.3 倍，且**不及 D8 温度校准 soft voting**。失败性质：内层各 fold 最优 γ **全部取边界 1.0**、λ 多为 0 → 该族在给定信号下已用尽仍不足（容量问题，非实现问题）；仅在 R6/R7 胜过 RF（恰为时间块泄漏最重的两环境）。跨线一致性（事后解释，非判定依据）：独立线 M2 测得全局块缩放仅恢复 67.5%、类别条件 89.1%，与「全局分辨率不足」同向；§17.3 设计未被 M2 污染（D11 纯洁性条款）。**处置**：§17.1 路线 A 与 §17.3 路线 B 均未通过 → 贡献 3 目前无成立方法，两者如实作负结果；路线 C（类别条件、无标签）为唯一剩余候选，须自带冻结协议 + 开发用自采、确证在 UNSW 一次性 + 报告方法族多重性，且通过线必须对标 RF（0.687025 / regret 0.020275）。**透明度**：因 API 中断，实现脚本由判定人本人编写（实现与判定同一方），五道门均为机器可复算的客观检查 | 是（负结果进正文/附录） |

## 待登记（已排期，执行口径见 `docs/EXECUTION_PLAN_20260829.md`）

（当前无待登记项。）

2026-08-29 全部完结并在主表登记：D4-TOPOLOGY、P2-UNSW-PILOT、D8-VOTING（两次登记于
2026-08-29 晚合并为一行，内容取并集，无信息丢失）、S1-DEEP-5SEED、E1-G0-GRID、
E2-CONDITIONAL（判定见其行与 `results/e2_conditional/E2_CONCLUSION.md`）。

已完成并转入正式登记：G0 网格全量、时间块 IID 对照（并入 G0-GRID 行）、E1 全部 11 任务（E1-FULL 行，
含原"剩余 4 个任务"——其 B 臂为时间块口径，`b_split_basis` 字段已单独标注）。

## 维护规则

1. 每次实验**开工前**登记一行（ID 可先用日期前缀），跑完补「结论」。
2. commit 列填 `code/` 仓库的 `git rev-parse HEAD`；纯分析脚本改动也要先提交再跑。
3. 「进正文」= 该实验的数字可能出现在论文正文；改为「否」需注明理由。
4. 临时验证（如 `/tmp` 里的 smoke）若结论有价值，登记时在输出列注明"临时，结论已转录"。
